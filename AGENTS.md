# fantastic-editor 开发协作约束

## Ponytail 默认规则

- 编码、修 Bug、重构、设计和依赖选择默认按 ponytail `full` 执行：先确认需求是否必须存在，再复用仓库已有代码，随后优先 Node/TypeScript 标准库和 Electron/Windows 原生能力，最后才新增最小实现。
- 不新增只有一个实现的接口、工厂、包装层或“以后可能用到”的配置；删除或收缩优先于扩展。任何安全校验、数据完整性、错误处理和无障碍要求不得为追求少代码而删除。
- 修改 Bug 前先追踪共享函数的全部调用者，修根因；非平凡分支、循环、解析、安全或发布逻辑必须留下一个可运行的最小测试/自检。
- 有意保留的简化必须用 `ponytail:` 注释写明上限和升级触发条件，避免延期腐烂。

## 审查节奏

- `ponytail-review`：针对待提交 diff，只审过度设计，不替代正确性、安全或性能审查。
- `ponytail-audit`：跨里程碑做全仓复杂度审计，只输出删除/收缩建议，不自动改代码。
- `ponytail-debt`：发布前收集全部 `ponytail:` 标记；没有标记时明确记录为 clean ledger。
- `ponytail-gain` 的数字是外部基准，不得推算为本仓库节省量；本仓库只报告真实测试、行数和延期标记。
- `ponytail-help` 只用于查询命令和模式，不改变项目状态。

## 项目边界

- canonical Markdown、解析/资源安全边界、公众号只创建草稿、代码签名门禁和现有测试要求优先级高于“少写几行”。
- 当前公众号范围固定为单账号；多账号、直接发布和群发必须另行授权，不因简化实现而偷偷扩大范围。
