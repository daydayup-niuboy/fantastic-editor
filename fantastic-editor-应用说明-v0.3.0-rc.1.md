# fantastic-editor 应用说明

**版本：v0.3.0-rc.1（发布候选）** · 发布日期：2026-08-31 · 平台：Windows x64

fantastic-editor 是一款面向 Windows 的**本地优先 Markdown 编辑器**：所有编辑、预览、导出都在本机完成，不依赖任何云服务；可选接入微信公众号官方 API 实现草稿同步与一键发布。适合写公众号文章、技术文档、带公式和流程图的笔记，并需要导出 PDF / Word / 离线 HTML 的用户。

---

## 1. 版本与下载

发布页：<https://github.com/daydayup-niuboy/fantastic-editor/releases/tag/v0.3.0-rc.1>

| 安装包 | 说明 | 大小 | SHA-256 |
|---|---|---|---|
| `fantastic-editor-0.3.0-rc.1-setup.exe` | 安装版，含安装/卸载向导 | ≈130 MB | `CCD3212F…FFAB1`（完整值见发布页） |
| `fantastic-editor-0.3.0-rc.1-portable.exe` | 便携版，单文件免安装，适合 U 盘携带 | ≈130 MB | `1ED7305A…BC05B`（完整值见发布页） |

两个产物均已通过隐私、生产态和安装/启动/卸载门禁。

> ⚠️ **首次运行提示**：当前版本未配置 Authenticode 代码签名证书，Windows 可能显示"未知发布者"或 SmartScreen 拦截。请核对 SHA-256 后，点击"更多信息 → 仍要运行"。后续版本计划接入签名。

**系统要求**：Windows 10/11 x64。打包版自带运行环境，普通用户**无需安装 Node.js**；从源码运行才需要 Node.js ≥ 24。

---

## 2. 安装

- **安装版**：双击 setup.exe，按向导完成安装，自动创建开始菜单与桌面快捷方式，支持控制面板卸载。
- **便携版**：双击 portable.exe 直接运行，不写注册表，可放在任意目录或 U 盘。

两者均内置 Electron 运行时，首次启动即进入欢迎页。

---

## 3. 界面与基础操作

### 主界面

- **欢迎页**：显示最近打开的文件（仅显示文件名，不暴露绝对路径，隐私优先）。
- **多文档选项卡**：同时打开多个 `.md` / `.markdown` 文件，各选项卡独立保留未保存草稿；标签可拖动排序，或用 `Alt+Shift+←/→` 移动。
- **资源管理器**：文件夹工作区下可在侧栏浏览并打开文件。
- **三种视图**：编辑 / 分栏 / 预览，分栏宽度可拖动调整。
- **主题**：浅色/深色主题，选择记忆在本机；预览字体可选（微软雅黑、Segoe UI、Arial、等线、宋体、楷体）。
- **同步滚动**：明确的 ON/OFF 开关。开启后编辑区滚动驱动预览区（单向：编辑区 → 预览区）；框选源码时预览显示对应块级范围提示框。
- **状态栏**：实时显示当前快照的 Worker 解析耗时与资源解析耗时，大文档有分级提示。

### 打开与保存

- 主界面"新建"按钮创建空白文档；支持窗口级拖拽打开，一次可拖入多个 Markdown 文件。
- 自动保存**恢复快照**：异常退出后重启自动恢复打开的标签和未保存草稿；若原文件已被外部修改，保存时触发冲突保护；若原文件已丢失，草稿以未命名文档恢复。
- 未命名文档需先"另存为"才能执行导出等操作。

---

## 4. 编辑功能

### 双模式编辑

编辑区可在"**源代码**"和"**所见即所得**"之间切换，选择保存在本机。两种模式共用同一套撤销/重做历史，保存与全部导出始终读取同一份 Markdown 原文——所见即所得不会产生"导出内容与源码不一致"的问题。

### 所见即所得模式

- **可直接编辑**：标题、普通段落、引用段落、列表项（含含子列表的父项正文）、表格单元格；段落/列表/表格中含公式、图片、链接或行内代码时，可直接编辑这些结构前后的普通文字，未触及的行内 Markdown 逐字符保留。
- **快捷键**：`Ctrl+B` 粗体、`Ctrl+I` 斜体、`Ctrl+K` 链接。
- **段落**：`Enter` 新建段落，`Shift+Enter` 软换行；段首 Backspace 与段尾 Delete 合并相邻块；点击文档空白处新建段落。粘贴纯文本统一为 LF，中文输入法组合期间不会提前写回。
- **列表**：`Tab` / `Shift+Tab` 调整层级；可对当前项及全部子项整树缩进、提升、上下移动。
- **表格**：工具栏插入/删除行列、设置列对齐；`Tab` / `Shift+Tab` / `Enter` 切换单元格，最后一个单元格按 `Tab` 自动追加空白行。任务清单点击勾选框切换完成状态。
- **结构块操作**：选中块后可上移、下移、复制、确认删除，或在下方插入常用 Markdown 块；也可拖动手柄或按 `Alt+↑` / `Alt+↓` 移动完整源码块。所有块操作保留原始空白并作为一个撤销项提交。
- **跨块编辑**：可跨标题、段落、引用和列表项框选，执行删除、复制、剪切、多段粘贴及跨块粗体/斜体/删除线；遇到图片、公式、代码、Mermaid、表格等受保护结构会在修改前拒绝，不会破坏原文。

### 图片 / 公式 / Mermaid

- **图片**：直接拖到编辑区指定位置，或用"插入图片"按钮放到光标处。应用自动校验文件签名与容量，以"安全化名称 + 内容哈希"复制到 Markdown 同级 `assets/` 目录，插入相对引用并同步预览。支持 PNG、JPEG、GIF、WEBP、SVG；重复图片按内容哈希复用。属性面板可修改 alt、替换或删除（删除只移除引用，保留 assets 原文件）。
- **公式**：LaTeX 面板配合 KaTeX 即时预览；导出时无网络的隔离 Chromium 窗口内生成 PNG，输出中不携带任何脚本。
- **Mermaid**：`mermaid` 代码块在预览中实时渲染流程图；导出时同样生成 PNG 派生图（公众号等位图输出最高 2 倍像素），不携带 Mermaid 运行时。

---

## 5. 导出

统一导出菜单，全部导出前会等待字体与图片加载完成：

| 格式 | 特性 |
|---|---|
| **PDF** | A4 纵向分页；处理标题孤页、表头重复、长代码换行、单页图片；过宽表格和公式在可读范围内缩放，仍可能裁切时**明确失败**而非静默截断 |
| **Word (.docx)** | A4 固定页面；标题用可编辑的 Word 标题样式；列表用原生编号；表格确定性列宽、重复表头、行不拆分；正文全程可编辑 |
| **单文件离线 HTML** | 内嵌 KaTeX 字体与样式、已授权图片；保留浅色/深色主题；响应式表格与长文本断行；通过 CSP 与最终安全审计**阻止脚本及本地地址** |

---

## 6. 微信公众号发布

### 6.1 方案 B 兼容复制（无需任何 API 权限）

公众号对正文内的图片、公式有特殊处理要求。fantastic-editor 采用"正文一次复制 + 占位符逐项替换"的稳妥方案：

1. **正文复制**：首个一级标题自动作为公众号建议标题，不重复进入正文；普通图片、块级公式和 Mermaid 生成 `【FE类型NN｜整段替换】` 占位，行内公式生成 `【FE公式NN｜行内替换】`。
2. **替换助手**：按占位符逐项"复制图片 → 到公众号粘贴"，正文复制、替换图片复制、实际完成替换是**三个独立状态**，逐项确认图片出现且占位文字消失。
3. **验收**：替换完成后按顺序完成保存、重开、移动端预览；全部人工步骤完成后可保存一份隐私最小化的 Markdown 验收记录。

> ⚠️ 粘贴后**不要**再使用公众号自带的"一键排版"，它会覆盖列表和代码块样式。

### 6.2 发布主题（三套原创预置）

正文可选"**微信原生增强**""**极简墨白**""**深蓝科技**"三套主题。主题只改变排版表现，不修改 Markdown、图片/公式/Mermaid 替换顺序或任务完成状态；最终主题 ID 写入验收记录。导出菜单内置手机预览：320 / 375 / 414px 三档宽度自动并行检查横向溢出、局部滚动、过小文字、文字对比度和标题间距，可逐档查看明细。

### 6.3 API 批量同步与一键发布（v0.3.0 新增）

已配置公众号 API 凭据后，发布助手提供两个入口：

- **一键同步到公众号草稿箱**：主进程批量上传普通图片、公式 PNG、Mermaid PNG，替换为微信持久图片 URL，上传封面并创建草稿，再回读校验——无需逐张复制粘贴。
- **一键发布到公众号**：明确二次确认后调用微信发布接口并轮询结果，只有确认成功才显示"已发布"。

**公众号设置**（主界面顶部常驻入口）：

- 填写 AppID、AppSecret、选择默认封面；AppSecret 使用系统级加密（Electron safeStorage）保存，重新打开配置**不回显明文**。
- 自动检测凭据及 **IP 白名单**：若微信返回 `40164 invalid ip`，界面直接显示并允许复制微信识别到的当前公网 IPv4，提示到微信开发者平台加入白名单。家庭/办公网络 IP 变化后可在同一位置重新检测。

**范围约定**：产品固定为单账号、单篇发布，不做多账号、群发、定时群发及第三方平台授权。没有 API 权限时仍可使用方案 B 兼容复制。

---

## 7. 数据安全与隐私

- **本地优先**：编辑、预览、导出全部在本机完成；唯一联网场景是你主动配置公众号 API 后的草稿同步/发布，以及微信 IP 白名单检测。
- **脚本隔离**：Mermaid/KaTeX 在无网络隔离窗口渲染，离线 HTML 经 CSP 与安全审计，输出不含可执行脚本；源 Markdown 的 data URI 固定阻止，导出 Data URI 只能由主进程校验后的本地资源生成。
- **最近文件不暴露路径**：渲染进程只接收不透明 ID、文件名和时间。
- **凭据加密**：AppSecret 系统级加密存储，界面不回显。
- **质量门禁**：42 个测试文件、224 项测试通过；打包版逐项运行 7 组生产态冒烟（启动、PDF、DOCX、离线 HTML、公式 PNG、Mermaid PNG、完整 UI）。

## 8. 容量与性能边界

| 项目 | 上限 |
|---|---|
| 单篇文档字符数 | 1,000 万 |
| 单个 Markdown 文件 | 40 MiB |
| 单篇资源引用数 | 10,000 个 |
| 单个资源文件 | 50 MiB |
| 单次导出资源总预算 | 200 MiB（去重后） |
| 单次需图片化的公式 | 500 项 |

超出边界会**明确失败或给出阻断诊断**，不会静默截断。大文档自适应延长防抖，新解析终止旧 Worker。

## 9. 已知限制（当前版本）

- 同步滚动为单向（编辑区 → 预览区），预览滚动不反向驱动编辑器；选区提示为块级语义范围。
- 普通复制暂为纯文本（`text/plain`）；通用 HTML/Markdown 双格式复制与智能粘贴列入 P1。
- 表格不支持合并/拆分单元格（继续走安全降级）；WYSIWYG 冻结为 P0 编辑基线，新增语法进入 P1。
- 未配置代码签名证书：首启会遇 SmartScreen 提示（见第 1 节）。
- 撤销 Markdown 中的图片引用不会自动删除 assets 里的图片文件（防止误删）。

## 10. 反馈

- 问题反馈：<https://github.com/daydayup-niuboy/fantastic-editor/issues>
- 联系邮箱：niuboy5188@gmail.com（作者：Tbin）

---

*本文档基于 v0.3.0-rc.1 发布说明与仓库 README（2026-08-31）编写。*
