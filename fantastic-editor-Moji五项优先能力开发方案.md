# fantastic-editor：Moji 五项优先能力开发方案

> 文档状态：开发设计草案
> 编写日期：2026-08-31
> 适用基线：`fantastic-editor 0.3.0-rc.1` 及其后续开发版本
> 核心约束：只做增量增强，不削弱现有编辑、导出、公众号、安全、恢复和发布门禁

## 1. 文档目标

本方案聚焦以下五项能力：

1. 文档大纲与标题导航
2. 统一查找与替换
3. 可调阅读宽度与预览字号
4. 代码块一键复制
5. Windows Markdown 文件关联与单实例打开

五项能力来自对 Moji 阅读体验和桌面产品体验的对照，但实现必须服从 fantastic-editor 已有架构，不能把 Moji 的实现直接移植进来。

本轮不调整 Markdown 语法范围，不改变 UDM 版本，不重写编辑器，不改变导出格式，不扩展公众号账号或发布范围。

## 2. 总体结论

### 2.1 产品优先级

| 顺序 | 能力 | 用户价值 | 主要原因 |
| --- | --- | --- | --- |
| 1 | 文档大纲与标题导航 | 高 | 长文写作和阅读的基本导航能力，现有 UDM 已提供标题与 SourceRange |
| 2 | 统一查找与替换 | 高 | 所有编辑器的高频基础能力，当前缺口明显 |
| 3 | 阅读宽度与预览字号 | 中高 | 直接改善阅读舒适度，改动面小 |
| 4 | 代码块一键复制 | 中 | 技术文档高频操作，容易形成可感知的体验提升 |
| 5 | 文件关联与单实例打开 | 中高 | 提升 Windows 原生应用完整度，但涉及启动、恢复和未保存内容保护 |

### 2.2 推荐开发顺序

产品优先级不等于工程交付顺序。为降低回归风险，推荐按下面顺序实现：

1. 建立基线与非回退门禁。
2. 阅读宽度与预览字号。
3. 代码块一键复制。
4. 文档大纲与标题导航。
5. 统一查找与替换。
6. Windows 文件关联与单实例打开。

先完成两项局部、可独立验证的功能，再进入导航、搜索和启动流程，可避免多个高交互功能同时修改 `App.tsx`、预览组件和主进程。

## 3. 不可削弱的现有能力

以下内容是五项功能共同的硬门槛。任一功能导致其中一项回退，就不能合并或进入发布候选。

### 3.1 文档与编辑边界

- Markdown 原文继续是唯一可保存、恢复和导出的 canonical 内容。
- 源码模式与所见即所得模式继续共用同一份 Markdown 和同一套撤销历史。
- 不得通过序列化预览 DOM 或 `contenteditable` DOM 重建整篇 Markdown。
- 所见即所得的 SourceRange、旧快照拒绝和受保护结构规则保持不变。
- LF/CRLF、GBK/GB18030 转换确认和外部修改冲突保护保持不变。

### 3.2 资源与安全边界

- Renderer 继续保持 sandbox、context isolation 和最小 preload API。
- Renderer 不接收最近文件或外部打开文件的绝对路径。
- 图片、SVG、公式、Mermaid 和本地资源仍走现有授权、解析、派生和审计链路。
- 新功能不得把 `file:`、`blob:`、未授权 `data:` 或本机路径写入预览、剪贴板或输出。
- 文件关联只能接受 `.md` 和 `.markdown`，且仍执行现有大小、编码、文本和指纹校验。

### 3.3 输出与公众号边界

- PDF、DOCX、离线 HTML、公众号 HTML、公众号兼容复制的输入仍来自当前已提交的 `PreviewSession`。
- 阅读宽度、搜索高亮、大纲选中态、代码复制按钮不得进入任何导出结果。
- 公众号 API、AppSecret 安全存储、IP 白名单、草稿回读和发布确认流程不得改变。
- 单账号、单篇、只在明确确认后发布的产品范围保持不变。

### 3.4 恢复、标签与发布边界

- 多标签未保存草稿、标签顺序和活动标签继续进入恢复快照。
- 外部双击文件不得绕过未保存内容保护，也不得静默清空文件夹工作区。
- 安装版与便携版继续执行隐私、生产冒烟、安装/卸载和签名门禁。
- 新增依赖必须说明必要性；能使用现有 UDM、CodeMirror、React、Electron 或浏览器原生能力时，不自建框架。

## 4. 总体技术思路

### 4.1 复用现有数据流

```text
Markdown 原文
  → Parse Worker
  → ParsedDocument / UDM / SourceRange
  → PreviewSession
  ├─ 大纲：读取 heading 节点，不重新解析
  ├─ 搜索：源码使用 CodeMirror；预览/WYSIWYG 使用可见 DOM 文本索引
  ├─ 阅读设置：只改变 Renderer CSS 变量
  └─ 代码复制：只增强屏幕预览 DOM

Windows 外部打开请求
  → Main 进程校验并暂存路径
  → Renderer 收到不透明请求 ID 和显示名
  → 未保存/工作区保护
  → Main 复用 FileSessionManager.openPath
  → Renderer 复用现有标签接入流程
```

### 4.2 最小实现原则

- 大纲直接读取当前 `PreviewSession.parsedDocument`，不创建第二套 Markdown 解析器。
- 搜索不把 `<mark>` 写进预览 HTML，不修改 WYSIWYG DOM；优先使用浏览器 CSS Custom Highlight API。
- 阅读设置只提供少量预置值，不做无限配置面板。
- 代码复制按钮只存在于屏幕预览，不修改 parser 和输出 HTML。
- 文件关联复用 `FileSessionManager.openPath()` 和现有编码确认，不建立第二套文件打开逻辑。

## 5. 能力一：文档大纲与标题导航

### 5.1 用户目标

- 在长文中快速查看标题结构。
- 点击标题后，在当前工作模式中定位到对应内容。
- 文档修改后，大纲与当前解析快照同步，不使用过期 SourceRange。
- 后续可以增加当前标题跟随，但第一阶段先保证点击定位可靠。

### 5.2 第一阶段范围

包含：

- 在现有左侧栏增加“资源管理器 / 大纲”切换。
- 展示 H1–H6、缩进层级和空标题回退名称。
- 源码模式点击标题：移动光标、选中标题并滚动到可见区域。
- 预览模式点击标题：滚动到对应标题。
- 所见即所得模式点击标题：滚动到对应可编辑或受保护块。
- 解析未完成或快照已失效时禁用定位，并显示“正在更新大纲”。

暂不包含：

- 拖动标题改变章节结构。
- 在大纲中直接重命名标题。
- 自动生成目录并写回 Markdown。
- 为跳级标题制造不存在的虚拟标题。

### 5.3 数据结构

建议在 Renderer 内增加最小纯函数，不改变 UDM：

```ts
interface OutlineEntry {
  nodeId: string;
  level: number;
  label: string;
  from: number;
  to: number;
}
```

数据从 `ParsedDocument.children` 递归读取：

- 只收集 `type === "heading"`。
- `level` 读取现有 `attributes.level`。
- `label` 递归拼接标题下的 `text` 子节点；行内代码、链接等继续读取其文本子节点。
- 空标题显示“未命名标题”，不改原文。
- 定位使用 heading 自身的 `source.from` 和 `source.to`。

建议新增：

- `apps/desktop/src/renderer/src/document-outline.ts`
- `apps/desktop/src/renderer/src/document-outline.test.ts`
- `apps/desktop/src/renderer/src/DocumentOutline.tsx`

不建议把大纲状态写入 `packages/shared`，因为当前只有 Renderer 使用它。

### 5.4 详细开发步骤

#### 步骤 A：提取大纲

1. 编写 `extractDocumentOutline(parsedDocument)` 纯函数。
2. 验证 H1–H6、跳级标题、重复标题、内联格式标题、中文和 Emoji。
3. 保证结果按源码位置排序。
4. 对非常多标题采用简单上限提示：超过可渲染预算时明确显示“仅展示前 N 项”，不得静默截断。

#### 步骤 B：把大纲绑定到当前快照

1. `App.tsx` 在 Parse Worker 成功并建立当前 `PreviewSession` 后更新大纲。
2. `applyDraftChange()` 已经会清除预览同步身份；同时把大纲标记为 stale，而不是继续使用旧 SourceRange。
3. 新快照的 `documentId + sourceHash` 与当前活动文档一致时才恢复导航。
4. 切换标签、关闭标签、恢复会话时清理旧大纲选中态。

#### 步骤 C：增加定位能力

1. 给 `MarkdownEditorHandle` 增加 `revealSourceRange(from, to)`：使用 CodeMirror transaction 设置 selection，并使用 `scrollIntoView`。
2. 给 `SynchronizedPreviewHandle` 增加 `revealSourceRange(from, to)`：只在当前 identity 匹配时查找最合适的 `data-source-from/to` 元素并滚动。
3. 给 `WysiwygEditorHandle` 增加同名方法：定位当前渲染层中的块，不触发写回。
4. `App.tsx` 根据 `editorMode` 和 `viewMode` 选择目标，不复制 SourceRange 映射算法。

#### 步骤 D：加入左侧栏

1. 复用现有 activity bar 和 sidebar 容器。
2. 使用真实 `button`、`nav`、`ol` 和 `aria-current`。
3. 标题层级使用缩进表达，不依赖颜色区分。
4. 第一版不做复杂折叠；当真实文档证明需要时再增加折叠状态。

#### 步骤 E：当前标题跟随（第二阶段）

1. 复用预览中的 SourceRange anchors 和编辑器 viewport anchor。
2. 只更新当前大纲项，不反向驱动文档滚动。
3. 使用 `requestAnimationFrame` 合并高频滚动更新。
4. 没有稳定 anchor 时保留上一个有效项，不猜测标题。

### 5.5 验收与测试

- 单元测试：标题提取、文本拼接、排序、空标题和跳级标题。
- Renderer 测试：点击 H2 后源码 selection 落在 H2 SourceRange。
- UI smoke：大纲存在、键盘可访问、点击后预览或编辑器滚动。
- 回归：切换标签后不能使用前一文档的大纲位置。
- 回归：修改标题后旧大纲立即失效，新解析完成后才恢复。
- 回归：大纲不进入 PDF、DOCX、HTML 或公众号内容。

### 5.6 完成定义

- 三种视图都能可靠定位。
- 不新增 parser profile，不修改 Markdown。
- 没有过期 SourceRange 导航。
- 现有同步滚动和选区提示继续通过测试。

## 6. 能力二：统一查找与替换

### 6.1 用户目标

- `Ctrl+F` 在源码、预览和所见即所得中都可查找。
- 显示当前结果序号和总数，支持 Enter / Shift+Enter 或按钮切换。
- `Ctrl+H` 在源码模式提供安全替换。
- 搜索高亮不修改 Markdown，不进入撤销历史和导出结果。

### 6.2 模式边界

| 模式 | 查找 | 替换 | 原因 |
| --- | --- | --- | --- |
| 源代码 | 支持 | 支持单个/全部 | CodeMirror 可以对 canonical Markdown 直接执行事务 |
| 实时预览 | 支持 | 不支持 | 预览不是数据源 |
| 所见即所得 | 支持 | 第一阶段不支持 | 跨内联节点替换需要重新映射 Markdown，贸然实现会破坏格式和撤销边界 |

如果用户在所见即所得模式按 `Ctrl+H`：

1. 先提交当前安全编辑块。
2. 提示“替换需要进入源代码模式”。
3. 用户确认后切换源码并打开替换面板。
4. 提交失败时保持当前模式，不丢失输入。

### 6.3 技术选择

#### 源码模式

使用 CodeMirror 官方 `@codemirror/search`，不手写查找替换状态机。虽然它是一个新增依赖，但它与当前 CodeMirror 6 同源，能正确处理 selection、事务、撤销和超大文档，比自制实现更小、更可靠。

#### 预览和所见即所得

使用浏览器原生能力：

- `TreeWalker` 建立当前内容容器的可见文本索引。
- `Range` 表示跨多个 inline DOM 节点的命中范围。
- CSS Custom Highlight API 显示全部结果和当前结果。
- 不插入 `<mark>`，避免破坏 React、Mermaid、WYSIWYG 和 SourceRange DOM。

如果运行环境不支持 CSS Custom Highlight API，只滚动并选中当前结果，同时显示兼容提示；不要降级为修改内容 DOM。

### 6.4 详细开发步骤

#### 步骤 A：接入源码查找

1. 安装与当前 CodeMirror 版本兼容的 `@codemirror/search`。
2. 在 `MarkdownEditor.tsx` 加入官方 search extension 和 keymap。
3. 给 `MarkdownEditorHandle` 增加打开查找、打开替换和关闭面板的方法。
4. 验证替换操作是一个正常 CodeMirror transaction，可被撤销和重做。
5. 保持现有复制、剪切、智能粘贴和图片 anchor transaction 不变。

#### 步骤 B：构建只读 DOM 搜索索引

1. 增加 `visible-text-search.ts` 纯逻辑与测试。
2. 只遍历预览文章或 WYSIWYG 内容根节点，不遍历工具栏、复制按钮和状态文字。
3. 忽略隐藏元素和 `aria-hidden="true"` 的装饰节点。
4. 保存扁平文本位置到 DOM Text 节点/offset 的映射。
5. 查找采用字面量匹配；第一版不做正则表达式。
6. 默认不区分大小写，中文按原字符匹配。

#### 步骤 C：建立搜索面板

1. 搜索状态属于当前界面会话，不写入恢复快照和本地设置。
2. 切换标签时保留查询文本，但重新计算当前文档结果；当前序号归零。
3. Escape 关闭面板并把焦点还给原编辑区或预览区。
4. Enter 下一项，Shift+Enter 上一项。
5. 空查询不创建高亮。
6. 结果超过显示预算时明确提示，不静默假装已经搜索完整。

#### 步骤 D：处理更新与竞态

1. 文档修改、预览 HTML 更新、Mermaid 渲染完成或切换主题后重建只读索引。
2. 使用约 150ms debounce，避免每个输入字符都遍历大 DOM。
3. 每次重建绑定当前 `documentId + sourceHash`；旧任务结果直接丢弃。
4. 关闭搜索、切换标签和组件卸载时清理 CSS Highlight registry。

#### 步骤 E：快捷键接入

1. 在 `App.tsx` 的全局快捷键处理里优先识别 `Ctrl+F` 和 `Ctrl+H`。
2. 输入框已有焦点时，不拦截浏览器正常文本操作之外的按键。
3. CodeMirror 已处理的事件不重复处理。
4. 快捷键说明进入应用内帮助文档和 UI smoke。

### 6.5 验收与测试

- 源码：查找、上一项、下一项、替换一个、替换全部、撤销和重做。
- 预览：匹配跨越 `<strong>`、`<em>`、链接等 inline 节点的可见文字。
- WYSIWYG：高亮不改变 `innerHTML`，不产生 Markdown 写回。
- 安全：搜索内容不会被当成 HTML 执行。
- 性能：大文档输入搜索词时界面不能长时间失去响应。
- 回归：搜索高亮不进入复制 payload、PDF、DOCX、离线 HTML或公众号输出。
- 回归：Ctrl+Shift+V、Ctrl+B、Ctrl+I、Ctrl+K 和 IME 组合输入保持原行为。

### 6.6 完成定义

- 三种视图 `Ctrl+F` 可用。
- 替换只作用于 canonical Markdown，并可撤销。
- WYSIWYG 搜索不触发 DOM 写回。
- 搜索状态不会污染导出和恢复数据。

## 7. 能力三：可调阅读宽度与预览字号

### 7.1 用户目标

- 根据窗口和阅读习惯选择合适的正文宽度。
- 调整预览字号，不影响 Markdown 内容。
- 重启后恢复设置。
- 默认设置与当前界面一致，升级后不会突然改变用户现有排版体验。

### 7.2 设置范围

第一版只提供有限预置：

```ts
type ReadingWidth = "narrow" | "comfortable" | "wide" | "full";
```

建议映射：

| 预置 | 最大宽度 | 用途 |
| --- | ---: | --- |
| 窄 | 680px | 长文专注阅读 |
| 舒适 | 820px | 默认值，保持现有行为 |
| 宽 | 1040px | 表格、技术文档 |
| 全宽 | none | 临时查看超宽内容 |

预览字号范围：12–24px，步长 1px，默认 14px，与当前 CSS 保持一致。

### 7.3 关键边界

- “阅读宽度”只影响屏幕实时预览和 WYSIWYG 可视区域，不传入输出适配器。
- “预览字号”第一阶段只影响屏幕显示，不改变 PDF、DOCX、HTML 和公众号既有字号。
- 现有字体选择仍按当前规则传递给输出；不要把字体、字号和阅读宽度合并成新的主题系统。
- 小窗口继续受容器可用宽度约束，不能制造横向溢出。

### 7.4 详细开发步骤

#### 步骤 A：规范化设置

1. 在现有 `preview-font.ts` 中增加最小的宽度和字号 normalize 函数，不建立通用设置框架。
2. 无效、越界或旧值回退到 `comfortable / 14px`。
3. 增加纯函数测试，覆盖 `NaN`、负数、过大值和非法字符串。

#### 步骤 B：保存和恢复

1. 复用 `App.tsx` 当前 localStorage 模式。
2. 增加独立键：
   - `fantastic-editor-reading-width`
   - `fantastic-editor-preview-font-size`
3. 设置只保存标量，不保存 CSS 字符串。
4. 读取失败不能阻止应用启动。

#### 步骤 C：CSS 接入

1. 把 `.preview-content > * { max-width: 820px; }` 改为受控 CSS 变量。
2. 把 `.markdown-preview` 当前 14px 改为受控 CSS 变量。
3. `SynchronizedPreview` 和 `WysiwygEditor` 接收相同的屏幕阅读设置。
4. 公式、代码块、表格和 Mermaid 的相对字号规则保持不变。
5. 微信手机预览继续使用自己的主题和宽度审计，不读取这两个设置。

#### 步骤 D：界面控件

1. 在现有预览 pane header 的字体选择器旁增加宽度选择器。
2. 字号使用减小、当前值、增大、重置四个紧凑控件。
3. 所有按钮提供 `aria-label`、禁用边界和键盘焦点样式。
4. 第一版不增加全局设置窗口，不提供任意 CSS 输入。

### 7.5 验收与测试

- 默认启动截图与当前 820px / 14px 基线一致。
- 四个宽度预置在窄窗口下都不产生页面级横向滚动。
- 12px 和 24px 边界正确，继续点击不会越界。
- 重启后设置恢复。
- 切换标签不会改变设置，因为它是应用级阅读偏好。
- PDF、DOCX、离线 HTML和公众号输出的既有快照不发生变化。
- WYSIWYG 工具栏、图片属性、表格工具栏在 24px 正文下仍可用。

### 7.6 完成定义

- 默认视觉无变化。
- 用户设置可恢复且输入经过规范化。
- 屏幕阅读设置不影响输出。
- 不新增主题抽象或设置服务。

## 8. 能力四：代码块一键复制

### 8.1 用户目标

- 在预览中直接复制 fenced code block 或缩进代码块内容。
- 复制结果不包含 Markdown 围栏、语言标识、按钮文字和行号装饰。
- 操作完成后获得明确反馈。

### 8.2 实现边界

- 只增强实时预览 DOM。
- 不在 `document-core` 生成复制按钮。
- 不把按钮写入 `previewHtml`、离线 HTML、PDF、DOCX 或公众号输出。
- Mermaid 成功渲染后使用图形查看逻辑，不显示“复制代码”；Mermaid 失败并回退为代码块时可以复制原始 Mermaid 文本。

### 8.3 详细开发步骤

#### 步骤 A：预览装饰

1. 在 `SynchronizedPreview` 当前 HTML 注入完成后查找 `pre > code`。
2. 给对应 `pre` 增加屏幕专用容器样式和真实 `button`。
3. 按钮文字为“复制”，成功后短暂显示“已复制”。
4. HTML 或主题更新时先移除旧装饰，避免重复按钮。
5. Mermaid 渲染完成后再执行一次装饰，确保成功图形和失败回退状态正确。

#### 步骤 B：复制内容

1. 从目标 `code.textContent` 读取内容。
2. 统一换行为 LF，但不擅自删除代码内部空白。
3. 通过用户手势调用浏览器 Clipboard API。
4. 如果生产态 Electron 不允许 Clipboard API，再增加一个严格的纯文本 IPC；不得直接暴露 Electron `clipboard` 对象。
5. 失败时显示“复制失败，请选中代码复制”，不修改文档。

#### 步骤 C：事件和无障碍

1. 使用文章容器事件委托，避免每个代码块各注册一个长期监听器。
2. 按钮可用 Tab 聚焦，Enter 和 Space 可触发。
3. `aria-label` 包含可区分的代码块序号或语言。
4. 复制状态通过现有 `onStatus` 或 `role="status"` 播报。

### 8.4 验收与测试

- 普通 fenced code、无语言 code、缩进 code 均复制纯代码。
- 中文、Emoji、CRLF 来源、末尾换行和反引号内容保持正确。
- 一个页面多个代码块不会复制错目标。
- HTML 更新和主题切换后按钮不重复。
- Mermaid 成功时不出现代码复制按钮；失败回退时可以复制源码。
- UI smoke 在真实系统剪贴板中读回复制内容。
- 导出 HTML 中不存在复制按钮选择器或按钮文案。

### 8.5 完成定义

- 复制内容与代码节点文本完全一致。
- 不改 parser，不改 UDM，不改输出。
- 生产包真实剪贴板冒烟通过。

## 9. 能力五：Windows 文件关联与单实例打开

### 9.1 用户目标

- 安装版可以注册 `.md` 和 `.markdown` 文件关联。
- 用户双击文件时启动 fantastic-editor 并打开文档。
- 应用已经运行时复用当前实例，在新标签中打开，不再启动第二个窗口进程。
- 外部打开不会丢失当前未保存内容或静默离开文件夹工作区。

### 9.2 平台范围

- 只支持 Windows 安装版 NSIS。
- 便携版不主动写入系统文件关联。
- 不强制把 fantastic-editor 设为默认应用；由 Windows 和用户选择决定。
- 本轮不扩展 macOS 或 Linux。

### 9.3 安全架构

主进程可以接收绝对路径，但 Renderer 继续只接收：

```ts
interface ExternalOpenIntent {
  requestId: string;
  displayName: string;
}
```

主进程使用短期 Map 保存 `requestId → absolutePath`。Renderer 确认可以打开后，只把 `requestId` 传回主进程。这样既能让 Renderer 执行未保存内容交互，又不暴露路径。

请求必须设置短期过期时间，并限制待处理数量，防止第二实例参数造成无限内存增长。

### 9.4 详细开发步骤

#### 步骤 A：安装版文件关联

1. 在根 `package.json` 的 electron-builder 配置中增加 `.md` 和 `.markdown` `fileAssociations`。
2. 复用当前 appId、productName 和正式图标。
3. 确认卸载会移除本应用写入的关联信息。
4. 便携版构建和启动不写注册表。

#### 步骤 B：单实例锁

1. 在 `app.whenReady()` 前调用 `app.requestSingleInstanceLock()`。
2. 获取失败的第二实例立即退出，不创建窗口或注册 IPC。
3. 主实例监听 `second-instance`，解析其中的 Markdown 路径。
4. 主实例恢复或显示窗口，并把请求加入待处理队列。
5. 初次进程的 `process.argv` 使用同一解析函数，避免两套规则。

#### 步骤 C：严格解析启动参数

1. 只接受绝对路径和 `.md/.markdown` 扩展名。
2. 忽略 Electron、安装器和调试参数。
3. 对路径数量和单个字符串长度设上限。
4. 去重使用 Windows 不区分大小写规则。
5. 不在日志、状态栏或 Renderer 错误中输出完整绝对路径。
6. 最终读取仍交给 `FileSessionManager.openPath()`，继续执行 realpath、大小、编码、文本、指纹和重复会话检查。

#### 步骤 D：Renderer 就绪握手

1. 恢复会话和 IPC 监听完成后，Renderer 发送“external-open-ready”。
2. 主进程只有在握手后才发送不透明 intent，避免启动期间丢失双击文件。
3. 多个请求按顺序处理，不并发弹出编码确认对话框。
4. 同一文件已打开时激活现有标签，不生成重复标签。

#### 步骤 E：保护未保存内容和文件夹工作区

现有 `FileSessionManager.openPath()` 在从文件夹工作区切换到单文件模式时可能清理工作区会话。因此外部打开必须先走 Renderer 决策：

1. 当前是普通单文件标签模式：复用现有打开结果接入逻辑，增加或激活标签。
2. 当前存在未提交 WYSIWYG 编辑：先调用 `commitPendingEditor()`；失败则取消外部打开。
3. 当前是文件夹工作区：明确提示“打开系统文件将离开当前文件夹工作区”。
4. 文件夹工作区存在未保存标签：逐项保存、放弃或取消，不能直接调用 `openPath()`。
5. 用户取消时，主进程丢弃对应 requestId，不改变任何文件会话。
6. 用户确认后，再由主进程调用现有 `openWithConversionConfirmation()`。
7. 如果需要编码或混合换行确认，继续使用现有对话框和预期指纹复查。

#### 步骤 F：复用 Renderer 标签接入逻辑

1. 把当前“文件对话框、最近文件、拖入文件、工作区文件”成功结果的公共接入部分收缩成一个现有层级内的函数。
2. 外部打开结果复用该函数，不复制标签、活动文档、状态、恢复快照和预览重置逻辑。
3. 不创建通用文件工厂、事件总线或多实现接口。

#### 步骤 G：发布验证

1. 安装测试机上的 NSIS 包。
2. 验证右键“打开方式”可以看到 fantastic-editor。
3. 应用未运行时双击 `.md`，验证启动和打开。
4. 应用已运行且有未保存标签时双击另一个 `.md`，验证不会丢失草稿。
5. 双击已打开文件，验证只激活现有标签。
6. 卸载后验证应用自己的关联注册被清理。
7. 验证便携版不修改注册表。

### 9.5 验收与测试

- 参数解析纯函数测试：空参数、引号、Unicode、空格、相对路径、错误扩展名、重复路径和超长输入。
- Main 测试：锁失败退出、second-instance 入队、Renderer 就绪后冲刷队列。
- IPC 测试：Renderer 只收到 requestId 和 displayName。
- 集成测试：外部文件仍经过编码和混合换行确认。
- UI smoke：已有未保存文档时外部打开不会丢失内容。
- Installer smoke：安装、文件关联、启动、卸载清理。
- 隐私门禁：日志、截图和 IPC payload 不包含绝对路径。

### 9.6 完成定义

- 安装版可由 Windows 打开 Markdown。
- 已运行时保持单实例。
- 外部打开不绕过未保存和工作区保护。
- Renderer 路径隐私边界不回退。
- 便携版无注册表副作用。

## 10. 跨功能交互规则

### 10.1 文档切换

切换标签时统一执行：

1. 提交或拒绝当前 WYSIWYG 未提交编辑。
2. 清除搜索 DOM 高亮和当前匹配。
3. 清除旧大纲 active 状态。
4. 清除预览瞬态选择框和同步滚动状态。
5. 绑定新文档 PreviewSession 后重建大纲和搜索索引。

### 10.2 文档修改

每次 canonical Markdown 改变：

- 大纲立即标为 stale。
- 预览搜索高亮立即失效。
- 源码 CodeMirror 搜索由编辑器自身更新。
- 阅读设置不变。
- 代码复制按钮等待新预览 HTML 后重新装饰。

### 10.3 导出

导出前无需关闭大纲或搜索，但输出输入只能来自现有 PreviewSession：

- 不读取屏幕 DOM。
- 不读取 CSS Highlights。
- 不读取代码复制按钮。
- 不读取阅读宽度和屏幕字号。

这样可以从结构上保证五项 UI 增强不会污染输出。

## 11. 分阶段交付与回滚策略

| 里程碑 | 内容 | 可独立发布 | 回滚方式 |
| --- | --- | --- | --- |
| M0 | 记录当前测试、类型检查、构建和生产冒烟基线 | 否 | 无代码变化 |
| M1 | 阅读宽度与字号 | 是 | 删除两个 localStorage 读取和 CSS 变量，默认值仍回到当前样式 |
| M2 | 代码块复制 | 是 | 移除预览 DOM 装饰，不影响 parser 或输出 |
| M3 | 大纲与点击导航 | 是 | 隐藏/移除 sidebar 大纲入口，不影响文档模型 |
| M4 | 查找与源码替换 | 是 | 移除全局入口；CodeMirror search extension 可单独撤回 |
| M5 | 文件关联与单实例 | 是 | 移除 builder association 和外部打开握手；普通应用内打开仍可用 |

每个里程碑只合并自身必要文件，不预先为后续里程碑建设通用框架。

## 12. 统一质量门禁

### 12.1 每个里程碑必须执行

```powershell
npm test
npm run typecheck
npm run build
```

实现开始时先记录实际测试文件数和测试项数；完成后测试数可以增加，但原有测试不得减少或被跳过。

### 12.2 涉及 UI 的里程碑

至少增加或更新生产态 UI smoke，验证：

- 键盘操作。
- 焦点恢复。
- 读屏状态播报。
- 深色/浅色主题。
- 窄窗口布局。
- 标签切换后的状态隔离。

### 12.3 涉及安装与主进程的里程碑

```powershell
npm run dist:rc
```

并继续通过：

- 隐私门禁。
- 便携版隔离启动。
- 生产态基础、PDF、DOCX、HTML、公式、Mermaid 和 UI 冒烟。
- 安装、启动和卸载门禁。
- 有签名证书时的 Authenticode 与时间戳门禁。

## 13. 最终验收清单

### 功能验收

- [ ] 大纲由当前 UDM 生成，三种视图可定位。
- [ ] `Ctrl+F` 覆盖源码、预览和 WYSIWYG。
- [ ] 替换只作用于源码且可撤销。
- [ ] 阅读宽度和字号可保存，默认视觉不变。
- [ ] 代码复制只复制纯代码并通过真实剪贴板验证。
- [ ] 安装版文件关联和单实例打开可用。
- [ ] 外部打开不会丢失未保存内容或静默清空工作区。

### 非回退验收

- [ ] canonical Markdown 和 UDM 版本未改变。
- [ ] WYSIWYG SourceRange、受保护结构和撤销边界未回退。
- [ ] 图片导入、资源授权和预览派生未回退。
- [ ] PDF、DOCX、离线 HTML输出未变化。
- [ ] 公众号兼容复制、草稿同步和显式发布确认未变化。
- [ ] 恢复快照、标签顺序、最近文件隐私和外部修改保护未变化。
- [ ] Renderer sandbox 与 preload 最小边界未回退。
- [ ] 原有自动测试、类型检查、构建和发布门禁全部通过。

## 14. 明确延期项

以下内容不属于本五项的第一轮实现：

- 大纲拖动重排、标题重命名和自动目录写回。
- WYSIWYG 直接替换跨格式文字。
- 正则表达式搜索、全工作区搜索和跨文件替换。
- 任意 CSS、任意百分比阅读宽度和完整主题设计器。
- 代码块执行、终端集成和代码沙箱。
- 便携版自动注册文件关联。
- macOS/Linux 文件关联和跨平台发布。

达到以下触发条件后再升级：

- 用户真实文档证明平面大纲不足，再增加折叠和 scroll-spy。
- 有明确跨文件检索需求和性能预算，再建设工作区搜索。
- 真实输出需求要求字号同步，再单独设计输出排版设置，不能复用屏幕阅读值偷渡改变导出。
- Windows 安装版文件关联稳定通过一个发布周期后，再评估其他平台。

---

本方案的核心不是把 Moji 做过的功能全部加入 fantastic-editor，而是复用当前已经可靠的文档模型、编辑事务、安全边界和发布门禁，补齐最有价值的阅读导航与桌面集成能力。
