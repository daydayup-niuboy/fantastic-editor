# fantastic-editor 开发进度

> 更新日期：2026-08-31
> 软件作者：Tbin · 联系邮箱：niuboy5188@gmail.com
> 当前里程碑：`0.2.0-rc.3` 基线已冻结；`0.3.0-rc.1` 已进入发布候选收口，官方 API 自动草稿同步已通过真实账号验收，单账号单篇“一键发布”仅完成代码接入，未宣称真实发布验收通过

> 当前已构建版本：`0.3.0-rc.1` Windows x64 安装版与单文件便携版已生成；`0.2.0-rc.1/rc.2/rc.3` 历史产物继续保留，不覆盖。

> 发布隐私边界：公众号 AppID、AppSecret、封面路径和运行时 IP 信息只允许保存在安装后用户目录；AppSecret 以 Windows 系统加密值保存。构建脚本会清理 `FANTASTIC_EDITOR_WECHAT_*` 环境变量，打包文件明确排除配置、环境变量和证书文件，`npm run verify:privacy` 会扫描安装版/便携版及解包目录，防止本机私有信息进入发布包。

## 当前状态摘要

- 编辑器、资源解析、所见即所得、同步滚动、KaTeX、Mermaid、PDF、DOCX、离线 HTML和公众号输出主功能均已实现。
- 微信公众号默认路线已升级为官方 API 自动草稿，并新增显式确认后的一键发布：应用内安全配置凭据和封面，检测 IP 白名单，批量上传正文图片/公式/Mermaid，创建草稿并回读，再提交发布并轮询结果；方案 B 仅作为兼容兜底。
- 2026-08-31 用户确认真实公众号草稿验收清单 1–5 全部通过：文章进入草稿箱，图片、公式、Mermaid、封面和最终格式正常；一键发布代码已接入，但真实发布验收仍未执行，产品不支持群发。
- 当前验证基线：42 个测试文件、224 项测试、严格类型检查、生产构建、隐私门禁、便携版隔离启动、basic/PDF/DOCX/离线 HTML/公式/Mermaid/UI 七组生产冒烟及安装—启动—卸载门禁均已通过。
- `0.3.0-rc.1` 安装版、便携版和 SHA-256 清单已生成；公益免费项目暂不采购商业代码签名证书，候选包保持 `NotSigned`，签名流水线保留为未来可选能力。范围固定为单公众号、单篇显式发布，不做多账号和群发。

## 2026-08-31：通用剪贴板兼容性规划

- 当前状态：所见即所得复制仍只写 `text/plain`；普通粘贴按纯文本处理并统一换行，公众号方案 B 的富文本复制是专用输出链路，不能当作通用 HTML 粘贴能力。
- P1 目标：复制同时提供 `text/plain`（规范 Markdown）与 `text/html`（安全白名单渲染片段），兼容 Word、Typora、浏览器和公众号编辑器；粘贴按 HTML 优先、Markdown/纯文本降级，并保留一次撤销事务。
- 安全边界：HTML 必须经过标签、属性、协议和资源审计；禁止脚本、事件属性、`file:`/`blob:`/`data:`/`app:`/`fantastic-asset:`、localhost/loopback、绝对路径和未授权图片地址。无法安全转换时不改动原文，并提供纯文本/Markdown 显式降级。
- 验收范围：双格式实际读回、跨 Word/Typora/浏览器/公众号互拷，列表/表格/链接结构保真，图片/公式/Mermaid 明确降级，危险 HTML 不执行，单次粘贴可撤销。该项尚未实现，不改变 `0.3.0-rc.1` 发布候选基线。

## 2026-08-31：0.3.0-rc.1 发布候选收口节点

- 版本范围已从 `0.3.0-dev.1` 推进到 `0.3.0-rc.1`，根包、桌面包、共享包、文档核心包和 lockfile 保持一致。
- 用户已完成真实公众号草稿验收清单 1–5；本记录只覆盖“同步到草稿箱”结果，不把“一键发布”或群发写成已验收能力。
- 发布门禁必须重新运行测试、签名配置测试、严格类型检查、生产构建、Windows 安装/便携产物校验及七组生产冒烟；不得复用 dev.1 的旧哈希作为 RC 结果。
- RC 产物保持未签名策略，发布清单必须标明 `NotSigned`、文件大小和 SHA-256；历史 `0.2.0-rc.*` 与 `0.3.0-dev.1` 产物不覆盖。
- 已生成：安装版 136,615,932 字节，SHA-256 `CCD3212F6E8C45D85A7F3BF66EA056C814530AB117C62B547D0A1ABA2A7FFAB1`；便携版 136,358,042 字节，SHA-256 `1ED7305A921BA9A84E4B1BA99B73B4A1B0C6974D0F61F68C12D415A5242BC05B`；两者签名状态均为 `NotSigned`。
- 便携版隔离启动、七组生产冒烟、产物元数据校验及安装—启动—卸载门禁已通过。安装门禁通过临时无空格盘符运行，显式隔离 TEMP/TMP，并在卸载器临时进程完成后确认主程序已实际移除。

## 2026-08-31：安装版安装/卸载门禁收口

- 修复 NSIS 辅助安装器在带空格工作区及异常 TEMP/TMP 环境下的自动验收：门禁临时映射一个未占用盘符，安装目标仍位于经过边界检查的项目 `build-tmp`，不会触碰正式安装目录或用户数据。
- 自定义卸载初始化在注册表安装位置缺失或过期时，以卸载器目录或 NSIS `_?=` 原始参数作为受控回退；正常注册表路径继续有效。
- `npm run verify:installer` 已重新通过：静默安装、已安装程序启动、应用内 smoke 完成标记、静默卸载和主程序移除全部得到实际验证，报告位于 `build-tmp/installer-smoke-0.3.0-rc.1/installer-smoke-report.json`。
- 重建后的安装版为 136,615,932 字节，SHA-256 `CCD3212F6E8C45D85A7F3BF66EA056C814530AB117C62B547D0A1ABA2A7FFAB1`，Authenticode 状态仍如实记录为 `NotSigned`；便携版为 136,358,042 字节，SHA-256 `1ED7305A921BA9A84E4B1BA99B73B4A1B0C6974D0F61F68C12D415A5242BC05B`。

## 2026-08-31：发布包隐私隔离与退出稳定性

- 发布构建前会移除所有 `FANTASTIC_EDITOR_WECHAT_*` 环境变量；electron-builder 文件清单排除 `wechat-api-config-v1.json`、`.env*` 和证书私钥文件，避免把开发机凭据、封面路径或白名单信息带入安装版/便携版。
- 新增 `npm run verify:privacy`：扫描 `release/win-unpacked`、安装版和便携版，拒绝私有配置文件、当前环境凭据和本机用户目录路径。该门禁已通过。
- 公众号配置仍只在安装后写入当前用户的 Electron `userData`，AppSecret 使用 Windows 系统加密；新用户启动发布包时默认无配置，需在应用内单独填写。
- 冒烟完成标记改为设置退出码后优雅退出，解决 Windows `0x80000003` 在完整 UI 冒烟收尾阶段的异常；重新构建后七组生产冒烟全部通过。

## 当前可用能力

- Windows Electron + React + TypeScript + CodeMirror 桌面应用可实际启动。
- 主界面提供新建（含 Ctrl+N）、可关闭/切换的多文档选项卡，以及窗口级 `.md` / `.markdown` 多文件拖入；各选项卡独立保留未保存草稿。
- 多文档标签支持鼠标拖动排序和 Alt+Shift+左右键移动；顺序随恢复快照保存。欢迎页提供最近文件入口，Renderer 只接收 recentId、文件名和时间，绝对路径只保存在主进程。
- 已实现图片拖放与按钮插入：图片落到 CodeMirror 指定坐标，或插入当前光标/选择区；异步导入锚点随编辑事务映射，多图保持顺序并用空行分隔。
- 图片由主进程校验文件签名、MIME、单图/总量预算和会话身份后写入 Markdown 同级 `assets/`；使用内容哈希防覆盖并复用相同目标，Renderer 不接收源/目标绝对路径。未命名文档会先另存为。
- 导入图片重新进入标准 ParseRequest → ResolveRequest → PreviewSession 管线并同步预览；同一 ImageNode 自动进入 PDF、DOCX、离线 HTML和公众号方案 B 图片替换清单。
- 已实现本地会话恢复：打开标签和未保存草稿以防抖方式写入主进程多代快照；损坏的最新快照可回退上一代。原文件缺失时恢复为未命名文档，原文件在退出期间被外部修改时仍保持保存冲突保护。
- 已完成第一轮产品级 UI 重构：精简顶栏、资源管理器、现代选项卡、欢迎页、统一导出菜单、编辑/分栏/预览三种视图、可拖动分隔线，以及可记忆的浅色/深色主题。
- 已实现可记忆的“同步滚动”开关：P0 由编辑区单向驱动预览区，使用 UTF-16 SourceRange 块级锚点和区间插值，不按整页百分比；快速滚动通过 requestAnimationFrame 合并。
- CodeMirror 非空主选区会在预览区显示独立、不可点击的块级范围提示框；跨块显示多个框，图片精确选区优先提示图片。编辑导致预览身份过期时立即隐藏，图片/公式/分栏尺寸变化后由 ResizeObserver 重新测量。
- data-source 内部标记和 SelectionOverlay 只存在于实时预览 Renderer，不进入 ParsedDocument、PreviewSession IPC、恢复稿、PDF、DOCX、离线 HTML或公众号输出。
- 可打开单个本地 Markdown 文件，也可显式打开文件夹并递归列出 Markdown 文件；单文件模式不会递归扫描父目录。
- 支持编辑、保存、另存为、未保存提示、UTF-8 BOM 与 LF/CRLF 往返、外部修改冲突检测；混合换行必须选择统一为 LF 或 CRLF，GBK/GB18030 候选必须预览并明确确认转为 UTF-8，确认后立即标记为未保存。
- 预览使用唯一 `document-core` 语义，支持 P0 Markdown、KaTeX 公式、本地栅格图片和隔离转换后的 SVG。
- 资源访问具有 workspace/document/grant/revision 身份校验、realpath 边界检查、Junction 防逃逸、内容哈希和短期句柄。
- 源 Markdown data URI 固定阻止并脱敏，不进入 ParsedDocument、资源快照、IPC、缓存或日志。
- PDF：独立隐藏 BrowserWindow，等待字体和图片就绪后调用 `printToPDF`；每个任务独立 Session、窗口、临时目录，支持取消和 45 秒超时。
- Word：Node Utility Process 生成 DOCX，支持标题、段落、行内样式、链接、列表、引用、代码、表格、图片与公式 PNG。
- 离线 HTML：单文件、无脚本、自包含 KaTeX CSS/字体，图片只从已授权且重新校验的字节生成 Data URI；20 MiB 软上限、50 MiB 硬上限。
- 微信公众号：官方 API 自动草稿为默认主流程，已真实验证普通图片、公式 PNG、Mermaid PNG、封面上传、草稿创建与回读；一键发布已接入 `freepublish/submit` + `freepublish/get`，方案 B（正文复制、连续编号占位与图片替换助手）保留为无 API 权限时的兼容兜底。
- P1 公众号正文主题第一阶段已实现：导出菜单可选择并记忆微信原生增强、极简墨白、深蓝科技；主进程白名单编译绝对行内样式，任务结果和人工验收记录携带最终主题 ID。
- 公众号替换图片只保留在主进程；Renderer 只收到 jobId/itemId 和无路径元数据。点击“复制此图片”后由主进程写系统位图剪贴板。
- 所有输出统一使用不可变任务身份、预检、一次任务范围的精确省略批准、取消/超时和五种终态；“部分完成”不会显示为“完整成功”。
- 已完成大文档/大图片第一轮稳定性收尾：打开前按文件大小拒绝、解析前按字符数与资源数拒绝；新解析会真正终止旧 Worker；解析与恢复防抖随文档大小自适应，恢复快照只保留最新待写任务。
- 状态栏显示当前身份一致快照的 Worker 解析耗时、主进程资源解析耗时、字符数和资源数；25 万字符、1,000 个资源或 500 ms 以上任务进入关注级，1,500 ms 总耗时或任一阶段 1,000 ms 以上进入慢任务级。编辑后旧指标立即清除。
- 图片与公式实行共享容量预算：单资源 50 MiB、单篇 10,000 个资源引用、去重后的解析/单次导出资源总量 200 MiB、单次图片化公式 500 项；同内容栅格图在导出收集与进程传输前复用同一字节对象。
- 已配置 electron-builder 的 Windows x64 NSIS 与 portable 两条打包流程：`npm run dist:win`、`npm run dist:portable` 和统一的 `npm run dist:rc`。构建固定使用项目本地 Electron 运行时和项目内临时目录，避免运行时重复下载及系统 TEMP 异常；NSIS 支持自选安装目录并创建开始菜单/桌面快捷方式。
- 发布构建不会携带本机公众号配置：`scripts/build-windows.mjs` 在调用 electron-builder 前移除所有 `FANTASTIC_EDITOR_WECHAT_*` 环境变量；electron-builder 文件清单排除 `wechat-api-config-v1.json`、`.env*` 和证书私钥文件；`verify:privacy` 检查发布目录不存在私有配置文件，且安装版/便携版不含当前环境凭据或本机用户目录路径。
- 已生成与界面品牌一致的绿色 `f` 正式 SVG/PNG/ICO。最新交付是 `0.3.0-rc.1` 安装版与单文件便携版；历史 NSIS/portable 候选继续保留。

- 已实现“源代码 / 所见即所得”双编辑模式：CodeMirror 常驻作为 canonical `editorText` 和历史持有者；标题/普通段落可直接编辑，支持粗体、斜体、删除线、链接与标题级别，复杂块通过精确 SourceRange 源码卡片编辑；模式偏好本机持久化。
- 所见即所得图片拖入和“插入图片”复用原有主进程安全导入链路；保存、导出、关闭标签和模式切换前提交待处理视觉事务，旧快照写回会被拒绝。
- 所见即所得直接编辑已完成稳定性迭代：支持 Enter/Shift+Enter、段落边界 Backspace/Delete 合并、纯文本多行粘贴 LF 规范化和 IME composition 延迟提交；点击空白画布只保留一个 Renderer 临时空段落，重复点击只聚焦，失焦或 Backspace/Delete 清除，输入实际内容后才提交 canonical Markdown。安全文本块之间已支持跨块编辑，经过受保护结构的选择会在提交前拒绝。
- 所见即所得第二轮已支持非嵌套普通/有序/任务列表项、引用段落和表格单元格直接编辑；列表 Enter 新建同类项目，任务框直接切换，表格支持 Tab/Shift+Tab/Enter 导航。嵌套列表和表格结构调整仍保留源码卡片。
- 所见即所得第三轮为标准 Markdown 图片增加结构化属性面板：alt 保真写回、复用安全导入链路替换、确认后删除引用；替换保留原 alt，删除保留 assets 文件，Wiki 图片与过期 SourceRange 不做猜测性改写。
- 所见即所得第四轮支持列表项 Tab / Shift+Tab 调整一级层级及 Ctrl+B/I/K 行内格式快捷键；新增当前解析 ready 门控，Markdown 改变后禁止旧 HTML 重新绑定新 SourceRange。
- 所见即所得第五轮为行内/块级公式、Mermaid 和普通 fenced code block 增加专用结构化面板；公式即时 KaTeX 校验，Mermaid 实时预览，代码语言和内容分离编辑，全部通过保真 SourceRange 事务写回。
- 所见即所得第六轮允许直接编辑混合段落、列表项和表格单元格中公式、图片、链接、行内代码前后的普通文字；四类行内结构作为受保护原子保留原始 Markdown，跨原子的破坏性操作会被拒绝。
- 所见即所得第七轮为标准内联链接和行内代码增加专用面板：链接分字段编辑并保留未触及语法，代码内容与反引号 fence 安全分离，点击链接不触发浏览器导航。
- 所见即所得第八轮增加 GFM 表格结构化工具栏：支持行列插入/删除、列对齐和末格 Tab 自动追加行；表头与唯一列删除受保护，结构事务后旧投影立即失效，空单元格使用合法零长度内容锚点。
- 所见即所得第九轮支持标题、段落、引用和安全叶级列表项间的跨块删除、复制、剪切、直接输入、多段纯文本粘贴及粗体/斜体/删除线；每次批量操作只有一个撤销项，复杂结构在提交边界块前拒绝。

## 当前验证结果

- 自动测试：2026-08-31 本轮实际运行，42 个测试文件、224 项全部通过。
- TypeScript 严格类型检查：2026-08-29 本轮实际运行通过。
- Electron 生产构建：2026-08-29 本轮实际运行通过。
- Windows x64 发布产物：NSIS 安装版和 portable 便携版均构建、PE 头/版本/哈希/签名状态校验通过；便携版完成隔离用户目录启动冒烟；安装版完成静默安装、已安装程序启动、静默卸载及主程序移除验证。
- 打包生产态门禁：基础启动、PDF、DOCX、离线 HTML、公式、Mermaid、完整 UI 共 7 组全部通过；Windows GUI 启动器提前返回问题已用应用内部 JSON 完成标记消除，不再以外层启动器退出码冒充任务完成。
- 发布隐私门禁：`npm run verify:privacy` 已通过，当前发布目录未发现公众号配置文件、环境变量私有值或本机用户目录路径；运行时配置仍由每位用户在应用内单独填写并保存在自己的 userData 中。
- 固定输出样例：PDF 87,657 字节且文件头有效；DOCX 10,142 字节且 ZIP 必需条目与关键正文存在；离线 HTML 372,694 字节、自包含字体且无活动脚本；完整 UI 截图 258,459 字节。
- 生产 Electron UI 冒烟：沙箱 preload、主窗口、品牌区、资源管理器、选项卡、新建文档、图片插入按钮、编辑/预览分栏、无横向溢出、拖拽覆盖层、主题切换和同步滚动开关均通过；可信 Ctrl+A 输入产生 1 个预览范围提示框；浏览器 File 经 contextBridge 到达主进程并执行会话校验。
- P1 可访问性 UI 冒烟：状态栏具有 polite live status；当前文档标签具有 `role=tab` 与选中状态；分栏分隔器可聚焦并用方向键改变 `aria-valuenow`；解析失败可从状态栏或诊断区重新触发当前快照解析；Ctrl+Tab/Ctrl+Shift+Tab/Ctrl+W 可完成文档标签切换与关闭；主题预览支持焦点圈、Escape 关闭和焦点回归。
- 隔离公式窗口冒烟：真实 KaTeX PNG 生成通过。
- DOCX Utility Process 冒烟：真实 DOCX ZIP 数据生成通过。
- PDF 冒烟：真实 Chromium `printToPDF` 生成通过。
- 公众号冒烟：Utility Process 主题 HTML 生成及系统 HTML 剪贴板写入/读回通过。
- npm 安全审计：最近一次成功结果为 0 个已知漏洞；2026-08-29 的联网复核被当前执行环境的数据外发策略阻止，因此未把该次请求记为重新通过。
- 本机 Node 开发基准（2026-08-25，非跨机器承诺）：约 72 万字符文档解析 724 ms、预览 HTML 125 ms；10,000 个图片引用约 659 ms；5,000 个公式节点约 148 ms。另有 36 万字符解析/预览自动压力用例，5 秒硬超时内通过。

## 关键安全与一致性约束

- Renderer 不接收绝对文件路径、源图片字节或主进程资源句柄内部信息。
- Worker 只解析；文件系统解析由主进程完成。
- PDF 不进入 Utility Process；DOCX、离线 HTML 和公众号 HTML 不调用 `printToPDF`。
- SVG 预览派生缓存与导出期 DerivedAssetManifest 分离。
- 公式派生键使用 SourceRange + LaTeX 哈希 + displayMode，不依赖不稳定 nodeId。
- 公式隔离窗口在每个导出任务完成后销毁，不跨 jobId 复用 DOM 或 Session。
- 公众号适配器和主进程剪贴板写入层共用同一标签/属性级安全审计：拒绝实际标签或属性中的 `file:`、`blob:`、`data:`、`app:`、`fantastic-asset:`、localhost/loopback、脚本和事件属性，但不得把正文或代码块中仅用于说明的协议文字误判为活跃资源。

## 已知分发边界与后续抽检

- 当前 `0.3.0-rc.1` 便携版、安装版及历史候选均未使用 Authenticode 代码签名，Windows 可能显示“未知发布者”或 SmartScreen 提示；产物明确记录 `NotSigned`，不会误报为已签名。
- 按当前发布决策，第二台 Windows 不再是 `0.2.0-rc.2` 本机发布加固的阻断门槛。跨机器字体回退、目标 Word 版本、升级覆盖和接近 200 MiB 预算的长时间压力测试保留为后续兼容性抽检。
- 公众号主题的进一步真实平台验证暂缓。微信原生增强已有 `rc.2` 标准样例记录；极简墨白与深蓝科技不得宣称已完成当前版本逐主题平台验收。
- `0.3.0-dev.1` 已通过草稿接口回读并由用户在公众号后台人工确认最终草稿格式正常；这只证明草稿创建链路通过，不代表文章已发布或群发。

## 下一阶段计划

1. 固化公众号配置、IP 白名单、自动草稿、一键发布状态轮询和方案 B 降级的错误文案与最小人工回归清单；保持单账号、单篇、二次确认发布的安全边界。
2. 当前不采购商业代码签名证书；`0.3.0-rc.1` 公益候选保持 `NotSigned`，清单、Release 页面和 README 必须如实标注并提供 SHA-256。未来取得受信任证书或符合条件的免费开源签名服务后，再执行 `npm run dist:signed` 生成可选签名版。
3. 多公众号账号、群发和定时群发暂不开发；逐主题真实平台复验、封面生成和新的扩展语法按后续独立增量评估。
4. [计划] 开发通用 HTML/Markdown 双格式复制与智能粘贴，完成跨应用格式交换、安全审计、资源降级和回归验收后再纳入 P1 版本。

## 2026-08-29：Windows 代码签名流水线

- 新增 `signing:check`、`dist:signed` 和 `verify:signed`。签名发布先预检证书，再构建 NSIS 与 portable，最后用 Windows Authenticode API 强制要求两个产物均为 `Valid`；任一未签名、签名无效、过期或不可用都使发布失败。
- 支持两种互斥证书来源：`WIN_CSC_LINK` + `WIN_CSC_KEY_PASSWORD` 的 PFX，或 `FANTASTIC_EDITOR_WINDOWS_CERTIFICATE_SHA1` 指向 Windows 证书存储。证书、私钥和密码不得写入仓库；`.gitignore` 已阻止常见私钥/证书容器和 `.env` 文件。
- 签名构建强制配置 HTTPS RFC 3161 时间戳；默认使用 DigiCert 时间戳服务，可由 `FANTASTIC_EDITOR_TIMESTAMP_SERVER` 显式替换。签名后必须重新生成哈希与 release manifest。
- 本机证书存储未发现有效代码签名证书，相关环境变量也未配置，所以本轮只完成可验证的签名基础设施，没有伪造签名产物；最新 `0.3.0-dev.1` 便携版仍保持 `NotSigned`。

## 2026-08-31：公益免费项目未签名发布决策

- 项目确认暂不购买商业代码签名证书，原因是当前为公益免费项目，证书费用不值得作为发布前置条件。
- `NotSigned` 不再阻断 `0.3.0-rc.1`：未签名安装版和便携版可以作为公益候选发布，但必须记录签名状态、提供 SHA-256 校验值，并在 README/Release 中说明 Windows 可能出现未知发布者或 SmartScreen 提示。
- 不生成自签名公开包，不把未签名产物描述为已签名；现有 `signing:check`、`dist:signed` 和 `verify:signed` 仅作为未来获得受信任证书或免费开源签名服务后的可选路径保留。
- 产品范围固定为单公众号账号；默认只创建草稿，单篇发布必须显式二次确认。多账号、群发和定时群发不进入当前计划。

## 2026-08-31：单账号单篇一键发布接入

- 新增主进程发布链路：复用当前任务的完整资源上传和草稿创建，随后调用微信 `freepublish/submit`，并用 `freepublish/get` 轮询发布状态。
- Renderer 新增“一键发布到公众号”按钮；执行前必须二次确认，只有官方 `publish_status = 0` 才显示已发布。轮询最多持续 90 秒，处理中、超时和失败均不显示为成功，并保留草稿 ID/发布任务 ID（如已获得）。
- 本功能不扩大到多账号、群发或定时群发；真实账号发布验收尚未执行，当前仅完成代码接入和自动化连接器测试。
## 2026-08-26：Mermaid、字体和同步开关显示

- 加入 Mermaid 11.17.2；`mermaid` fenced code block 在实时预览中以严格安全模式渲染，保留 SourceRange 锚点并设置数量、源码长度和超时上限。
- 新增无网络 Mermaid 隔离 Chromium 渲染器，导出任务生成受控 PNG；离线 HTML/PDF 内嵌 PNG，DOCX 写入图片，公众号方案 B 生成 `diagram` 编号替换项。
- 新增预览字体选择器，支持微软雅黑、Segoe UI、Arial、等线、宋体、楷体和自定义本机字体；设置本机持久化并通过 OutputTheme 传给全部导出适配器。
- 同步滚动按钮改为明确的 `同步滚动 ON/OFF`，继续使用 `aria-pressed` 和原有布尔状态。
- 验证：28 个测试文件、147 项测试通过；类型检查、生产构建、Mermaid PNG 隔离渲染冒烟和真实 Electron UI 冒烟通过。

## 2026-08-26：源代码 / 所见即所得双编辑模式

- 新增所见即所得编辑层，与预览复用主题、字体、KaTeX、本地图片和 Mermaid 渲染；默认隐藏重复预览，切回源代码后恢复原视图。
- 新增最小文本事务与快照校验；可视修改通过 CodeMirror transaction 写回，源代码和所见即所得共用撤销/重做历史。
- 标题和普通段落支持直接编辑及常用行内格式；图片、公式、Mermaid、列表、表格、引用、代码块采用 SourceRange 源码卡片，避免整篇 HTML 反序列化破坏 Markdown 写法。
- 修复 fenced code block 的预览 SourceRange 属性未输出问题，使 Mermaid 渲染后仍能定位并编辑原始围栏。
- 验证：29 个测试文件、151 项测试通过；严格类型检查、生产构建和真实 Electron UI 冒烟通过。UI 冒烟覆盖可视写回、跨模式撤销/重做、Mermaid 源码卡片写回与源代码分栏恢复。

## 2026-08-26：所见即所得编辑稳定性迭代

- 直接编辑从定时整块刷新调整为块级提交，避免输入过程中重绘 DOM 导致光标跳动；同一块在失焦、保存或模式切换时以最小 SourceRange 补丁写回。
- 新增 Enter 段落、Shift+Enter 软换行、段首 Backspace/段尾 Delete 合并相邻直接编辑块，以及点击文档空白处创建新段落。
- 新增中文输入法 composition 保护、纯文本粘贴 CRLF/CR→LF 规范化和跨块破坏性选择保护。
- 新增 Markdown 块插入与合并单元测试；当前验证为 29 个测试文件、153 项测试通过，严格类型检查、生产构建与真实 Electron UI 冒烟通过。UI 冒烟覆盖直接格式化、段落拆分/合并、空白新增、IME 延迟提交、多行粘贴、撤销/重做和复杂块源码卡片。

## 2026-08-26：所见即所得复杂块直接编辑第二轮

- 解析器为每个表格单元格生成精确预览 SourceRange，正确跳过转义管道和代码跨度中的管道，不把表格分隔符或外围空格纳入可编辑范围。
- 新增列表/任务列表、引用和表格单元格保真序列化；列表 Enter 可生成新项目，任务框原子切换，表格支持键盘切格。
- 修复连续可视事务的旧 HTML/旧 SourceRange 竞态：旧闭包不得重绘，当前 DOM 在新解析返回前按同一 TextChange 同步映射。
- 验证：29 个测试文件、157 项测试通过；严格类型检查、生产构建和真实 Electron UI 冒烟通过。冒烟覆盖表格双单元格编辑与 Tab 导航、列表 Enter 新项目、引用写回、任务框切换及后续撤销/重做。

## 2026-08-26：所见即所得图片操作第三轮

- 标准 Markdown 图片点击后打开结构化属性面板，支持 alt 文本写回、替换图片和删除图片引用；不再要求用户直接修改整段图片源码。
- 图片替换继续调用主进程图片校验与 assets 导入服务，以完整图片 SourceRange 原子替换并保留原 alt；取消或锚点过期不会误写。
- 删除操作二次确认，只删除 Markdown 引用并明确保留 assets 原文件；Wiki 图片与不匹配 SourceRange 保持源码卡片或拒绝操作。
- 验证：29 个测试文件、159 项测试通过；严格类型检查、生产构建和真实 Electron UI 冒烟通过。冒烟覆盖图片属性面板打开及 alt 写回，并继续覆盖现有段落、列表、引用、表格、Mermaid、字体和同步滚动功能。

## 2026-08-26：所见即所得列表层级与预览身份门控

- 列表项支持 Tab 缩进一级、Shift+Tab 退格一级；操作以完整当前列表项范围生成单个可撤销 TextChange，并保留 marker、任务框、正文和换行。首个同级项不允许无父项缩进，顶层退格不改写原文。
- 增加 Ctrl+B、Ctrl+I、Ctrl+K 所见即所得快捷键，继续复用已有粗体、斜体和链接结构化事务。
- 修复连续可视事务间的解析身份竞态：canonical Markdown 变化后立即关闭 HTML ready 状态；只有当前 documentId 的最新解析响应可以恢复渲染，旧 HTML 不再有机会绑定新文本偏移。
- 验证：29 个测试文件、160 项测试通过；严格类型检查、生产构建和真实 Electron UI 冒烟通过。冒烟覆盖列表缩进/退格、Ctrl+B 格式化，以及修改后继续编辑源码卡片时 SourceRange 仍准确。

## 2026-08-26：所见即所得复杂内容结构化编辑第五轮

- 预览解析器为行内公式输出精确 SourceRange 包装；行内和块级公式点击后进入 LaTeX 专用面板，在不改变原分隔符和边界空白的前提下即时显示 KaTeX 结果或语法提示。
- Mermaid fenced code block 使用固定语言的源码字段和隔离实时预览；普通 fenced code block 分离语言与内容字段，并保留 fence 字符、meta 和尾随换行。内容与 fence 冲突时自动增长开闭 fence。
- 三种面板继续使用 expectedText 快照校验、完整节点 SourceRange 最小补丁和 CodeMirror 共享撤销历史；渲染 DOM、SVG 与面板草稿不进入 Markdown 或输出。
- 验证：29 个测试文件、162 项测试通过；严格类型检查、生产构建和真实 Electron UI 冒烟通过。冒烟实际修改公式 LaTeX、Mermaid 源码、普通代码内容及语言字段，全部成功写回 canonical Markdown，整体 `valid: true`。

## 2026-08-26：所见即所得混合行内内容编辑第六轮

- 解析器为行内链接和行内代码补充精确 SourceRange，并与既有公式、图片范围统一输出 `data-source-kind`；扫描数量与解析 token 不一致时不绑定可疑范围，整块安全降级。
- 段落、安全叶级列表项和表格单元格可在包含公式、标准图片、链接或行内代码时继续直接编辑周围文字；原子结构的原始 Markdown 切片仅保存在 Renderer WeakMap，序列化时原样回填。
- 公式和图片点击后仍进入专用面板；链接和行内代码内部暂由源代码模式修改。跨原子退格、删除、粘贴和格式化被拒绝，避免浏览器拆散 Markdown 结构。
- 验证：29 个测试文件、162 项测试通过；严格类型检查、生产构建和真实 Electron UI 冒烟通过。冒烟覆盖四类原子共存、图片 alt 面板写回、周围文字编辑、原始链接/代码/公式保真及跨原子删除保护，整体 `valid: true`。

## 2026-08-27：所见即所得链接与行内代码结构化编辑第七轮

- 标准内联链接点击后打开三字段面板，可编辑显示文字、destination 和可选 title；未修改字段继续保留原始尖括号、标题引号、空白和转义写法，参考式链接安全降级为源码卡片。
- 行内代码点击后打开内容面板；原 fence 默认保留，内容包含冲突反引号时自动增长到安全长度，并按 CommonMark 规则处理边界空格。CR/LF 不允许进入行内代码事务。
- 链接和代码仍是直接编辑块中的受保护原子；面板写回后同步更新当前 WeakMap 切片和 SourceRange，后续编辑周围文字不会覆盖已提交修改。点击链接会阻止默认导航。
- 验证：29 个测试文件、164 项测试通过；严格类型检查、生产构建和真实 Electron UI 冒烟通过。冒烟覆盖链接三字段修改、行内代码 fence 自动增长、周围文字继续编辑和共享撤销/重做，整体 `valid: true`。

## 2026-08-27：所见即所得表格结构化编辑第八轮

- 表格单元格选中后显示结构化工具栏，可在当前行上方/下方插入数据行、删除非表头行，在当前列左右插列、删除非唯一列，并设置左/中/右对齐。
- 所有结构操作解析并替换完整 GFM 表格 SourceRange，正确忽略转义管道和代码跨度内的管道；单元格内容、首尾管道风格、缩进、对齐与尾随换行保持，规范化仅限被修改表格的分隔空格。
- 最后一个单元格按 Tab 自动追加空白数据行；新空单元格的零长度内容 SourceRange 可正常选中。结构事务提交后立即清空旧 DOM，等待当前 ParsedDocument 投影，避免旧行列坐标继续接受操作。
- 验证：29 个测试文件、165 项测试通过；严格类型检查、生产构建和真实 Electron UI 冒烟通过。冒烟覆盖单元格编辑、插列、列居中和末格 Tab 追加行，整体 `valid: true`。

## 2026-08-27：所见即所得跨块选区与批量编辑第九轮

- 浏览器原生 Range 可映射到标题、普通段落、引用段落和安全叶级列表项的 MarkdownBlockSelectionFragment；首尾局部偏移与中间整块共同生成一个快照校验 TextChange，不序列化整篇 HTML。
- 支持 Delete/Backspace、直接输入、Enter、text/plain 复制、剪切和多段纯文本粘贴；粘贴统一 LF、按段落分隔并转义行首标题/引用/列表触发符。Ctrl/Cmd+B、Ctrl/Cmd+I 和工具栏删除线按块包装，但整次批量操作只有一个撤销项。
- 选区端点位于已有格式节点内，或范围经过图片、公式、链接、行内代码、代码块、Mermaid、表格、源码卡片及其他复杂结构时拒绝。安全检查先于 commitDirectEdit，拒绝不会顺带规范化首块；直接块序列化持续保留 `\#` 等行首转义。
- 验证：29 个测试文件、166 项测试通过；严格类型检查、生产构建和真实 Electron UI 冒烟通过。冒烟实际覆盖跨块粗体、CRLF 多段纯文本替换、行首 `#` 转义保留及跨受保护原子删除拒绝，整体 `valid: true`。
## 2026-08-27：所见即所得嵌套列表结构化编辑第十轮

- 含子列表的父项新增独立正文编辑投影；用户可直接修改父项文字，提交时只替换首行正文，全部子项源码逐字符保留。深层纯列表同样支持，代码块、表格、块级公式和 Mermaid 后代继续安全降级。
- Tab、Shift+Tab、行首 Backspace、上移和下移均以完整列表项 SourceRange 操作当前项及全部后代；结构工具栏明确显示“结构操作包含全部子项”，一次操作对应一个共享撤销项。
- 父项 Enter 可新建同类型同级项，任务同级项默认未完成，有序项使用当前显式序号加一；空父项 Enter 退出当前层级。既有项移动保留显式序号和 marker，不格式化未触及兄弟项。
- 新增纯事务测试及真实 Electron 嵌套列表场景。验证：29 个测试文件、168 项测试通过；严格类型检查、生产构建通过。真实 UI 冒烟实际完成父项正文写回、子项保留、整棵子树缩进和提升，并连同前九轮场景得到 `valid: true`。
## 2026-08-27：所见即所得块级组织操作第十一轮

- 选中任意顶层结构块后显示统一块工具栏，支持上移、下移、复制、二次确认删除，以及在当前块下方插入段落、标题、列表、引用、代码、公式、Mermaid、表格或图片模板。
- 新增内部块拖放和 Alt+ArrowUp / Alt+ArrowDown 快捷移动；与外部图片文件拖放分流，投放位置提供前/后视觉提示。
- 块事务基于完整 SourceRange 与 expectedText 快照，保留跨越区间的原始空白；引用复制自动补足块边界，一次操作只产生一个共享撤销项。
- 真实 Electron UI 冒烟覆盖模板插入、鼠标拖拽、键盘移动、复制和删除，结果为 valid: true。

## 2026-08-27：所见即所得稳定性收尾与 P0 冻结第十二轮

- 拖拽记录原文快照，拖动期间 canonical Markdown 变化会使操作安全失效；重绘时清理旧块上下文，避免旧 DOM 或旧 SourceRange 写回。
- Escape 可取消尚未提交的直接编辑；插入控件完善焦点行为，块工具栏与落点标记加入键盘可达和输出隔离样式。
- 新增 5,000 块大文档移动、复杂间隔逐字符保真、自身范围拒绝和引用复制边界测试。
- 所见即所得第一至第十二轮至此冻结为 P0 编辑基线。后续主线转向导出适配器和微信公众号真实环境验收，新编辑语法进入 P1 评估。
## 2026-08-27：PDF 导出质量与分页第十三轮

- 新增 PDF 专用 A4 纵向打印契约：冻结页边距、标题避孤页、正文 widows/orphans、表格跨页与表头重复、图片/公式/Mermaid 单页约束。
- 长代码改为打印时自动换行并允许跨页；表格单元格和行内代码支持超长连续内容断行，避免横向滚动区域在 PDF 中被裁切。
- 新增 PdfLayoutAudit。隐藏 Chromium 等待字体和图片后检查表格与 KaTeX 宽度，自动缩放过宽内容；缩放产生明确警告，低于可读下限仍溢出时阻断输出。
- 生产 PDF 冒烟升级为至少两页的中文长文、公式、跨页表格和超长代码样例，真实 Electron/Chromium 生成验证通过。
- 验证：30 个测试文件、176 项测试通过；严格类型检查、生产构建和真实多页 PDF 冒烟全部通过。

## 2026-08-27：Word 与离线 HTML 输出质量第十四轮

- Word 导出固定 A4 页面和明确边距，补齐正文与 Heading 1–6 样式、标题分页控制、居中图片/公式/Mermaid，以及可自动换行的多行代码块。
- 列表改为 Word 原生 numbering，覆盖有序、无序、任务状态和 0–8 级嵌套；表格改为确定性固定列宽、重复表头、行不拆分和单元格对齐，修复引用子内容重复输出。
- 离线 HTML 使用首个标题生成并转义 document title，加入语义 main、浅色/深色主题、响应式表格与长文本断行，同时强化 CSP 和最终安全审计，禁止脚本、事件处理器及本地/临时地址。
- 新增 DOCX OOXML 和离线 HTML 契约测试，并在真实 Electron Utility Process 中完成两类生产冒烟。系统 Microsoft Word 已成功打开 QA DOCX 并导出 PDF；当前环境缺少 LibreOffice，且页面栅格化受工具额度限制，因此未把逐页 PNG 视觉检查标记为完成。
- 验证：30 个测试文件、178 项测试通过；严格类型检查与生产构建通过，DOCX 和离线 HTML Utility Process 冒烟通过。

## 2026-08-27：微信公众号方案 B 可靠性与验收第十五轮

- 修复“复制图片成功即自动算作已粘贴”的状态错误：新增独立的已复制与已完成替换集合，复制成功后必须由用户在公众号实际替换并手动确认。
- 修复 completed-with-omissions 公众号任务不显示剩余替换助手的问题；含省略项时继续展示可用图片/公式/流程图，并始终标记为部分完成。
- 每个替换项新增与正文逐字符一致的 placeholderText，助手直接显示占位文字；正文无图片时也会显示完整验收流程。
- 新增按顺序解锁的正文粘贴、逐项替换、保存草稿、重新打开和移动端预览清单；撤销任何前置状态会级联清除后续确认，文档或任务变化会清空全部状态。
- 公众号 HTML 和替换位图写入剪贴板后增加读回校验；最终 HTML 安全审计阻止可执行标签、事件处理器、data URI、本地/临时协议及 loopback 地址。
- 验证：31 个测试文件、181 项测试通过；严格类型检查、生产构建、真实 Electron 公众号剪贴板冒烟和完整 UI 冒烟全部通过。真实公众号后台保存、重开和移动端预览仍需在目标账号人工验收。

## 2026-08-27：公众号真实验收资产与记录第十六轮

- 新增 fixtures/wechat-acceptance/standard.md 和本地静态 SVG，固定覆盖公众号正文样式、图片、公式、Mermaid、表格、列表、引用、代码及移动端检查项。
- 新增“保存人工验收记录”：只有正文粘贴、全部替换、草稿保存、重新打开和移动端预览均由用户确认后才启用。
- 主进程通过 jobId 读取终态公众号任务，严格核对 replacement itemId 集合；报告只记录哈希、状态和替换元数据，不包含账号、正文、路径、图片或凭据。
- 报告对含省略任务持续标记部分完成，并明确声明不代表文章已经发布或群发；使用安全替换保存 Markdown 文件。
- 验证：33 个测试文件、184 项测试通过；严格类型检查、生产构建和真实 Electron 公众号剪贴板冒烟通过。
- 用户已授权使用 Chrome 中登录的真实公众号，但当前 Chrome 控制连接被 Windows 辅助进程故障中断，两次恢复均失败；本轮没有操作公众号后台，没有创建、保存或发布草稿。真实保存、重开和移动端验收仍为未完成。

## 2026-08-28：公众号排版与替换体验第十七轮

- 用户已在真实公众号后台粘贴标准文章并提供完整截图；普通图片、行内/块级公式和 Mermaid 图片均存在，表格可读，证明方案 B 的资源生成和逐项位图复制主链路可用。
- 截图同时暴露旧虚线占位框残留、正文 H1 与公众号标题重复、行内公式替换容易断段，以及公众号“一键排版”会破坏列表和代码块样式的问题。
- 公众号适配器现在移除正文首个 H1 并返回建议标题；替换项新增 inline/block 布局，正文改用短 FE 文本标记且不再生成虚线空框。
- 验收助手新增标题提示、二次排版风险警告和分布局操作说明；确认条件改为图片已出现且占位文字已消失，重开说明同步纠正。
- Mermaid 隔离渲染采用 18px 基准文字、紧凑间距和最高 2 倍像素 PNG。验证：33 个测试文件、184 项测试通过；严格类型检查、生产构建、真实 Electron Mermaid 高清渲染冒烟和公众号剪贴板读回冒烟均通过。
- 随后生成的 `wechat-acceptance-2026-08-28T01-21-17-131Z.md` 使用短 FE 标记，记录了用户对正文粘贴、四项资源替换、草稿保存、重新打开和移动端预览的完整人工确认。该记录证明清单被用户声明完成，不代表应用读取或自动验证了平台最终草稿，也不代表文章已发布或群发。

## 2026-08-28：空白文档、临时段落与便携版交付修复

- 修复 Windows 运行环境中 `os.tmpdir()` 可能返回未展开环境变量路径、导致新建空白文档 `mkdtemp` 失败的问题。主进程现在显式创建并传入 `app.getPath("userData")/untitled-sessions` 作为未命名会话基目录。
- 新建文档的 canonical `editorText` 现在是真正空字符串，`requiresSave = true`；保存按钮保持可用，首次保存走“另存为”，允许写出合法的 0 字节 Markdown 文件。
- 新建、打开文件和打开文件夹入口等待恢复存储初始化完成，避免应用启动时恢复扫描与新会话创建竞态。
- 修复所见即所得空白画布连续点击生成多个不可删除“输入新段落…”标签的问题：同一时间最多一个临时段落，提示改为“开始输入…”，重复点击只聚焦，失焦和 Backspace/Delete 均可清除；空提示不进入 canonical Markdown、历史、恢复稿或输出。
- UI 冒烟断言已增加 `blankParagraphDeduplicated` 和 `blankParagraphDeleted`，并要求新建标签正文为空、保存按钮可用且显示未保存状态。最近一次专项 UI 冒烟和便携 EXE 启动冒烟结果为通过；本次进度校核没有重新运行会清理测试恢复目录的 UI 模式。
- 新增 `npm run dist:portable`。当前单文件产物为 `release/fantastic-editor-0.1.0-portable.exe`，114,004,094 字节，SHA-256 `EA4E3AE56A60CDE5B5A07306C8CE359482F8FA6500C7611396EAB3DF38FAE860`，未签名。
- 2026-08-28 本轮重新验证：33 个测试文件、187 项测试全部通过；严格 TypeScript 类型检查和 Electron 生产构建通过。当前 `release/` 未发现 NSIS setup.exe 本体，因此不能把安装版列为当前可交付文件。

## 2026-08-28：公众号主题与封面视觉研究准备

- 已把 `guizang-social-card-skill` 安装到本机 `$CODEX_HOME/skills/guizang-social-card-skill`，并确认 SKILL.md、LICENSE、assets 和 references 完整。根据 Codex 安装规则，该 skill 从下一轮任务起可被发现和调用。
- 当前只完成研究工具安装和开发规格整理，没有把上游模板、CSS、脚本、字体、图片或校验器复制进 fantastic-editor，也没有新增运行时依赖、主题 UI、正文主题或封面对输出代码。
- 上游主要生成小红书卡片和公众号 21:9 / 1:1 封面对 PNG，不是公众号可编辑长正文主题。P1 计划独立实现三套原创正文主题和结构化主题编译器；P2 再评估独立 WechatCoverAdapter。
- 上游许可证为 AGPL-3.0，并提供单独商业授权说明。直接内置前必须先完成许可证与分发义务评估；当前默认采用 clean-room 设计借鉴路径。

## 2026-08-28：P0 Windows 发布候选版收口

- 新增与现有绿色 `f` 品牌一致的 `build/icon.svg`，并通过 `npm run icons:generate` 确定性生成 512 px PNG 和 Windows ICO；主程序、NSIS 安装器、卸载器与快捷方式统一使用该图标。
- 新增 `scripts/build-windows.mjs`：固定复用 `node_modules/electron/dist`，并将 TEMP/TMP 仅在构建子进程内指向项目 `build-tmp/`，解决 GitHub 运行时下载超时和 NSIS `!tempfile` 失败。`npm run dist:rc` 现在一次生成安装版、便携版并执行产物核验。
- 新增 `scripts/verify-release.mjs` 和发布清单；核验 EXE 大小、PE MZ/证书表、产品版本、SHA-256 与 SHA-512。当前两个产物均明确为 `NotSigned`，不会误报为已签名。
- `fantastic-editor-0.1.0-setup.exe`：136,570,829 字节，SHA-256 `CADDFE36A4EE57460D37D0E52926BC366A614B5C149BDB80DF2404CE4459CF68`。
- `fantastic-editor-0.1.0-portable.exe`：136,313,046 字节，SHA-256 `D21CBC8DD4D22CC735C3ADB1455721B83AF1E3AEE062F14329B6209F0EC275FE`。
- 本机发布冒烟通过：最终单文件便携版使用隔离 userData 正常启动；NSIS 在项目测试目录完成静默安装，安装后的程序正常启动，卸载器返回 0 且移除测试安装目录。
- 最终回归：33 个测试文件、187 项测试全部通过；严格 TypeScript 类型检查、Electron 生产构建和发布产物核验通过。仍未完成代码签名、升级覆盖和第二台参考 Windows 验收，因此当前状态是“可供测试的 P0 RC”，不是正式公开发行版。

## 2026-08-28：P1 公众号正文多主题第一阶段

- 新增共享 `WechatThemeId` 白名单和三套原创预置：微信原生增强、极简墨白、深蓝科技；没有复制或打包外部 skill 的模板、CSS、脚本、字体、图片或校验器。
- 导出菜单新增公众号主题选择器并使用 localStorage 记忆；只有公众号目标携带 `wechatThemeId`，PDF、DOCX、离线 HTML 和 canonical Markdown 均不受影响。
- 主进程再次验证主题 ID，并通过 `WechatThemeDefinition` 生成绝对行内样式。Renderer 不能提交任意 CSS/HTML；未知 ID 在任务创建前失败。
- `OutputResult.wechatThemeId`、替换验收助手和人工验收报告记录最终生效主题，报告不信任 Renderer 二次声明。
- 新增三主题等价回归：主题 HTML 样式互不相同，但建议标题、资源集合、省略集合、替换项顺序和任务终态一致；安全断言继续拒绝 style/class、脚本、事件属性、data URI 和本地地址。
- 验证：33 个测试文件、190 项测试通过；严格 TypeScript 类型检查和 Electron 生产构建通过。该阶段已由下一节的同编译器主题预览和移动宽度审计继续推进。

## 2026-08-28：P1 公众号正文多主题第二阶段

- 将三套 `WechatThemeDefinition` 和 `applyWechatThemeToFragment` 提到共享包，公众号最终输出与 Renderer 预览不再维护两套主题实现。
- 导出菜单新增“主题与手机预览”，可在 320px、375px、414px 三档宽度即时切换主题；预览按真实投影移除首个 H1，并继续显示已解析的本地图片、公式与 Mermaid。
- 新增移动布局审计：320px、375px、414px 三档探针自动并行测量，分别显示状态；区分阻断性溢出和允许局部滚动的复核项，检查小于 12px 的文字、WCAG 普通/大字号对比度，以及按字号比例计算的标题前后实际间距。
- 三主题增加移动安全样式：固定表格布局、单元格强制断词、代码块安全换行、长链接和正文任意断行；预览中的图片与 SVG 限制为正文宽度。
- 本地审计不改变 `mobilePreviewed`，不生成 OutputResult，也不替代公众号后台保存、重开和真机预览。本节形成时曾计划继续三套主题真实回归；2026-08-29 后续产品决策已暂缓该项，因此它不再是当前本地代码与发布收口的阻断项。
- 验证：35 个测试文件、195 项测试通过；严格 TypeScript 类型检查、Electron 生产构建和完整 Electron UI 冒烟通过。UI 冒烟会真实打开主题预览，等待三档并行审计完成并确认三个宽度状态均存在。

## 2026-08-28：P1 0.2.0-rc.1 便携候选交付

- 根应用、Desktop、document-core、shared 与 lockfile 的项目版本同步提升为 `0.2.0-rc.1`；UDM 和输出 schema 未升级。旧 `0.1.0` 安装版、便携版和清单继续保留。
- 发布前重新运行严格 TypeScript 检查、35 个测试文件/195 项测试、生产构建和完整 Electron UI 冒烟，全部通过；UI 冒烟包含三档主题探针完成断言。
- 已生成 `release/fantastic-editor-0.2.0-rc.1-portable.exe`：114,044,108 字节，SHA-256 `44E514D53F997A58048F2F1A86BFF1190F48A3FACE1B56F5CD06BF36349B065B`，产品版本 `0.2.0-rc.1`，PE 头 `MZ`，签名状态 `NotSigned`。
- 新 EXE 使用独立 `build-tmp/portable-smoke-0.2.0-rc.1` 用户目录启动并自动退出，退出码 0。SHA-512 和机器可读信息见 `release/fantastic-editor-0.2.0-rc.1-portable-manifest.json`。
- 本包是供测试的 `rc.1` 历史候选；当时尚未完成真实公众号三主题逐套验收、代码签名、SmartScreen 和第二台 Windows 验收。第二台 Windows 后续已由 2026-08-29 产品决策移出 `rc.2` 阻断门槛。

## 2026-08-28：公众号双层安全审计误判修复

- 用户在包含 `file:`、`data:`、`app:` 和 localhost 等技术说明文字的真实长文中触发两次误判：先由 WechatClipboardAdapter 最终审计阻止，修复后又被主进程剪贴板写入前的重复旧正则阻止，并错误包装为 `OUTPUT_FILE_WRITE_FAILED`。
- 新增共享 `auditWechatHtmlMarkup`，只检查最终 HTML 标签和属性，不扫描正文文本节点。适配器、主进程剪贴板写入层和公众号生产冒烟统一调用该实现，避免安全规则漂移。
- 真实危险标签、class/id/事件属性，以及标签属性或内联样式中的本地、临时、内嵌资源地址仍会阻止；正文和代码块只是在讨论这些字符串时允许正常复制。
- 公众号剪贴板持久化失败改用 `OUTPUT_CLIPBOARD_WRITE_FAILED`，不再误报为文件写入失败。新增真实验收样例、说明文字放行、危险属性拦截和剪贴板错误码回归。
- 2026-08-28 用户已在重新构建的本机生产版中复测并确认公众号正文写入剪贴板成功。该确认不等于三套主题均已完成真实公众号粘贴、逐项替换、保存、重开和手机预览。
- 本轮最终验证：36 个测试文件、204 项测试全部通过，严格 TypeScript 类型检查和 Electron 生产构建通过。既有 `0.2.0-rc.1` 便携 EXE 早于本修复生成，不能作为包含修复的最新候选包；下一候选必须提升版本号并重新生成清单与哈希。

## 2026-08-28：P1 0.2.0-rc.2 便携候选交付

- 根应用、Desktop、document-core、shared 与 lockfile 的发行版本统一提升为 `0.2.0-rc.2`；UDM、ParsedDocument、ResolutionSnapshot 和 OutputResult schema 均未升级。
- 发布门禁实际重跑：36 个测试文件、204 项测试全部通过，严格 TypeScript 类型检查和 Electron 生产构建通过。
- 已生成 `release/fantastic-editor-0.2.0-rc.2-portable.exe`：114,043,522 字节，SHA-256 `4DE3F25E398E7DD296FB1EB2219DBA8F2A57BD423CCCDEE08CCA848FA6B5622A`，产品版本 `0.2.0-rc.2`，PE 头 `MZ`，签名状态 `NotSigned`。
- 新 EXE 使用独立 `build-tmp/portable-smoke-0.2.0-rc.2` 用户目录启动并自动退出，退出码 0；完整 SHA-512 与机器可读信息见 `release/fantastic-editor-0.2.0-rc.2-portable-manifest.json`。
- 用户对重新构建生产版的公众号正文复制已确认成功；`rc.2` 因此纳入双层安全审计一致性修复。本条记录形成时仍把三套主题、第二台 Windows 和代码签名列为人工门槛；2026-08-29 的后续决策已将第二台 Windows改为非阻断抽检，并暂缓另外两套主题验收。

## 2026-08-28：0.2.0-rc.2 微信原生增强真实验收

- 用户使用真实公众号后台完成标准验收文章的正文粘贴、普通图片、行内公式、块级公式、Mermaid 替换、草稿保存、关闭重开和移动端预览，并在 fantastic-editor 中保存人工验收记录。
- 记录应用版本为 `0.2.0-rc.2`，主题为 `wechat-native-enhanced`，已批准省略项为 0；源文档 SHA-256 与 `fixtures/wechat-acceptance/standard.md` 实际 UTF-8/LF 内容完全一致。
- 记录已归档到 `docs/release/acceptance-records/wechat-acceptance-2026-08-28T13-12-41-546Z.md`，发行矩阵中微信原生增强标记为通过。
- 该人工记录不证明文章已经发布、群发或通过平台审核。当前还需完成极简墨白与深蓝科技的 `rc.2` 逐主题记录。

## 2026-08-28：便携版发布验证自动化

- `scripts/verify-release.mjs` 新增 `--portable-only` 与 `--smoke`，可独立验证当前版本便携 EXE 的最小体积、PE `MZ` 头、产品版本、SHA-256、SHA-512、签名状态和隔离 userData 启动退出码。
- 新增 `npm run verify:portable`；`npm run dist:portable` 现在打包成功后自动调用该门禁并生成 `fantastic-editor-<version>-portable-manifest.json`，不再依赖手工拼装清单。
- Windows 签名 API 可用时读取 Authenticode 状态；受限 PowerShell 环境无法加载签名模块时回退到 PE 证书表检查。`--require-signed` 只接受明确的 `Valid`，不会把回退状态误当成有效签名。
- 机器可读清单不记录本机工作区或 userData 绝对路径，避免把开发机路径带入公开发行元数据。
- 已对现有 `0.2.0-rc.2` 实跑通过：114,043,522 字节，产品版本匹配，签名状态 `NotSigned`，隔离启动退出码 0，哈希与既有候选一致。

## 2026-08-29：0.2.0-rc.2 本机发布加固完成

- `dist:rc` 统一生成 NSIS 安装版与 portable 便携版，并串联发行元数据检查、7 组打包程序生产态冒烟和安装链路冒烟；`release:gate` 在此之前先执行全部测试与严格类型检查。
- 修复 Windows GUI 外层启动器会先于真实 Electron 子进程返回导致的假通过风险。每个生产态场景现在必须由应用写出包含场景、有效性、PID 和完成时间的 JSON 标记；隐藏 PDF/公式/Mermaid 窗口销毁时不会抢先触发自动退出。
- 生产态场景 `basic`、`pdf`、`docx`、`offline-html`、`formula`、`mermaid`、`ui` 全部通过。PDF、DOCX、HTML 与 UI 截图均生成固定实物并完成结构验证，报告见 `build-tmp/production-smokes-0.2.0-rc.2/production-smoke-report.json`。
- 新增 `scripts/verify-installer.mjs` 与 `npm run verify:installer`。安装版已在项目 `build-tmp` 隔离目录完成静默安装、已安装程序启动、静默卸载与可执行文件移除验证，报告见 `build-tmp/installer-smoke-0.2.0-rc.2/installer-smoke-report.json`。
- 最终安装版为 136,594,523 字节，SHA-256 `112DF4E180495FB9161DBC2736089BF8F00D4FE64E05B95D9B890CF8F6B9B71F`；便携版为 136,336,752 字节，SHA-256 `149F306B567E15178780D3208D77B861632BFC1CFCCE9139518264C379769781`。两者版本均为 `0.2.0-rc.2`，签名状态均为 `NotSigned`。
- 用户决定不把第二台 Windows 作为本候选的阻断门槛，并暂缓另外两套公众号主题的真实平台验证。因此本轮结论限定为“本机发布加固完成”；跨机器兼容性、代码签名和未完成主题验收继续如实保留为非阻断边界。

## 2026-08-29：P1 可访问性与错误恢复第一轮

- 源码版本推进到 `0.2.0-rc.3`，避免后续开发覆盖已经冻结并记录哈希的 `0.2.0-rc.2` 安装版和便携版；本轮只更新源码，没有生成 rc3 EXE。
- 状态栏改为 `role=status`、`aria-live=polite`、`aria-atomic=true`，保存、解析、导出、图片导入和结构编辑反馈可以被辅助技术播报。
- 文档标签采用标准 tablist/tab/aria-selected 语义；视图切换按钮组补齐 pressed 状态，读屏不再只能依赖视觉高亮判断当前模式。
- 分栏分隔器可进入键盘焦点，提供 aria 最小值/最大值/当前值；左右方向键按 2% 调整，Shift+方向键按 8% 调整，Home/End 直达 28%/72%，同时保留鼠标拖动。
- 解析 Worker、解析提交、资源组合或解析调用失败时显示“重新解析”按钮；诊断区也提供重新解析和清除提示入口。重试只增加本地解析刷新版本，不修改 Markdown 或文件会话身份。
- 新增纯函数单元测试并扩展真实 Electron UI 冒烟。最终基线为 37 个测试文件、206 项测试，严格类型检查、生产构建和真实 Electron UI 冒烟全部通过。

## 2026-08-29：P1 全键盘工作流与模态焦点第二轮

- 文档标签采用 roving tabindex：只有当前标签进入正常 Tab 顺序，焦点位于标签时可用 Left/Right 与 Home/End 导航；`Ctrl+Tab` / `Ctrl+Shift+Tab` 在任意非模态编辑状态循环切换文档。
- `Ctrl+W` 关闭当前文档，继续复用既有未保存确认和最后标签保留规则，不另建绕过关闭保护的快捷路径。公众号主题预览打开时，这些全局文档快捷键暂停，避免按键穿透到背景页面。
- 公众号主题预览补齐 `aria-labelledby`、`aria-describedby` 和模态焦点圈。打开时焦点进入关闭按钮，Tab/Shift+Tab 循环于弹窗内部，Escape 关闭，关闭后焦点返回触发它的“导出”菜单。
- 新增标签导航纯函数及边界测试，并把真实 Electron UI 冒烟扩展为实际新建第二标签、正反向切换、关闭当前标签和主题弹窗焦点恢复。最终基线为 38 个测试文件、208 项测试；严格类型检查、生产构建和真实 Electron UI 冒烟 `valid: true`。
- 本轮仍属于 `0.2.0-rc.3` 源码开发线，没有生成 rc3 EXE，也没有覆盖已冻结的 `0.2.0-rc.2` 安装版、便携版及其哈希。

## 2026-08-29：P1 大文档观测、标签排序与最近文件第三轮

- Parse Worker 回传自身解析与预览 HTML 生成耗时；Renderer 对主进程资源解析单独计时，只在 ParsedDocument 与 ResolutionSnapshot 身份一致且 PreviewSession 接受后显示指标。正文变化、重试或身份失效时立即清空旧指标。
- 状态栏以“解析 N ms · 资源 N ms”显示，完整字符数、资源数和分级通过 title/aria-label 提供；分级只用于本地交互提示，不写入 Markdown、UDM、恢复快照或导出结果。
- 文档标签支持鼠标拖放重排和 Alt+Shift+左右键移动；普通左右键仍只导航，Ctrl+Tab/Ctrl+Shift+Tab 仍切换。标签数组新顺序直接进入既有恢复快照，不新增第二套顺序来源。
- 新增主进程 RecentFileStore，最多保存 10 项。Renderer 的列表对象没有路径字段，打开时只能提交 recentId；失效记录自动移除，最近记录持久化失败不会阻断核心打开或另存为。
- 最终基线为 40 个测试文件、215 项测试，严格类型检查、生产构建和真实 Electron UI 冒烟全部通过；冒烟实际验证标签键盘重排、性能指标及最近文件 IPC 的不透明边界。

## 2026-08-29：0.2.0-rc.3 最终本机候选收口

- 完整 release gate 首次运行发现打包版所见即所得长链路测试可能在旧 SourceRange 投影尚未更新时继续打开公式、代码或 Mermaid 面板。产品代码增加结构类型与原文切片匹配校验，旧投影只提示等待同步，不再猜测性编辑相邻源码；生产冒烟同时等待 `data-projection-ready=true`。
- 修复后重新从头执行 `release:gate`：40 个测试文件、215 项测试、严格类型检查、生产构建、版本与 PE 检查、便携版隔离启动、7 组生产态冒烟以及安装—启动—卸载链路全部通过。
- 安装版 `release/fantastic-editor-0.2.0-rc.3-setup.exe` 为 136,599,391 字节，SHA-256 `4079551C840B03E70795133D2FF95F4F9E669983953CC05407DB46854054EA65`。
- 便携版 `release/fantastic-editor-0.2.0-rc.3-portable.exe` 为 136,341,705 字节，SHA-256 `3E16B23453AC6DBF6382A077F84BB8A6F265828B38BDFB3B9DD829409950C698`，隔离启动退出码 0。
- 两个产物均为 `NotSigned`。`rc.3` 没有新增公众号真实平台验收记录；代码签名证书、尚未执行的主题逐套验收和可选跨机器抽检继续作为外部边界，不冒充代码未完成。

## 2026-08-29：0.3.0 公众号自动草稿同步开发起点

- 产品决策纠正：逐张“复制此图片”不能继续作为多图文章的主流程。Gemini 建议的 Clipboard Queue 只能减少切换窗口次数，仍然要求用户逐项粘贴，因此降级为无 API 权限时的兼容方案。
- 新主线是“一键同步到公众号草稿箱”：主进程按现有 `jobId` 读取已完成公众号任务的 HTML、普通图片、公式 PNG 和 Mermaid PNG，批量上传正文图片，替换为微信返回的远程持久 URL，上传封面，创建草稿并回读校验。
- 新增 `WechatDraftConnector`，仅接收主进程内部的不可变任务载荷；草稿执行 IPC 只发送 `jobId`，不接触图片字节、HTML、路径、AppID、AppSecret 或 access_token。
- 已增加应用内“公众号 API 配置”对话框，可录入 AppID、AppSecret 并选择默认封面图片。AppSecret 通过专用窄 IPC 交给主进程，由 Electron `safeStorage` 加密保存；Renderer 查询只得到 `hasAppSecret`，不会回显明文。环境变量保留为开发兼容入口，不再是正常使用前提。
- 新增 3 项凭据存储测试并保留 3 项连接器测试，覆盖加密落盘不含明文、留空保持既有密钥、清除配置、批量上传与草稿回读、缺少配置时零网络请求及非安全微信地址阻断。
- UI 的“一键同步到公众号草稿箱”入口现在就地显示配置状态、进行中、成功草稿 ID或失败原因。它只创建草稿，不发布、不群发；含已批准省略项的方案 B 任务禁止自动创建完整草稿。
- 公众号 AppID、加密 AppSecret 与默认封面配置入口已提升到主界面顶部常驻显示，不再要求用户先生成公众号任务才能找到。配置窗口新增微信接口/IP 白名单检测：打开或保存已完整配置时自动检测，遇到 `40164 invalid ip` 会提取微信识别的当前公网 IPv4、提供复制按钮和迁移后开发者平台的白名单设置步骤；白名单放行后显示连接就绪，并通过只读 HTTPS 查询补充显示及复制当前公网 IPv4。
- 白名单检测按钮不再因“尚未保存”直接置灰：表单填写完整后显示“保存并检测”，先安全保存再自动检测；已保存且没有修改时显示“重新检测”。仅在字段不完整、保存中或检测中禁用，避免普通用户误以为功能不可用。
- 2026-08-29 真实公众号验收通过：用户确认自动上传文章已经出现在公众号草稿箱，普通图片、公式和 Mermaid 图片均自动写入，最终格式正常。发布助手中重复的“配置公众号 API”入口已删除，配置只保留在主界面顶部常驻“公众号设置”，避免双入口造成认知负担。
- 本节点发布收口完成：类型检查通过，42 个测试文件、223 项测试通过；便携版隔离启动退出码为 0，basic、PDF、DOCX、离线 HTML、公式、Mermaid 与 UI 七组生产冒烟全部通过。新便携版为 `release/fantastic-editor-0.3.0-dev.1-portable.exe`，大小 114,056,245 字节，SHA-256 `4DB12AD1638361F80D5D8B93089763AA3EE8BA10F1920A3CDB89D39B548BF00B`，当前未签名。
- Windows 原生断点异常 `0x80000003` 已按本机证据归因到 Electron/Chromium GPU 初始化，并在主进程 ready 前固定调用 `app.disableHardwareAcceleration()`；编辑、公式和 Mermaid 继续使用软件渲染路径，优先保证便携版启动稳定。
- 源码包版本已推进到 `0.3.0-dev.1`；连接器模拟测试、凭据边界复核、真实账号仅创建草稿验收和 dev.1 便携发布验证均已完成。已构建的 `0.2.0-rc.3` 产物继续作为冻结历史基线；正式生成 `0.3.0-rc.1` 时必须重新执行完整发布门禁。
