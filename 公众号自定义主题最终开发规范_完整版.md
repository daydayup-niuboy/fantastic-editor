# fantastic-editor 公众号自定义主题最终开发规范

**文档状态：FINAL / 可直接实现**
**目标版本：v0.1**
**实施范围：P0 官方主题 Token 化 + P1 用户自定义主题 + P2 Overlay 导入导出**
**原则：Codex 按本文实现，不重新设计架构，不扩大范围。**

---

# 0. 执行规则

实现过程中，以下规则优先级最高。

## 0.1 十条不可修改合同

### 合同 1：主题不是文档数据

公众号主题属于 Output 配置。

不得写入：

```ts
sourceHash
ParsedDocument
ResolutionSnapshot
DerivedAssetManifest
replacementItems
```

主题只允许存在于：

```ts
OutputContext.theme
BeginOutputRequest.wechatThemeId
OutputResult.wechatThemeId
验收记录
```

主题切换不得改变文档解析结果，不得改变任何 `replacementItems` 的：

```ts
itemId
sequence
placement
```

---

### 合同 2：不重写现有 HTML 编译器

继续沿用现有：

```text
renderParsedDocumentHtml
→ stripUnsupportedPresentationAttributes
→ applyWechatThemeToFragment
→ wrapper <section>
```

禁止：

- 引入 Juice；
- 引入新的 DOM/CSS 编译器；
- 改 semantic DOM；
- 把 `h2` 改成 `section`；
- 把 `ul` 改成 `p + span`；
- 改占位符；
- 改现有 style 合并先后顺序。

必须继续保持：

```text
主题 style 在前
原 HTML style 在后
```

即原 style 仍具有后覆盖能力。

---

### 合同 3：用户只能改 Token，不能改 Recipe

三个官方主题继续拥有各自独立 recipe：

```text
wechat-native-enhanced
minimal-ink
deep-blue-tech
```

用户只允许修改 Token 值。

v0.1 禁止：

```text
自定义 CSS
style 字符串
伪元素
tagStrategy
DOM 模板
组件槽
背景图
字体文件
CSS URL
CSS import
主题 ZIP
主题市场
远程主题订阅
callout
useTableShell
```

---

### 合同 4：Custom themeId 格式唯一确定

官方 ID：

```ts
type OfficialWechatThemeId =
  | "wechat-native-enhanced"
  | "minimal-ink"
  | "deep-blue-tech";
```

历史别名：

```text
wechat-green
```

仅等价于：

```text
wechat-native-enhanced
```

但 `wechat-green`：

- 不得作为 `baseThemeId`；
- 不得写进 Overlay；
- 不得作为 Custom themeId 前缀；
- 保存时必须规范化为 `wechat-native-enhanced`。

Custom themeId 正则必须原样实现：

```ts
export const WECHAT_CUSTOM_THEME_ID_RE =
  /^(wechat-native-enhanced|minimal-ink|deep-blue-tech)\+[0-9a-f]{12}$/;
```

正确：

```text
minimal-ink+0123456789ab
```

错误：

```text
minimal-ink0123456789ab
minimal-ink++0123456789ab
minimal-ink+0123456789a
minimal-ink+0123456789abc
wechat-green+0123456789ab
```

不得动态拼接这个正则。

---

### 合同 5：Main 解析，Shared 永远不访问磁盘

架构固定为：

```text
themeId
   ↓
Main
resolveWechatThemeForOutput()
   ↓
ResolvedWechatTheme
   ↓
Shared
build / apply / compile
```

Main 负责：

```text
workspace themes/ IO
global themes IO
schema 校验
custom themeId 解析
hash 计算
hash 匹配
collision 检查
workspace/global precedence
```

Shared 负责纯函数：

```text
Token normalize
Recipe build
Definition build
HTML theme apply
Publish HTML compile
```

Shared：

**禁止读文件系统。**

Shared：

**禁止根据 Custom themeId 猜 Overlay。**

---

### 合同 6：Hash 基于完整 EffectiveTokens

绝对不能对 Partial patch 直接计算 hash。

固定流程：

```text
Official defaults
      +
OverlayPatch
      ↓
normalize
      ↓
WechatThemeTokens（完整 10 个字段）
      ↓
CanonicalIdentity
      ↓
SHA-256
      ↓
前 12 位
```

同一个最终视觉主题必须得到同一个 themeId。

---

### 合同 7：v0.1 没有 fontFamily Token

字体继续来自当前：

```text
OutputContext / preview font selection
```

Overlay 中出现：

```json
{
  "fontFamily": "..."
}
```

必须 schema reject。

不做：

```text
fontPresetId
主题字体
自定义字体
```

后续另立需求。

---

### 合同 8：官方 themeId 对应完整输出语义冻结

同一个官方 ID 下，下列内容全部属于冻结合同：

```text
Official default tokens
Recipe
mobileSafe
安全 CSS 声明
输出结构语义
```

例如：

```text
minimal-ink
```

以后不得静默：

```text
#333333 → #292929
h2 border 4px → 3px
修改 table shell
修改 wrapper spacing
```

只要变化会改变 publish HTML 的视觉语义：

**必须创建新的官方 themeId。**

注意：

```text
schemaVersion
```

只负责 Overlay 文件格式版本。

它不能用于给官方 recipe 做版本升级。

---

### 合同 9：系统只有一种 Final Publish HTML

唯一编译入口：

```ts
compileWechatPublishHtml()
```

该函数必须产生已经 normalize 完成的：

```text
FINAL publish HTML
```

之后：

```text
audit
clipboard
方案 B
API
宽度检查
真实输出
```

全部消费这个 HTML。

禁止任何下游模块：

```text
再次 apply theme
再次 wrapper
再次 normalize
维护另一套 CSS
```

Preview 可以再次调用同一个纯函数生成预览结果，但不得实现第二套编译逻辑。

---

### 合同 10：v0.1 不支持原地编辑 Overlay

自定义主题文件保存后视为 immutable。

允许：

```text
选择
删除
另存为
P2 导出
```

不允许：

```text
Edit existing overlay in place
```

需要修改自定义主题：

```text
当前自定义主题
→ 另存为自定义
→ 新文件
→ 新 themeId
```

旧文件和旧 themeId 保持有效，除非用户明确删除。

---

# 1. 项目范围

## 1.1 P0

完成：

```text
官方三主题 Token 化
官方 Recipe 函数化
官方 snapshot
Strict resolve
唯一 publish HTML compiler
HTML normalize
安全审计增强
```

P0 完成后：

**外部用户仍然只能看到三个官方主题。**

---

## 1.2 P1

完成：

```text
Overlay schema
EffectiveTokens
Canonical hash
Custom themeId
Workspace/global storage
Main resolver
IPC
主题列表
另存为自定义
375 preview
删除
完整发布
```

---

## 1.3 P2

完成：

```text
Overlay JSON 导出
Overlay JSON 导入
导入校验
README
```

---

## 1.4 明确不做

不要实现：

```text
主题市场
社区主题
第三方 CSS
FET ZIP
CY CSS import
doocs CSS import
花生 CSS import
URL Theme
Remote Theme
主题在线更新
背景图
主题字体
字体文件
组件模板
DOM Template
tagStrategy
callout
Markdown 新语法
自定义 h1/h2/h3 CSS
```

---

# 2. 修改文件范围

优先基于现有文件修改。

## 2.1 Shared

主要：

```text
packages/shared/src/wechat-themes.ts
packages/shared/src/wechat-themes.test.ts
```

允许按现有仓库结构拆成：

```text
wechat-themes.ts
wechat-theme-types.ts
wechat-theme-recipes.ts
wechat-theme-compiler.ts
```

但：

**不要创建第二套平行 Theme Model。**

UDM 已有 `WechatThemeDefinition` 时：

优先扩展已有类型。

不要创建：

```text
FETTheme
CustomRecipeDefinition
RecipesJsonDefinition
```

等重复概念。

---

## 2.2 Desktop Main

主要：

```text
apps/desktop/src/main/wechat-adapter.ts
apps/desktop/src/main/wechat-html-security.ts
```

增加 Main 层 Overlay repository / resolver。

文件名遵循仓库现有命名方式。

推荐逻辑模块：

```text
wechat-theme-repository.ts
wechat-theme-resolver.ts
```

如果仓库习惯集中在已有文件，则可以不拆文件。

---

## 2.3 Renderer

主要：

```text
apps/desktop/src/renderer/src/WechatThemePreview.tsx
```

以及当前主题 Selector 所在组件。

禁止 Renderer 直接：

```text
fs.readFile
读 themes/
读 userData
解析 custom themeId 到文件
```

---

# 3. 官方 ID

定义：

```ts
export const OFFICIAL_WECHAT_THEME_IDS = [
  "wechat-native-enhanced",
  "minimal-ink",
  "deep-blue-tech",
] as const;

export type OfficialWechatThemeId =
  (typeof OFFICIAL_WECHAT_THEME_IDS)[number];

export type LegacyWechatThemeAlias = "wechat-green";
```

规范化：

```ts
export function normalizeOfficialWechatThemeId(
  id: string
): OfficialWechatThemeId | null {
  if (id === "wechat-green") {
    return "wechat-native-enhanced";
  }

  if (OFFICIAL_WECHAT_THEME_IDS.includes(id as OfficialWechatThemeId)) {
    return id as OfficialWechatThemeId;
  }

  return null;
}
```

`wechat-green` 只在读取旧请求时兼容。

任何新的持久化结果必须写：

```text
wechat-native-enhanced
```

---

# 4. Token 数据模型

v0.1 最终 Token **恰好 10 个键**。

```ts
export interface WechatThemeTokens {
  accent: string;
  page: string;
  text: string;
  heading: string;
  muted: string;
  border: string;
  codeBg: string;
  codeText: string;
  sizeBodyPx: number;
  align: "left" | "justify";
}
```

不存在：

```ts
fontFamily
callout
useTableShell
headingFont
customCss
style
```

---

# 5. Token 约束

## 5.1 Color

所有颜色：

```text
#rrggbb
```

必须满足：

```ts
/^#[0-9a-fA-F]{6}$/
```

normalize 后：

```text
全部 lowercase
```

例如：

```text
#12ABEF
→
#12abef
```

特殊规则：

```text
#ffffff
→
#fefefe
```

`page` 最终不得保存为：

```text
#ffffff
```

Normalize 后自然得到：

```text
#fefefe
```

---

## 5.2 sizeBodyPx

运行时合法范围：

```text
integer
12 <= sizeBodyPx <= 22
```

P1 UI 只提供：

```text
15
16
17
```

Schema 仍允许：

```text
12—22
```

用于 P2 导入。

---

## 5.3 align

仅：

```text
left
justify
```

其它值全部拒绝。

---

# 6. 两层 Token 类型

只有输入瞬间允许 Partial：

```ts
export type WechatThemeOverlayPatch =
  Partial<WechatThemeTokens>;
```

允许出现位置：

```text
另存为面板 draft
P2 import parse 后的临时输入
测试
```

禁止进入：

```text
ResolvedWechatTheme
WechatThemeDefinition
publish compiler
磁盘最终 Overlay 文件
```

运行链路全部使用：

```ts
WechatThemeTokens
```

完整 10 个字段。

---

# 7. Token Normalize

实现一个纯函数：

```ts
export function normalizeWechatThemeTokens(
  baseThemeId: OfficialWechatThemeId,
  patch: WechatThemeOverlayPatch
): WechatThemeTokens
```

固定逻辑：

```ts
const merged = {
  ...OFFICIAL_THEME_TOKENS[baseThemeId],
  ...patch,
};
```

然后：

1. 校验未知字段；
2. 校验 8 个颜色；
3. 全部颜色 lowercase；
4. `#ffffff → #fefefe`；
5. 校验 `sizeBodyPx` integer 12–22；
6. 校验 `align`；
7. 返回完整 10-key object。

不得让其它模块自行：

```ts
token.x ?? default.x
```

Default merge 只允许发生在这里。

---

# 8. 官方 Token

定义：

```ts
export const OFFICIAL_THEME_TOKENS:
  Record<OfficialWechatThemeId, Readonly<WechatThemeTokens>>
```

初始值必须从当前三个官方 CSS 中提取。

目标：

**Token 化前后视觉不得变化。**

不要重新设计配色。

不要“顺便优化”。

---

# 9. Recipe

三个独立函数：

```ts
buildWechatNativeEnhancedRecipe(tokens)
buildMinimalInkRecipe(tokens)
buildDeepBlueTechRecipe(tokens)
```

再由：

```ts
buildWechatThemeDefinition(
  baseThemeId: OfficialWechatThemeId,
  tokens: WechatThemeTokens
): WechatThemeDefinition
```

统一 dispatch。

伪代码：

```ts
switch (baseThemeId) {
  case "wechat-native-enhanced":
    return buildWechatNativeEnhancedRecipe(tokens);

  case "minimal-ink":
    return buildMinimalInkRecipe(tokens);

  case "deep-blue-tech":
    return buildDeepBlueTechRecipe(tokens);
}
```

三套 Recipe：

**不得合成一套“大而全 CSS 模板”。**

Token 只控制值。

Recipe 控制主题结构性视觉特征。

---

# 10. mobileSafe

现有 `mobileSafe` 行为全部保留。

必须继续进入：

```text
wrapper
pre
table
```

用户 Overlay 无权覆盖 mobileSafe。

任何安全声明不能成为 Token。

---

# 11. WechatThemeDefinition

沿用 UDM 已有：

```ts
WechatThemeDefinition
```

至少必须可以提供现有编译器需要的：

```ts
id
wrapperStyle
styles
```

并增加必要的：

```ts
baseThemeId
tokens
```

如果 UDM 当前已有相同语义字段：

**使用 UDM 原字段名。**

不得因为本文示例字段名不同而复制第二套类型。

Custom Theme Definition 的：

```ts
id
```

使用完整：

```text
minimal-ink+xxxxxxxxxxxx
```

Official Definition：

```text
minimal-ink
```

---

# 12. Overlay 输入对象与磁盘对象

## 12.1 UI / import 临时输入

```ts
export interface WechatThemeOverlayInput {
  schemaVersion: "0.1";
  name: string;
  baseThemeId: OfficialWechatThemeId;
  tokens: WechatThemeOverlayPatch;
}
```

Input 可以 Partial。

但只存在于：

```text
validation + normalization
```

阶段。

---

## 12.2 磁盘最终格式

所有真正保存到：

```text
themes/*.json
wechat-themes/*.json
```

的文件必须是完整格式：

```ts
export interface WechatThemeOverlayFile {
  schemaVersion: "0.1";
  name: string;
  baseThemeId: OfficialWechatThemeId;
  tokens: WechatThemeTokens;
}
```

示例：

```json
{
  "schemaVersion": "0.1",
  "name": "品牌绿",
  "baseThemeId": "minimal-ink",
  "tokens": {
    "accent": "#07c160",
    "page": "#fefefe",
    "text": "#333333",
    "heading": "#111111",
    "muted": "#777777",
    "border": "#dddddd",
    "codeBg": "#f6f8fa",
    "codeText": "#24292f",
    "sizeBodyPx": 16,
    "align": "justify"
  }
}
```

示例颜色仅表达格式。

真正官方默认值必须从仓库当前 CSS 提取。

---

# 13. JSON Schema

Root：

```text
additionalProperties = false
```

只允许：

```text
schemaVersion
name
baseThemeId
tokens
```

Tokens：

```text
additionalProperties = false
```

只允许 10 个 Token。

磁盘 Overlay：

10 个字段全部 required。

Input：

允许 tokens partial。

---

## 13.1 name

本版补充固定实现：

```text
trim 后长度：1—64 Unicode code points
```

空字符串拒绝。

Name：

```text
只用于 UI
不参与 hash
允许重复
```

---

# 14. Canonical Identity

不要直接对完整 Overlay 文件：

```ts
JSON.stringify(file)
```

然后计算 hash。

必须构造：

```ts
export interface CanonicalWechatThemeIdentity {
  schemaVersion: "0.1";
  baseThemeId: OfficialWechatThemeId;
  tokens: WechatThemeTokens;
}
```

没有：

```text
name
slug
path
source
```

---

# 15. Canonical JSON

为了避免不同排序算法产生不同 ID：

**不要依赖通用 recursive key sorter。**

按固定顺序构造：

```ts
const canonicalIdentity = {
  schemaVersion: "0.1",
  baseThemeId,
  tokens: {
    accent: tokens.accent,
    page: tokens.page,
    text: tokens.text,
    heading: tokens.heading,
    muted: tokens.muted,
    border: tokens.border,
    codeBg: tokens.codeBg,
    codeText: tokens.codeText,
    sizeBodyPx: tokens.sizeBodyPx,
    align: tokens.align,
  },
};
```

然后：

```ts
const canonicalJson = JSON.stringify(canonicalIdentity);
```

UTF-8：

```ts
sha256(canonicalJson)
```

必须输出 lowercase hex。

---

# 16. Custom themeId

函数：

```ts
export function buildWechatCustomThemeId(
  baseThemeId: OfficialWechatThemeId,
  tokens: WechatThemeTokens
): string
```

算法：

```ts
const fullHash = sha256(canonicalJson);
const shortHash = fullHash.slice(0, 12);

return `${baseThemeId}+${shortHash}`;
```

示例：

```text
minimal-ink+a12b34c56d78
```

---

# 17. Hash 性质

以下必须成立：

### name 不同

```text
name A + same tokens
name B + same tokens
```

得到同一个 themeId。

### Token key input 顺序不同

得到同一个 themeId。

### 大小写不同

```text
#ABCDEF
#abcdef
```

normalize 后得到同一个 themeId。

### Partial 与 Full

如果最终 EffectiveTokens 一样：

得到同一个 themeId。

---

# 18. Overlay 存储位置

Workspace：

```text
<文章目录>/themes/<slug>.json
```

Global：

```text
<app userData>/wechat-themes/<slug>.json
```

---

# 19. Slug

Slug：

**不是 themeId。**

v0.1 不实现中文转拼音依赖。

创建新 Overlay 时固定使用：

```ts
custom-${crypto.randomUUID().slice(0, 8)}
```

例如：

```text
custom-a18b25f0.json
```

每次“另存为”产生新的 slug。

不得覆盖已有 Overlay 文件。

若极端随机冲突：

重新生成 UUID。

---

# 20. Immutable Overlay

v0.1 不提供：

```text
修改现有 JSON 后原路径覆盖保存
```

“编辑”行为统一等同：

```text
另存为
```

因此：

```text
old slug → old theme
new slug → new theme
```

旧任务仍可解析旧 themeId。

---

# 21. UI 列表优先级

主题列表包含：

```text
3 个官方主题
Workspace Overlay
Global Overlay
```

若 Workspace 与 Global：

```text
slug 相同
```

UI 只显示 Workspace 项。

标记：

```text
本文
```

注意：

**这是 UI list precedence，不是 resolver lookup 规则。**

---

# 22. Resolver 不按 slug 解析

发布解析：

**只按 themeId 内容身份查找。**

不得：

```text
themeId → slug
```

因为 themeId 根本不包含 slug。

Resolver 流程：

```text
Request themeId
     ↓
Official / alias?
     ↓ no
Custom regex?
     ↓ yes
Scan workspace + global Overlay
     ↓
validate + normalize
     ↓
derive Custom themeId
     ↓
match requested themeId
```

---

# 23. Main Resolver

定义：

```ts
export interface ResolvedWechatTheme {
  id: string;
  baseThemeId: OfficialWechatThemeId;
  tokens: WechatThemeTokens;
  definition: WechatThemeDefinition;
  source: "official" | "workspace" | "global";
  name: string;
}
```

实现：

```ts
resolveWechatThemeForOutput(
  themeId: string,
  workspaceRoot: string | null
): Result<ResolvedWechatTheme, WechatThemeError>
```

如果项目已有统一 Result 类型：

使用现有 Result。

不要新造另一个 Result 实现。

---

# 24. Resolve 顺序

## 24.1 Official

如果：

```text
wechat-native-enhanced
minimal-ink
deep-blue-tech
wechat-green
```

先 normalize official ID。

生成：

```text
OFFICIAL_THEME_TOKENS
→ buildWechatThemeDefinition()
```

成功。

---

## 24.2 Custom ID

先匹配：

```ts
WECHAT_CUSTOM_THEME_ID_RE
```

不匹配：

```text
WECHAT_THEME_UNKNOWN
```

匹配后解析：

```text
baseThemeId
shortHash
```

---

## 24.3 Scan

读取：

```text
workspace themes/
global wechat-themes/
```

每个 JSON：

```text
parse
validate
normalize
canonical
derive id
```

筛选：

```ts
derivedId === requestedThemeId
```

---

# 25. 12 位 Hash Collision

themeId 只保留 12 hex，因此必须防碰撞。

收集所有匹配：

```text
base + hash12
```

的 Overlay。

然后比较：

```text
canonicalJson
```

如果存在两个不同 canonicalJson：

返回：

```text
WECHAT_THEME_HASH_COLLISION
```

禁止任选一个。

如果多个文件：

```text
canonicalJson 完全一样
```

则 Definition 完全一样。

优先 metadata：

```text
workspace
>
global
```

但渲染结果不受影响。

---

# 26. Overlay Missing

如果 Custom themeId 没有任何匹配：

返回：

```text
WECHAT_THEME_OVERLAY_MISSING
```

不得：

```text
fallback 到 base
fallback 到 wechat-native-enhanced
fallback 到当前主题
```

UI 错误：

```text
找不到自定义主题 {themeId}。
请把文章的 themes 目录一起复制过来，
或重新选择一个可用的公众号主题。
```

---

# 27. Error Codes

统一至少实现：

```ts
type WechatThemeErrorCode =
  | "WECHAT_THEME_UNKNOWN"
  | "WECHAT_THEME_OVERLAY_MISSING"
  | "WECHAT_THEME_OVERLAY_SCHEMA_INVALID"
  | "WECHAT_THEME_HASH_COLLISION"
  | "WECHAT_THEME_INVALID_TOKEN"
  | "WECHAT_THEME_UNSUPPORTED_VERSION"
  | "WECHAT_THEME_IN_USE";
```

不要用：

```text
console.error + fallback
```

代替 Result/Error。

---

# 28. Shared apply API

底层正式 API：

```ts
applyWechatThemeToFragment(
  fragment: string,
  definition: WechatThemeDefinition
): string
```

Shared 不接受 Custom themeId。

为了兼容已有官方调用，可以保留：

```ts
applyOfficialWechatThemeToFragment(
  fragment: string,
  themeId: OfficialWechatThemeId | LegacyWechatThemeAlias
): string
```

其内部：

```text
normalize official ID
→ official tokens
→ definition
→ apply definition
```

Custom ID 传入这个兼容入口：

必须 reject。

不要 fallback。

---

# 29. Final Publish Compiler

定义一个唯一纯函数：

```ts
export interface CompileWechatPublishHtmlInput {
  fragment: string;
  definition: WechatThemeDefinition;
  wrapperFontFromContext: string;
}

export function compileWechatPublishHtml(
  input: CompileWechatPublishHtmlInput
): string
```

内部顺序固定：

```text
fragment
↓
stripUnsupportedPresentationAttributes
↓
applyWechatThemeToFragment(fragment, definition)
↓
build wrapper section
↓
normalizeWechatHtmlMarkup
↓
FINAL HTML string
```

如果 `stripUnsupportedPresentationAttributes` 目前不在 Shared：

不要为了形式强行跨包搬函数。

允许 Main 在调用 compiler 前执行它。

但必须保证：

```text
theme apply
wrapper
HTML normalize
```

只有一套正式实现。

---

# 30. Wrapper Font

Theme v0.1 不决定字体。

Wrapper Font：

只来自：

```text
OutputContext 当前已有字体设置
```

继续拼现有系统 fallback stack。

禁止：

```text
definition.tokens.fontFamily
overlay.fontFamily
```

标题不得创建第二套字体栈。

---

# 31. normalizeWechatHtmlMarkup

该函数：

```ts
normalizeWechatHtmlMarkup(html: string): string
```

只允许做两类 deterministic normalization：

```text
6 位 hex → lowercase
#ffffff → #fefefe
```

重要：

**只处理 HTML style 属性中的 CSS 值。**

不得全局替换正文文本。

例如正文代码：

```text
颜色值是 #FFFFFF
```

不得被修改。

允许处理：

```html
<p style="color:#FFFFFF">
```

变成：

```html
<p style="color:#fefefe">
```

不得引入 DOM 重写。

继续使用项目当前安全的 HTML/style 处理方式。

---

# 32. auditWechatHtmlMarkup

Audit：

**只读。**

禁止在 audit 内修改 HTML。

输入必须是：

```text
FINAL publish HTML
```

---

# 33. Audit 直接失败项

继续检查已有规则，并补：

```text
<style>
<script>
事件属性 on*
svg
canvas
video
iframe
```

CSS 禁止：

```text
display:flex
display:grid
gap
position
float
transform
linear-gradient
background-image
var(
calc(
transparent
```

背景颜色禁止 alpha。

包括：

```text
rgba(...)
hsla(...)
8-digit hex alpha
```

固定块宽：

```text
>390px
```

失败。

现有允许的短装饰线例外保持原实现。

不要为本需求重新定义装饰线算法。

---

# 34. Font size Audit

正文：

```text
font-size < 12px
```

失败。

已有：

```text
320
375
414
```

宽度审计继续运行。

已有：

```text
对比度
标题间距
overflow
```

规则继续保持。

---

# 35. Publish 单一数据流

正式输出必须：

```ts
const resolvedTheme =
  resolveWechatThemeForOutput(...);

const publishHtml =
  compileWechatPublishHtml(...);

const audit =
  auditWechatHtmlMarkup(publishHtml);

if (!audit.ok) {
  return failure;
}
```

后续：

```text
Clipboard
方案 B
API
验收
```

必须引用：

```ts
publishHtml
```

这个已经生成的字符串。

不得再次 compile。

---

# 36. Preview 架构

Renderer 不允许读取 Overlay 文件。

主题列表 IPC 只返回 metadata：

```ts
interface WechatThemeListItem {
  id: string;
  name: string;
  baseThemeId: OfficialWechatThemeId;
  source: "official" | "workspace" | "global";
  slug?: string;
}
```

不返回：

```text
absolute path
userData path
workspace filesystem path
```

---

# 37. Custom Preview Definition

Renderer 选择主题后：

```text
Renderer
   ↓ IPC
Main resolveWechatThemeForOutput
   ↓
serialized WechatThemeDefinition
   ↓
Renderer
compileWechatPublishHtml
```

必须新增或复用一个 IPC：

```ts
resolveWechatThemeForPreview(themeId)
```

Main 使用当前文章对应的 workspace。

Renderer：

不得自己解析：

```text
base+hash
```

不得自己扫描文件。

---

# 38. Preview Compiler

`WechatThemePreview.tsx`：

继续使用：

```ts
compileWechatPublishHtml()
```

不得创建：

```css
.theme-preview {}
var(--accent)
```

之类第二套主题 CSS。

375px：

作为自定义面板即时预览默认宽度。

已有 320/375/414 三档继续保留。

---

# 39. P1 UI

入口：

```text
公众号主题选择器
+
另存为自定义
```

---

# 40. “另存为自定义”行为

如果当前是 Official：

```text
当前 Official base
+
Official EffectiveTokens
```

作为 draft。

如果当前是 Custom：

```text
当前 Custom baseThemeId
+
当前 Custom EffectiveTokens
```

作为 draft。

Base：

只显示当前 `baseThemeId`。

**只读。**

用户要换 base：

先切换到另一个官方主题，然后重新“另存为”。

这样不实现“保存后改 base”。

---

# 41. P1 面板控件

只暴露：

```text
名称

accent
text
page
heading

字号：
15 / 16 / 17

对齐：
左对齐 / 两端对齐
```

以下 Token：

```text
muted
border
codeBg
codeText
```

P1 UI 不暴露。

它们从当前 EffectiveTokens 继承。

P2 JSON 可以修改它们。

---

# 42. 保存流程

点击保存：

```text
draft patch
↓
schema/input validation
↓
normalizeWechatThemeTokens()
↓
完整 EffectiveTokens
↓
build custom themeId
↓
生成 random slug
↓
写入 workspace themes/
↓
刷新列表
↓
选中新 themeId
```

默认保存位置：

```text
workspace themes/
```

不存在则创建目录。

不得默认写 Global。

---

# 43. 删除

官方主题：

```text
不能删除
```

Custom：

允许删除。

如果要删除当前选中主题，UI 必须二次确认，并先切换到该自定义主题对应的官方基底，再调用删除；仓库 API 仍不得直接删除传入的当前主题 ID。

同一内容身份可能同时存于 Workspace 与 Global。删除一个列表项时必须删除两个作用域中的全部同 ID 副本，避免隐藏副本在删除后重新出现。

如果文件被用户从磁盘外部删除：

下一次 output：

```text
WECHAT_THEME_OVERLAY_MISSING
```

---

# 44. Theme List

Official 显示：

```text
微信原生增强
极简墨白
深蓝科技
```

Custom 显示：

```text
{name}
{base display name}
{hash 前 6 位}
```

Workspace：

增加：

```text
本文
```

标记。

---

# 45. OutputResult

官方：

```ts
OutputResult.wechatThemeId = "minimal-ink";
```

自定义：

```ts
OutputResult.wechatThemeId =
  "minimal-ink+a12b34c56d78";
```

不得只保存：

```text
baseThemeId
name
slug
```

---

# 46. 完整 HTML 可复现定义

`themeId` 只保证复现：

```text
Theme tokens
+
Theme recipe
+
Theme safety semantics
```

完整 HTML 还受：

```text
源文档
OutputContext
实际 wrapper font
其它现有 Output 参数
```

影响。

如果验收记录要求 HTML 级复现：

至少同时记录：

```text
wechatThemeId
resolved wrapper font
现有其它影响 HTML 的 OutputContext 字段
```

---

# 47. 工作区拷贝行为

### 只复制：

```text
md
assets
```

如果任务引用官方 theme：

正常。

如果引用 custom theme：

失败：

```text
WECHAT_THEME_OVERLAY_MISSING
```

---

### 复制：

```text
md
assets
themes/
```

Custom theme：

可复现。

---

### Overlay 放 Global

所有 workspace：

可发现。

---

# 48. P2 导出

导出：

**直接导出标准化后的 `WechatThemeOverlayFile`。**

不额外包装 ZIP。

不导出 CSS。

不导出 Recipe。

---

# 49. P2 导入

导入步骤：

```text
read JSON
↓
root schema validation
↓
schemaVersion
↓
baseThemeId
↓
token whitelist
↓
partial/full tokens parse
↓
normalize
↓
build full OverlayFile
↓
hash
↓
用户选择 workspace/global
↓
新 random slug
↓
保存
↓
刷新列表
```

导入允许 Partial tokens。

但落盘前：

必须扩成完整 10 Token。

---

# 50. P2 必须拒绝

拒绝：

```text
非 JSON
schemaVersion != 0.1
未知 base
wechat-green as base
unknown root property
unknown token property
style
styles
customCss
fontFamily
CSS string
非法颜色
非法字号
非法 align
```

例如：

```json
{
  "style": "display:flex"
}
```

直接 schema reject。

不是等进 HTML 以后再 audit。

---

# 51. Snapshot P0

第一步必须在改主题代码之前执行。

使用现有：

```text
fixtures/wechat-acceptance/standard.md
```

分别生成：

```text
wechat-native-enhanced
minimal-ink
deep-blue-tech
```

当前 publish HTML。

保存到已有 snapshot 目录。

---

# 52. Snapshot 迁移规则

第一次 Token 化允许人工审核以下唯一差异：

```text
6 位 hex 大小写 normalize
#ffffff → #fefefe
```

除此之外：

任何 diff：

```text
STOP
```

先查原因。

---

# 53. Snapshot 一次性更新

首次 migration 审核通过后：

把 normalize 后的新结果提交成新的正式 snapshot。

从此：

```text
snapshot 必须 byte-for-byte 相同
```

不要长期维护：

```text
“允许颜色 diff”
```

的特殊 matcher。

---

# 54. 不允许顺手修的现有行为

本需求禁止顺手处理：

```text
img 没有独立 style
strong 没有独立 style
li 没有独立 style
h4-h6 复用 h3
原 style 后覆盖 Theme
Regex compiler 行为
其它已有 HTML quirks
```

全部由 snapshot 锁住。

要改：

另开 PR / issue。

---

# 55. Placeholder 不变量

任何主题：

不得改变：

```text
【FE类型NN｜整段替换】
```

包括：

```text
文本
数量
顺序
placement
```

方案 B：

```text
正文复制
替换图
完成
```

三态逻辑零修改。

---

# 56. 单元测试：ID

必须：

```ts
expect(valid("minimal-ink+0123456789ab")).toBe(true);

expect(valid("minimal-ink0123456789ab")).toBe(false);
expect(valid("minimal-ink++0123456789ab")).toBe(false);
expect(valid("minimal-ink+0123456789a")).toBe(false);
expect(valid("minimal-ink+0123456789abc")).toBe(false);
expect(valid("wechat-green+0123456789ab")).toBe(false);
```

---

# 57. 单元测试：Alias

```text
wechat-green
→
wechat-native-enhanced
```

新输出必须记录：

```text
wechat-native-enhanced
```

而不是 alias。

---

# 58. 单元测试：Canonical

必须：

```text
不同 name + same tokens
→ same id

token input key order 不同
→ same id

#ABCDEF / #abcdef
→ same id

Partial / equivalent Full
→ same id
```

---

# 59. 单元测试：Schema

拒绝：

```text
fontFamily
style
styles
unknownToken
unknown root key
version 0.2
invalid base
wechat-green base
#fff
rgb(...)
transparent
sizeBodyPx 11
sizeBodyPx 23
sizeBodyPx 16.5
align center
```

---

# 60. 单元测试：Overlay Resolve

覆盖：

```text
workspace only
global only
workspace + global same content
workspace + global same slug different content
missing
invalid custom ID
hash12 collision
```

重点：

```text
同 slug workspace/global
```

只影响 UI list。

Resolve 仍按 ID 找内容。

---

# 61. 单元测试：Collision

构造两个测试 identity：

```text
相同 base
相同 hash12
不同 canonicalJson
```

如果测试环境不方便真实暴力制造 SHA 前缀碰撞：

允许注入测试 hash function。

生产环境仍必须使用 SHA-256。

测试必须证明：

```text
collision
→ WECHAT_THEME_HASH_COLLISION
```

---

# 62. 单元测试：Official Snapshot

三官方主题全部 snapshot。

Token 化以后：

Migration 完成后 diff 必须为 0。

---

# 63. 单元测试：Accent

创建 Overlay：

只修改：

```text
accent
```

验证：

```text
标题 accent 改变
引用 accent 改变
其它 token 使用 base default
placeholder 不变
DOM semantic structure 不变
```

---

# 64. 单元测试：replacementItems

切换：

```text
official A
official B
custom
```

确保：

```text
itemId
sequence
placement
```

完全一致。

---

# 65. 单元测试：Delete

直接通过仓库 API 删除当前 Overlay：

```text
WECHAT_THEME_IN_USE
```

经 UI 二次确认删除当前 Overlay：

```text
切回对应官方基底
→ 删除 Workspace / Global 中全部同 ID 副本
→ list 与 resolve 均不再返回该自定义主题
```

外部手动删除当前 Overlay 文件后：

```text
resolve
→ WECHAT_THEME_OVERLAY_MISSING
```

绝不能 fallback。

---

# 66. 单元测试：Final HTML

确保正式 Output：

只调用一次：

```text
compileWechatPublishHtml
```

Audit 接收的 string：

必须与 Clipboard/API/方案 B 使用的 string 相同。

可以通过 mock / spy 验证。

---

# 67. HTML Normalize 测试

输入：

```html
<p style="color:#ABCDEF;background:#FFFFFF">
  #FFFFFF
</p>
```

输出：

```html
<p style="color:#abcdef;background:#fefefe">
  #FFFFFF
</p>
```

正文文本不得修改。

---

# 68. Audit 测试

分别验证拒绝：

```text
display:flex
display:grid
gap
position
float
transform
linear-gradient
background-image
var(
calc(
transparent
rgba alpha
8-digit hex alpha
width >390
font-size <12
```

官方三个主题必须全通过。

---

# 69. Renderer 测试

Custom Theme Preview：

必须通过：

```text
Renderer
→ Main resolve
→ Definition
→ shared compiler
```

测试 Renderer：

没有直接：

```text
fs
path to themes
userData path
```

依赖。

---

# 70. UI 验收

用户应能在 2 分钟内：

```text
选择 极简墨白
↓
另存为自定义
↓
名称 = 品牌绿
↓
改 accent
↓
375 preview 立即变化
↓
保存
↓
新 Custom Theme 自动选中
↓
公众号输出成功
```

---

# 71. 安全验收

Custom JSON 无论如何不能让用户注入：

```text
CSS declaration
CSS function
HTML
JS
event handler
```

输入能力严格限制为：

```text
8 个 #rrggbb
1 个 integer
1 个 enum
```

---

# 72. 开发步骤 A：建立基准

必须第一步完成。

1. 不改 Theme 代码。
2. `standard.md × 三主题` 生成 publish HTML。
3. 存 snapshot。
4. 保存当前 audit 结果。
5. 当前测试全部 green。
6. 提交独立 PR/commit。

Commit 建议：

```text
test(wechat): lock official theme publish snapshots
```

---

# 73. 开发步骤 B：建立类型和常量

在 Shared：

1. `OfficialWechatThemeId`
2. alias
3. custom ID regex
4. `WechatThemeTokens`
5. `WechatThemeOverlayPatch`
6. `OFFICIAL_THEME_TOKENS`
7. normalize function
8. token validation

此阶段先写单测。

不要先改 UI。

---

# 74. 开发步骤 C：Token 化三个 Official Recipe

逐个迁移：

```text
wechat-native-enhanced
minimal-ink
deep-blue-tech
```

对每个主题：

1. 从当前 CSS 抽取真实默认值；
2. 写进 `OFFICIAL_THEME_TOKENS`；
3. 把 CSS hard-coded value 替换成 token；
4. 保留 Recipe 结构；
5. 保留 mobileSafe；
6. 跑 snapshot。

若出现除允许 migration 之外的 diff：

停止修改。

---

# 75. 开发步骤 D：统一 Definition API

实现：

```ts
buildWechatThemeDefinition()
```

然后改：

```ts
applyWechatThemeToFragment()
```

正式内部使用：

```text
Definition
```

保留 Official compatibility wrapper。

旧 unknown fallback：

删除。

---

# 76. 开发步骤 E：Strict Official Resolve

现有：

```text
unknown → wechat-native-enhanced
```

改掉。

Official resolver：

```text
known
→ success

wechat-green
→ canonical official

unknown
→ WECHAT_THEME_UNKNOWN
```

修改旧测试。

---

# 77. 开发步骤 F：Final Publish Compiler

建立：

```ts
compileWechatPublishHtml()
```

把现有 Theme apply + Wrapper 逻辑集中。

然后加入：

```ts
normalizeWechatHtmlMarkup()
```

此时重新检查 snapshot。

完成 migration 后：

提交新的正式 normalized snapshot。

之后要求 diff = 0。

---

# 78. 开发步骤 G：Audit 只读化

检查现有：

```ts
auditWechatHtmlMarkup()
```

如果它当前包含 rewrite：

移动 rewrite 到：

```ts
normalizeWechatHtmlMarkup()
```

Audit 只：

```text
inspect
report
```

补所有拒绝项测试。

三个 Official 必须通过。

---

# 79. 开发步骤 H：Main Overlay Schema

实现：

```text
input parser
disk file parser
schemaVersion validation
additionalProperties false
token validation
```

先写：

```text
valid
invalid
unknown property
fontFamily
style
```

测试。

---

# 80. 开发步骤 I：Canonical + Hash

实现：

```text
canonical identity
fixed field order
JSON.stringify
SHA-256 UTF-8
lowercase hex
slice(0,12)
custom themeId
```

把：

```text
name
slug
path
```

排除。

写完整 hash 单测。

---

# 81. 开发步骤 J：Repository

Main 增加 repository：

```text
list workspace overlay files
list global overlay files
read
parse
save new
delete
```

保存：

```text
atomic write
```

推荐：

```text
write temp
rename
```

避免写一半断电。

若项目已有 atomic file helper：

使用现有 helper。

不要重复实现。

---

# 82. 开发步骤 K：Resolver

实现：

```ts
resolveWechatThemeForOutput()
```

顺序严格按本文第 24—26 节。

重点完成：

```text
Official
Alias
Custom
Missing
Collision
Workspace/global
```

不要写 fallback。

---

# 83. 开发步骤 L：Theme List

Main：

构建 Theme list metadata。

Official 固定在前。

Custom：

Workspace + Global。

同 slug：

Workspace UI 覆盖 Global。

Renderer 不拿绝对路径。

---

# 84. 开发步骤 M：IPC

增加/复用：

```text
listWechatThemes
resolveWechatThemeForPreview
saveWechatThemeAsCustom
deleteWechatTheme
```

所有文件操作留在 Main。

Renderer 不读文件。

---

# 85. 开发步骤 N：Renderer Selector

更新主题 selector：

显示：

```text
Official
Custom
```

Custom metadata：

```text
name
base
hash6
本文
```

未知 / Missing：

展示明确错误。

---

# 86. 开发步骤 O：另存为面板

实现：

```text
名称
4 色
字号
对齐
375 preview
保存
取消
```

Preview：

Custom draft 不需要先写磁盘。

流程：

```text
当前 resolved tokens
+
draft patch
↓
normalize
↓
build definition
↓
compileWechatPublishHtml
↓
preview
```

注意：

这仍是 shared pure pipeline。

不读 CSS。

---

# 87. 开发步骤 P：保存 Overlay

保存时：

```text
normalize
→ full tokens
→ hash
→ id
→ random slug
→ full OverlayFile
→ workspace write
→ refresh
→ select
```

再次从磁盘读取并 resolve 一次。

保存成功的最终判定：

```text
resolve(savedId)
```

必须成功。

---

# 88. 开发步骤 Q：Adapter 正式输出

修改：

```text
apps/desktop/src/main/wechat-adapter.ts
```

改成：

```text
themeId
→ Main resolve
→ Definition
→ render fragment
→ compile final HTML
→ audit final HTML
→ output
```

`OutputResult.wechatThemeId`：

使用 canonical resolved ID。

因此旧：

```text
wechat-green
```

输出结果写：

```text
wechat-native-enhanced
```

---

# 89. 开发步骤 R：方案 B / API

查找所有：

```text
公众号最终 HTML
```

消费者。

逐个改成：

消费 adapter/compiler 已经生成的：

```text
final publishHtml
```

禁止各自：

```text
resolve theme
apply theme
normalize HTML
```

重点回归：

```text
placeholder
图片替换顺序
API URL replacement
```

---

# 90. 开发步骤 S：删除

实现：

```text
delete custom overlay
```

当前 in-use：

拒绝。

非当前：

删除文件 + refresh。

删文件后不生成 tombstone。

---

# 91. 开发步骤 T：P2 Export

导出：

当前标准 OverlayFile。

不转换 CSS。

不丢 Token。

---

# 92. 开发步骤 U：P2 Import

实现完整校验链。

导入后：

```text
normalize
→ full file
→ new random slug
→ selected destination
→ save
→ resolve verify
→ refresh
```

不得相信导入文件自报的 hash。

Hash 永远本地重新计算。

---

# 93. PR 切分

## PR 1

```text
Official publish snapshots
```

要求：

```text
无行为变化
```

---

## PR 2

```text
Token types
Official tokens
Official recipes
Definition API
Strict official resolve
Final publish compiler
Normalization
Audit
```

外部仍只有官方三主题。

可单独发布。

---

## PR 3

```text
Overlay schema
Canonical hash
Repository
Main resolver
IPC
Theme list
Save As Custom
Preview
Delete
Formal output integration
```

这是 P1 用户可见版本。

---

## PR 4

```text
P2 Import
P2 Export
README
```

不要把四个 PR 揉成一次提交。

---

# 94. 每个 PR 的完成门槛

必须：

```text
existing tests pass
new tests pass
TypeScript no new errors
lint pass
build pass
official snapshots pass
```

如果仓库已有：

```text
pnpm test
pnpm lint
pnpm typecheck
```

等标准命令：

使用仓库现有命令。

Codex 先查看 package scripts，不自创测试系统。

---

# 95. 完成定义

以下全部满足才算完成。

- [ ] 三 Official Theme 全部 Token 化。
- [ ] 三主题 Recipe 保持独立。
- [ ] Official snapshot 锁定。
- [ ] Migration 后 snapshot byte-for-byte stable。
- [ ] Unknown ID 不再 fallback。
- [ ] `wechat-green` 只作为读取 alias。
- [ ] Custom ID 正则完全符合规范。
- [ ] v0.1 恰好 10 个 Token。
- [ ] `fontFamily` 不存在于 Overlay。
- [ ] Partial 只存在输入阶段。
- [ ] 磁盘文件始终 Full EffectiveTokens。
- [ ] Hash 基于 schema + base + Full EffectiveTokens。
- [ ] Name/slug/path 不参与 Hash。
- [ ] Main 独占 Overlay IO。
- [ ] Shared 不碰文件系统。
- [ ] Renderer 不读 Overlay 文件。
- [ ] Renderer Custom Preview 通过 Main resolve。
- [ ] Hash12 collision 会失败。
- [ ] Missing Overlay 会失败。
- [ ] Custom Theme 永不 fallback。
- [ ] Overlay 不支持原地编辑。
- [ ] Delete current theme 会拒绝。
- [ ] Final Publish HTML 只有一套 compiler。
- [ ] Audit 只读取 Final HTML。
- [ ] Clipboard 使用同一个 Final HTML。
- [ ] 方案 B 使用同一个 Final HTML。
- [ ] API 使用同一个 Final HTML。
- [ ] 320/375/414 Audit 使用同一编译结果。
- [ ] Placeholder 文本/顺序/placement 零变化。
- [ ] replacementItems 零变化。
- [ ] 用户可在 2 分钟内完成“另存为自定义”。
- [ ] 拷贝 `md + assets + themes/` 后 Custom Theme 可复现。
- [ ] 缺 `themes/` 时给出明确 Missing 错误。
- [ ] P2 import 拒绝未知字段和 CSS。
- [ ] P2 export 是标准 Overlay JSON。
- [ ] 不包含主题市场、CSS 编辑器等范围外功能。

---

# 96. Codex 开工指令

执行本任务时严格遵守：

1. 先阅读本文全部内容。
2. 再检查仓库现有实现和 UDM。
3. 字段已有等价名称时使用现有名称，不复制类型。
4. 不改变本文已拍板的架构。
5. 不增加范围。
6. 严格按 A → U 顺序实施。
7. 每完成一个阶段立即运行相关测试。
8. Snapshot 出现未批准差异立即停止该阶段并定位原因。
9. 不使用 silent fallback 修测试。
10. 不为了让测试通过改变本文合同。
11. 遇到仓库 API 名称不同，映射到现有 API；保持本文语义。
12. 遇到本文没有要求的历史问题，不处理，记录后继续。
13. 最终输出变更文件列表、测试结果、尚未处理的范围外问题。
14. 不实现任何本文“明确不做”的内容。

---

# 97. 最终数据流

最终架构必须收敛为：

```text
                    ┌──────────────────────┐
                    │   Official Tokens    │
                    └──────────┬───────────┘
                               │
                               │
Overlay Input ── normalize ────┤
                               ↓
                     Effective Tokens
                               │
                     Canonical Identity
                               │
                           SHA-256
                               │
                         Custom themeId
                               │
              ┌────────────────┴───────────────┐
              │                                │
              ↓                                ↓
      Workspace Overlay                 Global Overlay
              │                                │
              └──────────────┬─────────────────┘
                             ↓
                     Main Theme Resolver
                             │
                             ↓
                  ResolvedWechatTheme
                             │
                             ↓
                WechatThemeDefinition
                             │
                             ↓
                       Shared Compiler
                             │
                             ↓
                    FINAL PUBLISH HTML
                             │
            ┌────────────────┼────────────────┐
            ↓                ↓                ↓
          Audit           Preview          Output
                                             │
                            ┌────────────────┼─────────────┐
                            ↓                ↓             ↓
                        Clipboard         方案 B          API
```

这张图就是最终系统边界。

任何实现如果出现：

```text
Renderer 读 themes/
Shared 读 themes/
API 再 applyTheme
方案 B 再 compile
custom ID fallback official
hash Partial patch
Overlay 内存 fontFamily
```

均视为违反开发规范。

---

# 98. 到此停止

满足 Definition of Done 后停止开发。

不要继续实现：

```text
主题市场
社区主题
CSS Editor
更多 Recipe
在线下载主题
背景图
字体主题
自定义组件
```

这些属于后续独立需求。
