# fantastic-editor 公众号图片策略与技术验证方案

> 文档状态：`0.2.0-rc.3` 剪贴板基线已冻结；`0.3.0-rc.1` 官方 API 自动草稿路线已通过真实账号验收；`0.3.0-rc.2` 已包含自定义主题并重新通过本机发布门禁
> 版本：1.3-draft
> 关联文档：[fantastic-editor 开发项目书](fantastic-editor-开发项目书.md)  
> 关联规格：[统一文档模型规格](fantastic-editor-统一文档模型规格.md)

## 当前策略结论（2026-08-31）

- 方案 C“官方 API 自动创建草稿并可显式发布”已成为有接口权限用户的默认路线：批量上传普通图片、公式和 Mermaid PNG，替换正文占位，上传封面，创建草稿并回读校验；用户二次确认后再提交发布并轮询官方状态。用户已确认真实公众号草稿验收清单 1–5 全部通过；这不等同于真实发布验收。
- 方案 B“复制正文 + 图片替换助手”继续保留，但只作为无 API 权限、接口失败或用户主动选择人工流程时的兜底；不得再描述为多图一键发布。
- 方案 A“单次富文本复制并自动保留全部本地图片”未获得足够稳定性，不作为产品承诺。微信公众号后台的“一键排版”仍明确禁止作为后处理步骤，以免覆盖样式并破坏列表、任务项或代码块。
- 公众号设置只保留主界面顶部一个入口；AppSecret 由 Windows `safeStorage` 加密保存。白名单检测可识别 `40164`、显示/复制当前公网 IPv4，并解释迁移后的开发者平台配置路径。
- 自动同步的安全边界保持不变：默认只创建草稿；一键发布必须二次确认，且只发布当前单篇文章，不群发、不定时群发、不删除。任何未替换占位、本地/临时/Data URI、资源缺失或安全审计失败都阻止完整成功。

## 2026-08-31：RC 验收边界

- `0.3.0-rc.1` 只收口单公众号、单篇草稿同步和未签名 Windows 发行包；用户已确认草稿验收清单 1–5，发布接口仍保留为显式二次确认后的后续动作。
- 本轮发布门禁须重新产生安装版、便携版、产物校验和 SHA-256 清单；不得把 `0.3.0-dev.1` 产物哈希冒充 RC 哈希。
- RC 产物已生成且草稿真实验收清单 1–5 已通过；Windows 安装—启动—卸载自动门禁的目录残留问题已修复并随当前 RC.2 重新通过。该门禁与公众号内容验收仍是两条独立状态线。
- 当前 RC.2 最新安装版为 136,690,113 字节，SHA-256 `DD45E7A9012329340C4C10BE8C43F0488C112B0309CB744A0800F7A08998E2EE`；便携版为 136,432,037 字节，SHA-256 `A2643D21805C9DB6BF239A1D36B7A67948EAA692D85374A9298D99D513035F16`。两者均为 `NotSigned`，且已通过隐私、七组生产冒烟和安装/卸载门禁。

## 路线状态总表

| 路线 | 当前定位 | 验证状态 |
| --- | --- | --- |
| A：一次剪贴板带全部图片 | 不承诺 | 微信环境稳定性不足 |
| B：正文复制与逐项图片替换 | 兼容兜底 | 已实现并有真实平台历史记录 |
| C：官方 API 自动草稿 | 默认主流程 | 2026-08-29 真实账号通过 |


## Mermaid 流程图与字体补充策略

- Mermaid 代码块进入公众号方案 B 时，不复制 SVG、脚本、data URI 或临时地址。隔离 Chromium 渲染器先生成 PNG，公众号适配器再输出连续编号占位，替换项 `kind` 为 `diagram`。
- `diagram` 与本地图片、公式共同使用同一全局序号，逐项复制时只能从当前任务的受控替换清单读取 PNG。任务身份、内容哈希、尺寸和 MIME 必须一致；缺失或不匹配产生 blocking 诊断。
- 用户从微软雅黑、Segoe UI、Arial、等线、宋体、楷体等内置预设或自定义入口选择的正文字体，经主进程规范化后写入公众号根 `<section>` 的内联 `font-family`，并保留微信环境后备字体。字体设置不得改变图片顺序、占位编号、`approvedOmittedReferenceKeys` 或完成状态。
- 同步滚动 ON/OFF、预览 Mermaid SVG、`data-source-*` 和字体选择器均属于应用内部交互，不得进入 CF_HTML。回归测试必须断言公众号 HTML 无 Mermaid 运行时脚本、内部 SourceRange 属性或预览提示层。

## 源代码 / 所见即所得模式的公众号输出约束（编辑链路已实现）

> 编辑链路已落地并保持 canonical `editorText` 单一来源；source/wysiwyg 固定样例的规范化输出等价回归已经进入自动门禁，`0.3.0-rc.1` 官方 API 自动草稿也已完成真实公众号后台验收。
>
> 所见即所得直接编辑采用块级提交；处于未结束 IME composition 的内容、尚未失焦且未显式提交的非空新段落，不得直接启动公众号任务。空白画布上的“开始输入…”临时段落不是正文，`commitPending` 必须直接丢弃；非空段落必须提交成功后，才能以 canonical `editorText` 创建不可变输出快照。提交失败时阻止复制，不允许读取当前可视 DOM 补齐。
>
> 列表项、引用和表格单元格的直接编辑同样必须先写回正式 Markdown；任务框切换、列表 Enter 新项目和单元格 Tab 提交均不得从当前 DOM 直接生成公众号 HTML。表格单元格内部 SourceRange、可编辑状态和同步映射属性属于预览元数据，必须在 CF_HTML 生成前剥离。
>
> 图片属性面板的 alt 修改、图片替换和图片删除均已接入 canonical Markdown 事务。替换仍使用主进程校验后的资源导入回执；删除只移除引用且不改变其他图片的公众号替换顺序。任何未提交的图片属性、选择框或应用内部图片句柄不得进入 CF_HTML。
>
> 列表 Tab / Shift+Tab 层级调整和 Ctrl/Cmd+B、Ctrl/Cmd+I、Ctrl/Cmd+K 格式命令必须先以单次原子事务提交到 canonical Markdown，再由公众号适配器重新解析。所见即所得内部的 HTML ready/not-ready 门控、旧投影和 SourceRange 映射状态仅用于防止错误写回，不得进入输出快照、目标 HTML 或 CF_HTML。
>
> 公式、Mermaid 和普通 fenced code block 的专用面板也必须先提交 canonical Markdown。公众号任务只能读取重新解析后的 LaTeX 或 `codeBlock` 字段；KaTeX 即时 DOM、Mermaid 面板 SVG、语法提示、语言输入框和面板草稿不得进入输出。公式或 Mermaid 草稿仍未提交、SourceRange 已过期或提交失败时，统一 `commitPending` 必须阻止任务开始，不得以当前可视结果替代正式 Markdown。
>
> 第六轮混合行内编辑中的受保护原子、`data-source-kind`、`contenteditable` 属性、Renderer WeakMap 原始切片和原子选中边框全部属于短生命周期编辑状态，不得进入输出任务。用户修改原子前后的普通文字或通过公式/图片面板修改内容后，必须先提交 canonical Markdown 并等待当前快照重新解析；公众号适配器只读取该正式 Markdown。链接、行内代码、公式和图片的替换顺序及资源诊断不得从所见即所得 DOM 推导。
>
> 第七轮链接与行内代码面板的显示文字、地址、标题、代码草稿、fence 提示和表单 DOM 同样不得进入公众号任务。应用或完成操作必须先生成一个 canonical Markdown TextChange；公众号适配器重新解析正式链接和 `inlineCode` 语义，不读取面板 state 或当前 `<a>/<code>` DOM。链接地址的输出安全仍由统一 HTML 清洗、协议策略和目标适配器负责，编辑面板不得成为绕过清洗器的直通通道。
>
> 第八轮表格工具栏、当前行列索引、按钮禁用态、零长度空单元格锚点和结构调整期间的旧投影清空状态都属于 Renderer 临时交互，不得进入公众号任务。行列增删、列对齐或末格 Tab 追加行必须先以完整表格 SourceRange 的单次事务写回 canonical Markdown，并等待当前快照重新解析；公众号适配器只解析正式 GFM 表格，不读取当前 `<table>` DOM，也不复用编辑工具栏生成的 HTML。
>
> 第九轮跨块 DOM Range、Selection、浏览器选区高亮、ClipboardEvent、临时 fragment 和工具栏批量格式状态均属于 Renderer 短生命周期交互，不得进入公众号任务或 CF_HTML。跨块删除、剪切、替换、粘贴和批量格式必须先以单次 TextChange 写回 canonical Markdown 并等待当前快照解析；公众号适配器只读取正式 Markdown。跨图片、公式、代码、Mermaid、表格或其他受保护结构的操作被拒绝时不得提交边界块，也不得改变后续公众号预检、占位顺序、approvedOmittedReferenceKeys 或 OutputResult.status。复制所见即所得选区的 text/plain 与“复制到公众号”是两个独立入口，前者不得绕过公众号方案 B、资源预检或 CF_HTML 清洗。

- 编辑模式不得改变公众号输出语义。复制任务只接受当前 canonical `editorText` 解析得到的已提交 ParsedDocument、ResolutionSnapshot 和派生资源，不读取所见即所得 DOM、innerHTML、浏览器 Selection 或未提交 composition 文本。
- 从相同 documentId、sourceHash、workspaceRevision、主题和字体设置发起任务时，源代码模式与所见即所得模式必须产生相同的预检诊断、替换清单顺序、`approvedOmittedReferenceKeys` 消耗和 OutputResult.status。
- 所见即所得视图中的工具栏、选中态、编辑把手、只读提示、SourceRange 属性、同步滚动标记、KaTeX/Mermaid 预览 DOM 和应用临时 URL 不得进入目标 HTML 或 CF_HTML。
- 在所见即所得模式拖入、替换或删除图片，必须先以原子事务写回正式 Markdown 图片语法并完成资源解析；公众号适配器不得从可视 DOM 猜测图片或建立第二套附件顺序。
- 公式和 Mermaid 的可视编辑必须先提交 LaTeX 或 fenced code block 源码。未提交编辑、语法错误或派生资源 blocking 诊断不能被“当前画面看起来正常”掩盖。
- 固定测试文章必须分别从 source 与 wysiwyg 模式发起方案 A/B，比较规范化 HTML、替换项、资源哈希、诊断和最终状态；任何差异均视为回归失败。

## 一、问题定义

fantastic-editor 的公众号主流程包含本地图片，但本地图片不能直接依赖以下地址进入微信公众号：

- file 地址
- 应用内部协议地址
- blob 地址
- localhost 地址
- 仅在当前电脑有效的临时地址

同时，不能在未验证前假设微信公众号编辑器一定接受并持久化：

- Data URI
- Windows CF_HTML 中的内嵌图片
- Electron 剪贴板中的原生位图
- 浏览器复制出来的临时图片地址

因此，“带本地图片的一键复制”不是文档层面可以直接承诺的事实，而是首个必须验证并冻结的产品决策。

## 二、验证目标

本方案需要回答唯一核心问题：

> 在目标 Windows 和微信公众号编辑器环境中，能否把一篇包含多张本地图片、公式图片和复杂排版的文章，通过一次富文本复制稳定写入公众号草稿，并在保存、关闭、重新打开和跨端预览后继续显示？

如果答案是否定的，项目必须明确选择图片替换助手或把图片上传能力提前，不能继续使用“一键复制带图文章”的模糊表述。

## 三、不可违反的约束

- 最终公众号正文不能依赖本地或应用临时地址。
- 未经用户明确同意，不把文章图片上传到第三方图床。
- P0 不明文保存公众号密钥或令牌。
- 技术验证必须使用真实公众号编辑器，不能只在本地测试页验证。
- 图片在刚粘贴时可见，不代表验证通过。
- 必须保存草稿、关闭页面、重新打开并再次预览。
- 公式转换出的图片与普通图片适用同一验收规则。
- 验证不通过时，必须诚实降低 P0 产品承诺或正式扩大范围。

### 编辑器导入图片与公众号输出的关系

用户拖到编辑区或通过“插入图片”按钮导入的图片，写入 Markdown 同级 `assets/` 并形成普通相对 Markdown 图片语法。导入来源只属于文件会话和编辑事务，不改变公众号策略：

- 导入完成后，图片必须形成正式 ImageNode、ResourceReference 和 ResolutionRecord，不能由附件列表绕过解析、预检或安全检查。
- 公众号适配器按正文节点顺序处理这些图片，与用户手写 `![alt](./assets/name.png)` 完全一致。
- 本地预览成功只说明授权资源可读，不代表公众号已取得持久图片。
- 方案 B 必须自动把所有 resolved 导入图片加入编号占位和替换助手；用户不需要再次在应用内选择同一图片。
- 图片导入功能本身不把图片上传到微信，也不能把“拖入后可预览”宣传为“复制后公众号自动带图”。
- 真正无需逐图替换的能力必须由通过真实环境验证的方案 A，或使用微信图片上传与草稿接口的独立账号/API 模式提供。
## 四、P0 可选产品定义

技术验证完成后，只能冻结以下一种主定义。

方案 A 和方案 B 使用不同的处理顺序：

    方案 A
    解析资源 → 转换普通图片和公式图片 → 生成完整 HTML → 写入剪贴板 → 用户粘贴

    方案 B
    生成带编号占位的 HTML → 写入剪贴板 → 用户粘贴 → 逐张复制或上传普通图片和公式图片

主项目书和实现不得把两条流程合并成“先复制，再统一处理图片”。

### 方案 A：带图富文本一次复制

产品承诺：

- 用户点击一次“复制到公众号”。
- 正文、样式和所有支持的图片一起进入公众号编辑器。
- 保存草稿并重新打开后图片仍正常。
- 不需要用户逐张替换图片。

选择条件：

- 候选剪贴板方案通过全部必测用例。
- 没有本地或临时地址残留。
- 结果在目标账号和目标浏览器环境中稳定复现。

### 方案 B：文字样式复制加图片替换助手

产品承诺：

- 一次复制正文和排版。
- 图片位置使用明确编号的占位结构。
- 应用显示图片清单和正文位置。
- 用户逐张点击“复制此图片”，在公众号对应位置粘贴或上传。
- 应用跟踪哪些图片已经处理。

选择条件：

- 一次复制无法稳定持久化多张本地图片。
- 单张图片复制或公众号手动上传可稳定完成。
- 用户可接受额外操作。
- P0 不扩大到账号授权和素材接口。

该方案仍然可以完成公众号发布，但不能称为“带图一键复制”。

### 方案 C：将图片上传能力提前到 P0

产品承诺：

- 软件通过经验证的平台能力上传图片。
- 用持久化图片地址替换正文资源。
- 用户再复制正文或创建草稿。

选择条件：

- 业务必须要求一次完成带图文章。
- 方案 A 不通过。
- 用户愿意接受账号授权、服务端、安全和维护成本。
- 项目重新评估工期和发布边界。

方案 C 原本属于正式范围变更；现已由产品决策批准为 `0.3.0` 开发主线。它不改变 `0.2.0-rc.3` 的方案 B 兼容承诺，必须按账号/API 能力单独验收。

## 五、候选技术路线

### 路线 1：CF_HTML 加 Data URI

流程：

1. 将本地图片读取为受支持格式。
2. 根据限制进行尺寸处理。
3. 将图片编码为 Data URI。
4. 生成符合 Windows 富文本剪贴板要求的 HTML。
5. 同时写入 HTML 和纯文本。
6. 粘贴到公众号编辑器。
7. 保存并重新打开草稿。

需要验证：

- 公众号是否接受 Data URI。
- 粘贴时是否自动把 Data URI 转换为平台图片。
- 多图是否全部持久化。
- 大图是否触发剪贴板或编辑器限制。
- GIF 是否仍保留动画。
- WebP 是否需要转换。
- 公众号是否删除图片或只留下空标签。

未通过全部持久化测试时，该路线不能用于方案 A。

### 路线 2：渲染内容复制加浏览器剪贴板事件

流程：

1. 在隔离的复制视图中渲染公众号兼容内容。
2. 使用浏览器复制事件或选择复制生成富文本。
3. 观察编辑器如何接收图片。
4. 保存并重新打开草稿。

需要验证：

- 复制结果中图片源是什么。
- blob 或应用协议是否被复制成无效地址。
- 浏览器是否把图片转成可持久化的剪贴板数据。
- 多张图片是否按原位置进入正文。

如果复制结果仍依赖本地地址，该路线失败。

### 路线 3：HTML 加原生位图剪贴板格式

流程：

1. 写入 HTML 正文。
2. 同时写入原生图片剪贴板格式。
3. 测试单图和多图的对应关系。
4. 粘贴、保存并重新打开。

已知验证重点：

- 常见原生图片剪贴板通常只能明确表达当前单张图片。
- 必须验证公众号能否把原生位图与 HTML 中多个图片位置对应。
- 如果只能处理一张图片，该路线只能作为方案 B 的“复制此图片”，不能用于方案 A。

### 路线 4：图片替换助手

流程：

1. 公众号正文适配器把图片节点转为明显的编号占位，例如“图片 03：实验结果”。
2. 应用显示所有图片、原文位置、说明和处理状态。
3. 用户在公众号正文定位占位。
4. 点击应用中的“复制此图片”。
5. 在公众号中粘贴或使用上传按钮。
6. 用户确认后标记完成。
7. 所有图片完成后再保存草稿。

需要验证：

- 单图复制是否稳定。
- 占位符是否容易定位。
- 替换后标题、段落和图片顺序是否保持。
- 公式图片是否足够清晰。
- 20 张图片时操作成本是否可接受。

该路线是方案 A 失败后的默认 P0 降级方案。

### 路线 5：公众号素材或正文图片上传

流程：

1. 用户明确启用“创建公众号草稿”模式并选择已配置的公众号账号；该动作与“复制到公众号（方案 B）”分开命名和呈现。
2. 主进程或受控服务从系统凭据存储取得账号配置并获取/刷新 access token；Renderer、Markdown、恢复稿和日志不得接触 AppSecret 或 token。
3. 对普通图片和公式派生图片执行格式、尺寸、体积与透明背景预检；按当前微信接口约束转换为允许上传的输出格式。
4. 逐项上传正文图片，记录 source reference/formula reference 到微信持久 URL 的任务内映射；相同 contentHash 在同一账号和有效缓存期内可以复用，但不得跨账号混用。
5. 只在公众号输出 HTML 中把图片节点替换为微信返回的持久 URL，不修改 ParsedDocument、ResolutionSnapshot、editorText 或 `assets/` 文件。
6. 封面使用独立素材上传流程并取得 thumb_media_id；没有满足草稿接口要求的封面时不得伪装草稿创建成功。
7. 所有正文图片上传成功后才调用草稿创建接口；返回草稿标识后显示“草稿已创建”，不能显示“已发布”。
8. 查询和记录每项结果。任一未获批准的图片失败都使任务 failed；用户在预检阶段明确批准省略时只能得到 completed-with-omissions，不能显示完整成功。
9. 取消或超时时停止后续上传/草稿调用；已经上传到微信的远程资源可能无法自动回滚，结果页必须列出并提供后续清理说明。

该路线属于独立的方案 C/阶段 6 账号/API 能力，已经成为 `0.3.0-rc.1` 的默认主流程并通过真实账号验收，而不是剪贴板方案 A/B 的隐式降级路径。账号类型和接口权限、授权方式、图片与草稿接口限制、凭据安全、token 生命周期与缓存隔离仍是运行前置条件。当前代码在主界面顶部提供常驻配置入口与主进程连接器：AppSecret 经窄 IPC 送入主进程后由 Electron `safeStorage` 加密落盘，界面只返回 `hasAppSecret`，不回传或回显明文；环境变量仅保留作开发兼容入口。配置窗口打开或保存完整配置后必须检测微信连接；`40164 invalid ip` 应转换成白名单引导，显示并允许复制微信识别到的当前公网 IPv4。白名单已放行时可通过 `api.ipify.org` 的只读 HTTPS 查询补充显示公网 IPv4，界面必须披露来源且查询失败不得把微信连接误报为失败。正式多账号或分发产品仍应评估自托管连接器或微信公众号第三方平台授权。

配置入口必须唯一：主界面顶部常驻“公众号设置”，发布验收助手只负责同步与结果反馈，不再重复显示配置按钮。2026-08-29 真实账号验收已确认自动上传文章进入草稿箱且格式正常。

### 明确排除的路线

P0 不使用：

- 临时本地 HTTP 服务供公众号引用
- 未经授权的公共图床
- 把私人图片自动上传到第三方
- 只在当前页面存活的 blob 地址
- 未验证就默认 Data URI 可用

## 六、推荐验证顺序

按以下顺序执行，避免过早扩大范围：

1. `0.2.0-rc.3` 继续保留方案 B 作为无 API 权限时的兼容兜底。
2. `0.3.0` 优先实现路线 5：批量上传正文图片/公式/Mermaid 图片、替换持久 URL、上传封面、创建并回读草稿。
3. 只有真实账号接口不可用时，才评估浏览器辅助批量替换；剪贴板连续推送只作为最后兜底，不得宣传为全自动。
4. Data URI、临时本地服务和未经授权的公共图床不作为默认路线。

不允许在正式代码中同时保留三套未经验证的自动策略并依次碰运气。技术验证可以比较多条路线，产品冻结后必须有一个默认主策略和明确的失败行为。

## 七、测试环境

每次验证记录：

- 日期
- Windows 版本和系统架构
- Electron 和 Chromium 版本
- 使用的浏览器及版本
- 微信公众号编辑器页面版本或可识别特征
- 公众号账号类型
- 目标支持矩阵中的环境编号
- 是否为新草稿或已有草稿
- 显示器缩放
- 剪贴板增强软件是否启用
- 网络环境
- 测试人员

至少覆盖项目实际发布时支持的参考 Windows 环境。若不同浏览器结果不同，P0 必须明确支持浏览器范围。

## 八、固定测试文章

### 1. 最小文章

- 一个标题
- 两个段落
- 一张 PNG
- 一条行内公式
- 一个块级公式

用途：快速判断基本路线是否可行。

### 2. 标准文章

- 三级标题
- 列表、引用、表格和代码块
- 五张普通图片，其中至少两张通过拖放导入、一张通过“插入图片”按钮导入，其余为手写相对引用
- 图片位于不同段落
- 图片前后有说明文字
- 两个行内公式
- 两个块级公式
- 两个 Mermaid 流程图，其中一个用于验证中文节点和连线标签
- 中文、空格和括号文件名

若公式和 Mermaid 均采用图片策略，该文章的替换操作总量按十一项计算：五张普通图片、四个公式和两个流程图。

用途：作为主要 P0 回归样例，并评估方案 B 的真实人工操作成本。同一文章必须分别从源代码和所见即所得模式发起输出并比较结果。

### 3. 压力文章

- 二十张普通图片
- 两张大尺寸图片
- 多个会生成派生图片的公式
- 多个 Mermaid 流程图，覆盖接近数量上限、复杂中文标签、语法错误和单图源码长度上限
- 长代码块
- 宽表格
- 一篇接近实际长文长度的正文

用途：验证剪贴板大小、内存、等待时间和丢图情况，并验证达到公开上限时是否在复制前阻止任务。

压力文章分为：

- limit-within：处于产品公布上限之内，必须完整通过。
- limit-exceeded：超过产品公布上限，必须在复制前明确阻止或引导降级，不能进入半成功状态。

### 4. 格式文章

分别包含：

- PNG
- JPEG
- GIF
- WEBP
- SVG 经安全栅格化后的 PNG
- 透明背景图片
- 超宽图片
- 纵向长图
- 相同文件名但不同内容的图片

不支持的格式必须在复制前转换或给出 blocking 诊断。

### 5. 输入安全文章

分别包含源 Markdown data URI、Windows 盘符绝对路径、盘符相对路径和 UNC 图片引用。源 data URI 的预期结果固定为 DATA_URI_SOURCE_BLOCKED。payload 只允许存在于原始 editorText 和 document-core 解析输入，不得进入 ImageNode、ResourceReference、Diagnostic、ResolutionSnapshot、ResolveRequest、复制任务、缓存或日志；本测试与路线 1 在输出阶段生成 Data URI 明确分开。

## 九、完整测试步骤

每个候选路线执行以下步骤：

1. 启动干净的应用会话。
2. 打开固定测试文章。
3. 分别拖入图片到段落之间、点击“插入图片”插到当前光标，并确认生成 `assets/` 相对引用；确认所有本地图片和公式在应用预览中正常。
4. 执行公众号发布前检查。
5. 点击复制。
6. 在新的公众号草稿中粘贴。
7. 记录刚粘贴后的文字、样式、图片顺序和图片数量。
8. 等待编辑器完成可能的上传或转换。
9. 保存草稿。
10. 关闭当前编辑页面。
11. 重新进入公众号后台并打开该草稿。
12. 再次检查所有图片。
13. 使用公众号预览能力检查移动端效果。
14. 在另一浏览器会话或另一设备查看预览，确认图片不依赖当前电脑。
15. 检查正文是否残留 file、app、blob、localhost 或其他临时地址。
16. 记录失败图片、错误提示、耗时和内存。
17. 对同一路线重复至少三次。

只完成第 6 步不算通过。

## 十、通过标准

方案 A 必须同时满足：

- 最小文章和标准文章中的所有支持图片都进入正确位置。
- 最小文章和标准文章分别连续三次没有丢图。
- limit-within 压力文章中的普通图片和公式派生图片全部进入正确位置。
- limit-exceeded 压力文章在写入剪贴板前被明确阻止或引导到已冻结的降级方案。
- 保存、关闭和重新打开草稿后图片仍存在。
- 移动端或跨会话预览正常。
- 最终正文不依赖本地、应用或临时地址。
- 图片顺序不改变。
- 图片与说明文字的对应关系不改变。
- 公式图片清晰可读。
- 失败时应用在复制前或复制过程中给出明确错误，不静默漏图。
- 操作不需要用户逐张替换图片。

产品必须公布普通图片、公式派生图片合计后的数量、单图大小、总字节数和剪贴板大小限制。超过限制不等于方案 A 自动失败，但静默产生部分结果属于失败。

方案 B 必须满足：

- 文字和排版一次复制稳定。
- 每个图片占位可明确定位。
- 单图复制或上传稳定。
- 应用可以显示已处理和未处理状态。
- 保存并重新打开后，已替换图片仍存在。
- 用户不会因为占位消失而遗漏图片。
- 公式图片使用相同流程并保持清晰。
- 操作成本按“普通图片数 + 公式派生图片数 + 后续图表派生图片数”计算，不能只统计原始图片。

## 十一、失败分类

每次失败使用固定代码：

- WECHAT_IMAGE_NOT_INSERTED
- WECHAT_IMAGE_TEMPORARY_ONLY
- WECHAT_IMAGE_LOST_AFTER_SAVE
- WECHAT_IMAGE_LOST_AFTER_REOPEN
- WECHAT_IMAGE_ORDER_CHANGED
- WECHAT_IMAGE_FORMAT_REJECTED
- WECHAT_IMAGE_TOO_LARGE
- WECHAT_CLIPBOARD_TOO_LARGE
- WECHAT_FORMULA_IMAGE_BLURRY
- WECHAT_LOCAL_URL_REMAINED
- WECHAT_PARTIAL_COPY
- WECHAT_EDITOR_VERSION_REGRESSION

失败记录包括：

- 测试用例
- 路线
- 环境
- 重现步骤
- 实际结果
- 期望结果
- 截图
- 复制后的 HTML 摘要
- 是否可稳定复现

日志和报告不得包含用户真实私人文章或敏感图片。

## 十二、方案 A 的实现要求

只有验证通过后才实施。

### 1. 图片预处理

- 输入为匹配 sourceHash 和 workspaceRevision 的 ParsedDocument 与 ResolutionSnapshot。
- 只对 state 为 resolved 的 ResolutionRecord 按 referenceKey 获取受限 assetHandle；跨编辑缓存使用 assetCacheKey 和 contentHash。blocked、missing、ambiguous、unsupported 或 failed 的记录不得读取。
- 校验 MIME 和实际文件内容。
- 处理 EXIF 方向。
- 根据限制进行尺寸处理。
- ResourceReference.kind 为 remote-http、file-uri、app-internal 或 unsupported-scheme 时，按 P0 阻止策略处理，不能拼成本地路径。
- 源 Markdown data-uri 在 P0 固定阻止，生成 DATA_URI_SOURCE_BLOCKED，不解码。payload 只允许存在于原始 editorText 和 document-core 解析输入，不得带入资源模型、Resolve IPC、复制任务、缓存或日志。路线 1 只能从已授权、已校验的 resolved 资源字节重新生成输出 Data URI，不得复用原始源字符串。
- 根据冻结规则转换不兼容格式。
- SVG 使用安全栅格化结果。
- 普通图片格式转换图和缩放图由隔离图片处理进程生成；公式图片由当前 jobId 专属的隐藏、沙箱化 AssetRenderWindow 使用本地 KaTeX 批量生成，并在每项之间清理 DOM。两类结果都写入本次任务的 DerivedAssetManifest，任务结束后销毁窗口。
- 为派生图片生成 contentHash 和 derivedAssetKey；公式派生项使用 SourceRange、latexHash 和 displayMode，不使用 nodeId。
- 不修改 ParsedDocument 或 ResolutionSnapshot。
- 保留必要透明度。
- 默认不做破坏性过度压缩。

### 2. 复制任务状态

主进程创建 jobId。用户取消时发送 CancelJob(jobId)，不跨进程传输 AbortController 或 cancellationToken；取消后的迟到结果不得写入剪贴板。

复制任务的最终 OutputResult.status 固定为 completed、completed-with-omissions、failed、cancelled 或 timed-out。failed、cancelled 和 timed-out 可以从任何未完成状态进入。

    created
      → resolving-assets
      → converting-images
      → preflighting
      → awaiting-user-approval（仅存在可批准省略时）
      → building-html
      → writing-clipboard
      → completed / completed-with-omissions

任何图片仍为 missing、blocked、ambiguous、unsupported 或 failed 时，不允许显示“完整复制成功”。

WechatClipboardAdapter 必须在写剪贴板前返回 OutputPreflightResult，列出完整 candidateOmittedReferenceKeys 和不可覆盖诊断。主进程为预检创建 preflightId；只有用户批准全部候选才进入 building-html。若只批准部分候选，必须取消或修改选项后以新 preflightId 重新预检。生成或写剪贴板阶段出现新的未批准省略时，任务直接 failed。

若用户在复制前查看完整清单并明确批准省略，主进程把精确 referenceKey 写入本次任务的 approvedOmittedReferenceKeys。批准只绑定当前 preflightId、jobId、documentId、sourceHash 和 workspaceRevision，不得复用；不得读取被阻止资源。只有 omittedReferenceKeys 非空且与本次实际采用的 approvedOmittedReferenceKeys 完全一致时，状态才能为 completed-with-omissions；任何未批准的省略都必须使任务 failed。界面只能显示“已完成，但有省略”，不得显示“完整复制成功”，也不得把该次结果计入方案 A 的完整通过次数。

应用必须显示：

- 普通图片数
- 公式派生图片数
- 已处理图片总数
- 跳过图片数
- 转换失败图片
- 剪贴板总大小
- 当前 sourceHash 和策略版本
- 最终采用的图片策略
- 用户下一步

### 3. 最终 HTML

- 不包含本地绝对路径。
- 不包含应用内部协议。
- 不包含 blob 和 localhost。
- 不包含脚本、事件属性和危险协议。
- 图片 alt 文本保留。
- 宽度样式符合冻结的公众号主题。
- 复制专用属性在确认无用后移除。
- 公式派生图片按普通图片处理。

## 十三、方案 B 的实现要求

若自动多图复制不通过，P0 默认采用图片替换助手。

### 1. 正文占位

每个需要人工替换的普通图片或公式派生图片生成：

- 连续编号
- 类型：普通图片、公式或 Mermaid 流程图
- 布局：inline 或 block
- alt、公式摘要或简短说明（只进入助手 label，不写进短占位）
- 原文位置
- 资源格式和实际像素尺寸
- 状态

真实公众号验证后，占位文本冻结为短、明显、可搜索且容易完整选中的格式，例如：

    【FE图片03｜整段替换】
    【FE公式04｜行内替换】

占位不得使用虚线边框或固定高度空框。placeholderText 必须与正文逐字符一致，详细 label 只在应用助手中显示。

方案 B 的编号占位属于目标定义输出：普通图片占位的 referenceKey 计入 usedReferenceKeys，公式占位的 formulaReference 计入 usedFormulaReferences；两者都不进入 omittedReferenceKeys，也不需要 approvedOmittedReferenceKeys。只有占位未生成、顺序错误或无法定位时才使任务 failed。

### 2. 图片清单

应用侧显示：

- 图片或公式 1 至 N
- 类型与 inline/block 布局
- 普通图片 alt、公式摘要或 Mermaid 说明
- 格式和尺寸
- 原文字符位置
- 复制按钮
- “图片已出现且占位文字已消失”复选状态
- 普通图片数、公式数和总操作数

### 3. 完成检查

用户点击“全部图片已处理”前，应用显示未完成数量。

由于应用无法可靠读取公众号页面最终状态，P0 可以采用用户确认，但必须提供清晰检查清单，不能自动宣称发布内容完整。

### 4. 编辑器导入图片的处理

- 替换清单只能从当前 OutputContext 中的 ImageNode、ResolutionSnapshot 和 OutputResourceAsset 生成，不能扫描 `assets/` 目录后猜测附件。
- 拖放或按钮导入图片的 alt、SourceRange、referenceKey 和正文顺序与普通 Markdown 图片一致；清单顺序必须与公众号 HTML 占位顺序完全一致。
- 对同一资源的多次正文引用生成多个可定位占位，但可以复用同一已校验图片字节；应用不得因为 contentHash 相同而擅自删除某个正文位置。
- 点击“复制此图片”继续由主进程读取当前任务保留的受控字节并写入系统位图剪贴板，Renderer 不接收原图路径或图片字节。
- 图片在导入后被删除、替换或外部修改时，旧 OutputContext 失效；必须重新预检，不能沿用旧替换清单。

### 5. 实时预览交互标记的隔离

- “同步滚动”和源码选区提示属于本地 Renderer 交互能力，不改变 WechatClipboardAdapter 的输入、占位顺序、图片清单或完成状态机。
- 实时预览使用的 data-source-from、data-source-to、data-source-kind、元素键、选区提示框和激活样式不得进入公众号 HTML、CF_HTML、纯文本回退或图片替换清单。
- 公众号输出必须从当前 OutputContext 和目标定义的 WechatClipboardAdapter 重新生成，禁止复制内部预览 DOM 后仅靠 CSS 隐藏交互标记。
- 回归测试必须断言公众号 HTML 不含 data-source-*、PreviewSyncMap、SelectionOverlay 或应用内部元素键；同步滚动开关的开关状态不得改变同一 OutputContext 的公众号输出。

## 十四、公式、图表和封面

### 1. 公式

KaTeX 公式在公众号输出时生成高分辨率派生图片或经验证的兼容结构。

如果使用图片：

- 透明背景和正文颜色适配主题。
- 行内公式保持合适基线和高度。
- 块级公式居中。
- 倍率经过移动端预览验证。
- 进入与普通图片相同的持久化测试。
- P0 任一公式渲染失败都使当前任务 failed；公式节点没有 referenceKey，不进入 approvedOmittedReferenceKeys。

### 2. Mermaid

Mermaid 已纳入当前 P0 公众号输出承诺。代码块必须在无网络的隔离 Chromium 渲染器中转换为 PNG，不得把 SVG、脚本、data URI、blob URL 或应用临时地址写入 CF_HTML。方案 B 将每个成功生成的流程图作为 `kind: diagram` 的连续编号替换项；语法错误、超时、尺寸超限或派生资源校验失败产生 blocking 诊断，未经任务范围内明确批准不得显示为“完整成功”。标准文章和压力文章必须覆盖 Mermaid，并在真实公众号草稿中执行粘贴、保存、关闭、重新打开和移动端预览验证。

### 3. 封面

P0 不包含公众号封面上传。封面和文章元数据在 P1 或接口发布阶段单独设计，不与正文图片策略混淆。

## 十五、安全与隐私

- 默认只处理工作区授权范围内的图片。
- 编辑器导入只写 Markdown 同级 `assets/`；点击选择由主进程对话框授权，拖放只接收用户本次明确提供的受限字节，不开放任意路径读取 IPC。
- 导入源绝对路径、拖放图片字节和 ImportedAssetReceipt 不进入公众号日志；公众号适配器只按 referenceKey 使用重新校验后的任务资源。
- 账号/API 模式必须逐次明确选择账号并确认将要上传的图片数量；AppSecret/access token 使用系统凭据存储或受控服务，永不进入 Renderer、Markdown、导出 HTML或普通配置文件。
- 上传缓存按公众号账号、contentHash 和接口策略版本隔离；退出账号或撤销授权后清除本地 token 和不可继续使用的映射。
- 不向远程服务发送图片，除非用户明确选择需要上传的正式方案。
- 第三方图床不是默认依赖。
- 临时派生图片存储在应用专用临时目录。
- 应用退出或任务结束后按策略清理临时文件。
- 剪贴板可能包含文章和图片，完成操作后向用户提示剪贴板隐私风险。
- 不在日志中写入图片二进制、文章正文或完整私人路径。
- 图片解码和 SVG 栅格化在隔离进程中执行。
- 对图片尺寸、解码时间和总内存设限制，防止恶意图片耗尽资源。

## 十六、回归策略

公众号编辑器规则可能变化，因此：

- 每次发布 WechatClipboardAdapter 新版本前运行固定测试文章。
- Electron 或 Chromium 升级后重新运行。
- 公众号编辑器出现明显变化时重新运行。
- 图片策略版本单独编号，例如 wechat-image-strategy/1。
- 回归失败时由当前版本在本地显示兼容性警告，并通过正式版本说明发布适配状态；P0 不为此新增运行时远程配置、遥测或更新通道。
- 保留上一个已验证适配器的回滚能力。
- 同步滚动功能、双编辑模式或内部 PreviewAdapter 标记变更后，重新运行输出隔离测试，确保公众号正文和 CF_HTML 字节不发生非目标变化。

## 十七、决策记录模板

    决策日期：
    验证负责人：
    测试环境：
    测试账号类型：
    候选路线：
    固定测试文章版本：
    最小文章结果：
    标准文章结果：
    压力文章结果：
    保存后结果：
    重新打开结果：
    跨端预览结果：
    本地或临时地址检查：
    已知限制：
    安全与隐私影响：
    最终选择：方案 A / 方案 B / 方案 C
    P0 产品文案：
    P0 验收条款：
    需要修改的项目书章节：
    批准人：

## 十八、阶段 0 交付物

- 已冻结的参考 Windows、目标浏览器、公众号账号类型和测试环境矩阵
- 三条自动复制候选路线的验证结果
- 最小、标准、压力、格式和输入安全测试文章
- 粘贴后、保存后、重新打开后的截图
- 失败分类记录
- 性能和剪贴板大小记录
- 公众号图片策略决策记录
- 已冻结的 WechatClipboardAdapter 预检、批准、输入输出和状态机契约
- 已冻结的 P0 产品文案和验收条款

## 十九、当前推荐

`0.2.0-rc.3` 的本地剪贴板方案 B 已完成代码和本机发布收口；`0.3.0` 的方案 C 仍需真实账号接口验收。

执行顺序为：

1. 编辑器图片拖放/按钮导入继续接入方案 B；它不改变方案 B 的人工替换事实。
2. `0.3.0` 的默认公众号入口提供“同步到公众号草稿箱”和“一键发布到公众号”；后一动作必须二次确认，并在官方发布状态确认成功前保持处理中状态，轮询最多持续 90 秒。
3. 方案 B 的“复制到公众号”保留为没有 API 权限、接口失败或用户明确选择时的降级入口。
4. 方案 C 的凭据、远程图片和草稿状态必须隔离于 Renderer、Markdown、恢复稿和普通日志。
5. 每次接口策略或账号授权方式变化，先更新本节和测试，再宣称自动同步可用。

最终目标不是证明某种技术理论上可行，而是确保用户在真实公众号后台可以稳定完成一篇带图片文章的发布。
## 二十、嵌套列表编辑对公众号输出的约束

所见即所得第十轮只改变 Renderer 的编辑投影和对 canonical Markdown 的最小事务，不改变 WechatClipboardAdapter 的输入模型或图片方案 A/B/C。

- 公众号输出仍从最新 ParsedDocument、ResolutionSnapshot 和任务范围派生，不读取 `ListOwnContentProjection`、contenteditable DOM 或列表结构工具栏。
- 父项正文修改、整棵子树缩进/提升/移动和任务切换完成后，公众号 HTML 必须按 canonical Markdown 的最终列表层级生成；普通图片、公式和 Mermaid 的占位/替换顺序仍按文档语义顺序计算。
- 混合无序、有序和任务列表需加入固定公众号文章；验证复制、保存草稿和重新打开后层级、序号、checkbox 文案及图片占位顺序稳定。
- 复杂子树在编辑阶段降级源码模式不等于输出省略；只有既有预检与批准流程可以产生 `completed-with-omissions`，部分完成不得显示为完整成功。
- 回归必须确认 Renderer 的 `data-wysiwyg-list-own-content`、SourceRange 属性和列表工具栏不会进入 CF_HTML、系统剪贴板目标 HTML或公众号正文。
## 二十一、块级组织操作与公众号输出隔离

- 所见即所得第十一、十二轮只通过 canonical Markdown 的 SourceRange 事务改变文档；WechatClipboardAdapter 不读取块工具栏、拖动载荷、落点线、选中轮廓或 BlockEditContext。
- 块移动、复制、删除和模板插入完成后，公众号输出重新从最新 ParsedDocument 与 ResolutionSnapshot 生成，图片、公式和 Mermaid 的 used/omitted 顺序按最终文档语义顺序计算。
- 复制块不会复制 assets/ 文件；它只复制 Markdown 引用。相同资源仍按 referenceKey/contentHash 规则解析，方案 B 替换清单允许同一资源在多个正文位置出现。
- 固定公众号回归增加引用块复制、带图片块移动、Mermaid/公式块移动和删除后的清单顺序，并确认所有 Renderer 辅助属性与拖放标记不进入 CF_HTML。
- 第十二轮冻结不改变方案 A/B/C、批准省略或部分完成状态机；真实公众号持久化验收仍是发布承诺的唯一依据。
## 二十二、PDF 第十三轮对公众号输出的隔离

- A4 页边距、分页控制、表头重复、宽内容缩放及 PdfLayoutAudit 只在 target 为 pdf 时启用，不进入公众号 HTML 或 CF_HTML。
- WechatClipboardAdapter 不读取 pageCount、scaledElements、PDF 缩放标记或打印媒体样式；公众号正文仍按浏览器流式排版。
- PDF_CONTENT_TOO_WIDE 只阻断当前 PDF 任务，不可转化为公众号批准省略，也不改变方案 B 的图片、公式和 Mermaid 替换清单。
- 回归必须确认同一 ParsedDocument 的公众号输出在第十三轮前后无非目标字节变化。

## 二十三、Word 与离线 HTML 第十四轮对公众号输出的隔离

- DOCX 的 A4 页面、Word 样式、原生 numbering、固定表格列宽、重复表头和核心属性只属于 DocxAdapter，不进入公众号 HTML、CF_HTML 或图片替换清单。
- 离线 HTML 的 document title、语义 main、深色主题、响应式表格样式和离线 CSP 只属于 OfflineHtmlAdapter；WechatClipboardAdapter 继续使用公众号专用浅色排版与剪贴板外壳，不复制离线页面级元数据。
- DOCX/离线 HTML 的安全审计与资源内嵌不能改变方案 A/B/C、approvedOmittedReferenceKeys、used/omitted 顺序或 OutputResult 状态机。
- 同一 ParsedDocument 在第十四轮前后的公众号输出必须无非目标字节变化；回归重点确认 Word numbering 标识、离线 main/title/CSP 和深色变量均不会泄漏到公众号正文。
- Word 或离线 HTML 单独失败、取消、超时或部分完成不影响公众号任务，也不可转化为公众号范围内的已批准省略。

## 二十四、方案 B 发布验收助手第十五轮

### 1. 状态定义

- “正文已复制”只表示 CF_HTML 和纯文本已写入并从系统剪贴板读回，不表示已粘贴到公众号。
- “替换图片已复制”只表示当前位图已写入并从系统剪贴板读回，不表示占位已经替换。
- “已完成替换”“已保存草稿”“已重新打开复核”和“已完成移动端预览”均为用户对公众号后台实际操作的人工声明，应用不能自动推断。
- 清单全部完成只显示“本地验收清单已完成，仍不代表文章已发布”。含批准省略时继续显示部分完成警告。

### 2. 固定流程

1. 用户执行“复制到公众号”，应用生成带编号占位的正文并读回校验剪贴板。
2. 用户在公众号编辑器粘贴正文并勾选正文已粘贴。
3. 用户按清单逐项复制图片、公式或流程图，在与 placeholderText 完全一致的位置替换后手动确认。
4. 全部替换确认后才能勾选已保存草稿；保存后才能勾选重新打开复核；重开后才能勾选移动端预览。
5. 任一前置项撤销时，所有后续确认立即失效。
6. 文档变化、标签页切换、新任务或关闭助手时清空全部短寿命状态。

### 3. 安全与回归

- CF_HTML 写入后必须读回非空 HTML；位图写入后必须读回非空且尺寸一致的 nativeImage。
- 正文最终审计阻止可执行标签、事件处理器、data URI、本地/临时协议与 loopback 地址。
- 部分完成任务仍保留所有可用 replacementItems；批准省略的资源不生成虚假替换项，公式和 Mermaid 派生资源失败继续阻断。
- 固定回归覆盖文字文章、单图、多图、公式、Mermaid、批准省略、重新复制、手动确认、撤销级联和旧任务状态清除。
- 真实公众号的样式保留、草稿持久化、重新打开和移动端预览仍必须在目标账号中人工执行并留存验证记录；本轮代码不能替代该外部验收。

## 二十五、真实验收标准文章与记录第十六轮

### 1. 固定标准文章

- fixtures/wechat-acceptance/standard.md 是真实公众号回归的标准输入，assets/wechat-acceptance-chart.svg 是唯一普通图片资源。
- 每次公众号适配器、Electron、Chromium、图片转换或排版样式变更后，使用同一文章重新执行复制、逐项替换、保存、重开和移动端预览。
- 自动测试只验证语法覆盖、资源安全和适配器契约；真实后台结果必须在目标账号中操作并记录。

### 2. 人工验收记录

- 验收清单全部完成后可保存 Markdown 记录；主进程通过 jobId 绑定终态任务并核对全部替换项。
- 记录包含占位文字和省略键，便于追溯实际使用的方案 B 清单，但不保存文章正文、账号、路径或图片。
- completed-with-omissions 的记录固定为部分完成；清单完成不改变 OutputResult，也不升级为发布成功。
- 报告必须明确说明所有后台检查均为用户人工确认，应用未读取公众号最终草稿。

### 3. Chrome 使用边界

- 仅在用户明确授权的 Chrome 中使用现有登录状态；不检查 cookies、本地存储、密码或账号凭据。
- 可以创建或修改专用测试草稿、粘贴标准文章、替换测试图片、保存和重新打开；不得点击发布、群发或删除正式内容。
- 浏览器控制连接失败时，真实账号验证状态保持未完成；Electron 剪贴板冒烟、自动测试和验收记录功能不能代替公众号平台持久化结果。

## 二十六、真实截图反馈与替换体验第十七轮

### 1. 已验证结论

- 标准文章中的普通图片、行内公式、块级公式和 Mermaid 均已作为图片进入真实公众号草稿，图片生成、顺序和逐项位图复制链路可以继续保留。
- 原始 fantastic-editor CF_HTML 中的表格、编号列表、项目符号和代码块容器可见；公众号“一键排版”预览会把编号与项目符号变成普通段落，并移除代码块容器，因此验收流程禁止在粘贴后再次套用一键排版模板。
- 旧版虚线占位框在用户只替换内部文字时会留下空框。新版改用没有边框和固定高度的短文本标记；这是一项真实反馈驱动的兼容性修复。

### 2. 冻结替换动作

1. 粘贴正文；若存在建议标题，把建议标题填入公众号标题栏，正文不再包含重复 H1。
2. 行内项定位 `【FE公式NN｜行内替换】`，在原句内完整拖选标记并直接粘贴图片，不按 Enter；确认前后文字仍在同一段。
3. 块级项定位 `【FE图片NN｜整段替换】`、`【FE公式NN｜整段替换】` 或 `【FE流程图NN｜整段替换】`，完整选中整段标记后粘贴，不把图片追加在标记旁边。
4. 每项只有在图片可见且标记文字消失后才能人工确认。全部替换后搜索“【FE”，结果必须为零。
5. 保存草稿、关闭并重新打开，复核图片持久化、列表/代码样式和 FE 标记清除，再执行移动端预览。

### 3. Mermaid 清晰度

- 隔离渲染器采用更紧凑的 nodeSpacing/rankSpacing、18px 基准文字并在尺寸预算允许时输出 2 倍像素 PNG；公众号正文仍按内容宽度缩放显示，以提高高 DPI 屏幕和移动端的边缘清晰度。
- 横向节点过多导致的视觉字号偏小仍属于内容布局问题。用户可把 `flowchart LR` 改为 `flowchart TD`，应用不得擅自改变原 Mermaid 方向或语义。

## 二十七、空白编辑投影与分发形态回归

### 1. 空白临时段落隔离

- 所见即所得空白画布中的“开始输入…”仅是 Renderer 临时提示。它不得进入 canonical Markdown、公众号 HTML、CF_HTML、纯文本剪贴板、替换清单或人工验收记录。
- 同一文档最多保留一个空提示。重复点击不得增加公众号正文段落；失焦、Backspace、Delete、模式切换或公众号任务入口都必须清除仍为空的提示。
- `commitPending` 对空提示执行无事务丢弃，对非空新段落执行正式 Markdown 提交。只有提交成功并完成当前快照解析后，公众号适配器才能读取该段落。
- 固定回归必须断言连续点击空白区域后复制公众号，输出与未点击时完全一致；输入实际文字后，输出只新增一个对应段落且不包含提示文案。

### 2. 安装版与便携版一致性

- 开发运行、NSIS 安装版和单文件便携版必须对同一 documentId、sourceHash、主题和字体生成等价的预检诊断、CF_HTML、replacementItems、approvedOmittedReferenceKeys 和 OutputResult.status。
- 便携版不得使用可执行文件所在目录作为隐式工作区或图片授权根，也不得把临时解包目录写入正文地址。所有本地图片仍须经过现有工作区授权、内容哈希、派生资源和方案 B 替换链路。
- 发布前至少使用标准验收文章对便携版执行一次真实 Electron 剪贴板冒烟，确认输出不含 `file:`、`data:`、`blob:`、`fantastic-asset:`、`localhost` 或临时解包路径。真实公众号草稿持久化仍按本方案人工验收，不能由便携版启动成功代替。

## 二十八、正文主题与公众号封面对研究计划

### 1. 已安装研究工具

- 本机已安装 `guizang-social-card-skill`，供后续研究 Editorial / Swiss 视觉系统、主题令牌、版式选择、固定画板质量检查和公众号 21:9 + 1:1 封面对生成。它不随 fantastic-editor 分发，也不参与当前公众号复制任务。
- 上游 skill 面向 PNG 社交卡和封面，不直接解决可编辑长正文的微信兼容问题。其 1080/2100 像素字号、CSS Grid、背景纹理、WebGL、外部图源和 Playwright 截图流程不能直接进入 CF_HTML。

### 2. 正文主题验证路线

- 首批原创主题冻结为极简墨白、深蓝科技、微信原生增强；主题令牌经结构化公众号转换器编译成绝对行内样式，不复制上游模板、名称、素材或校验器代码。
- 每套主题必须使用标准验收文章覆盖标题、正文、粗体/斜体/删除线、链接、行内代码、引用、列表、表格、代码块、普通图片、行内/块级公式和 Mermaid。
- 自动检查覆盖 320px / 375px / 414px 横向溢出、局部滚动、最小字号、颜色对比度、标题前后实际间距、表格/代码宽度、残留 CSS 变量/class/伪元素、危险标签和本地临时地址。
- 真实公众号验收对每套主题分别执行粘贴、逐项替换、保存、关闭重开和移动端预览。相同正文和资源必须保持相同 replacementItems、placeholderText、approvedOmittedReferenceKeys 与 OutputResult.status。

#### 2026-08-28 第一、二阶段落地

- 导出菜单已增加“公众号正文主题”选择，内置微信原生增强、极简墨白、深蓝科技并持久化用户选择；选择只随 `BeginOutputRequest.wechatThemeId` 进入公众号任务。
- 主进程使用白名单 ID 解析独立实现的主题定义，生成绝对行内样式；未知 ID 在预检任务创建前拒绝。最终 HTML 继续禁止 style 标签、class、脚本、事件属性、本地协议和临时地址。
- `OutputResult` 与人工验收记录包含最终生效主题 ID。替换助手标题显示本次主题名称，避免用户在多主题回归时混淆验收对象。
- 自动测试已覆盖三主题 HTML 确实不同，同时 suggestedTitle、usedReferenceKeys、omittedReferenceKeys、replacementItems 和 OutputResult.status 保持一致。微信原生增强已在 `0.2.0-rc.2` 下完成真实粘贴、替换、保存、重开和移动端预览；深蓝科技有 `0.2.0-rc.1` 历史完整记录，极简墨白没有逐主题记录。当前还缺极简墨白与深蓝科技的 `rc.2` 记录。
- 主进程输出和 Renderer 主题预览已改为复用共享 `WechatThemeDefinition` 与行内编译器。预览保留本地图片、公式和 Mermaid 的安全渲染能力，但按真实投影移除首个 H1；切换主题不会启动输出任务或写剪贴板。
- 手机预览提供 320px / 375px / 414px 三档视口并自动并行运行三个隐藏布局探针；用户切换宽度只改变可视明细，不负责触发其余档位。每次主题、字体或 Mermaid 渲染变化后重跑全部审计。普通不可滚动元素溢出记为警告，代码块等显式局部滚动元素记为人工复核项；文字对比度低于 WCAG 阈值、标题前后实际间距低于字号比例阈值均记为警告。
- 为降低真实移动端风险，三主题的表格使用 `table-layout: fixed`，单元格、正文、列表、链接和代码增加断词/换行边界，图片与 SVG 预览限制为正文宽度。该本地结果不能代替真实公众号保存、重开和手机端验收，也不会自动勾选验收清单。
- `0.2.0-rc.1` Windows x64 便携候选版已经生成并通过隔离启动，供后续三套主题真实公众号回归使用。该 EXE 未签名，且生成成功不改变“每套主题必须分别粘贴、替换、保存、重开、手机预览”的验收要求。

### 3. 许可证与安全门槛

- 上游采用 AGPL-3.0 并提供商业授权路径。未完成许可证评估或取得作者书面授权前，不把其模板、CSS、脚本、字体、图片、提示词工作流或校验器打包进应用；默认只做独立实现的设计方法借鉴。
- 如果未来显式调用外部 skill，必须由用户发起，显示即将生成的目标和可能使用的素材来源；不得读取公众号账号、浏览器凭据或未授权文件，不得自动发布、群发或静默联网取图。

### 4. 封面对与正文隔离

- 公众号封面对是独立 P2 输出：生成 21:9 头图和 1:1 分享卡 PNG，由 `WechatCoverAdapter` 返回文件 artifact；不得混入正文 CF_HTML 或替换清单。
- 封面对验收检查精确尺寸、标题安全区、图片裁切、最小字号、PNG 有效字节、内容哈希、图源授权和许可证元数据。正文方案 B 的替换、部分完成和人工验收状态机不因封面对成功而改变。

## 二十九、公众号剪贴板双层安全审计回归

- WechatClipboardAdapter 生成完成后和主进程实际调用系统剪贴板前必须共用 `auditWechatHtmlMarkup`。审计仅检查标签与属性，不扫描正文文本节点；文章合法讨论 `file:`、`blob:`、`data:`、`app:`、`fantastic-asset:` 或 localhost 时不得误判。
- 标签或内联样式中的危险协议、loopback 地址，禁止标签、class/id 和事件属性继续产生 blocking 诊断，且不得写入剪贴板。放行说明文字不等于放宽资源地址安全边界。
- 回归样例必须同时覆盖：正文和代码块中的协议说明可复制；主题字体令牌注入 `data:` 被阻止；事件属性和禁止标签被阻止；适配器与主进程使用相同问题枚举。
- 系统剪贴板写入或 HTML 读回失败使用 `OUTPUT_CLIPBOARD_WRITE_FAILED`，不得再包装为 `OUTPUT_FILE_WRITE_FAILED`。
- 2026-08-28 用户已在重新构建的本机生产版中确认此前失败的正文可以写入系统 HTML 剪贴板。本项关闭双层误判回归，但不表示三套主题已分别通过真实公众号保存、关闭重开和手机端预览。
- 既有 `0.2.0-rc.1` 便携 EXE 不含本修复；下一候选版必须提升版本号、重新生成哈希和清单，并再次执行公众号剪贴板生产冒烟。
- `0.2.0-rc.2` 已按该要求生成：公众号剪贴板生产版复测由用户确认成功，便携 EXE 的 PE、版本、哈希和隔离启动检查通过。它可用于后续三主题逐套真实回归，但生成候选包本身不勾选粘贴、替换、保存、重开或手机预览状态。
- 当前逐主题状态以 `docs/release/0.2.0-rc.2-wechat-acceptance.md` 为唯一发行验收矩阵；旧记录只能作为历史证据，不得自动迁移勾选到新版本。
- 微信原生增强已由 `0.2.0-rc.2` 标准样例人工记录完成全部步骤，源哈希一致且无省略项；极简墨白和深蓝科技仍保持未完成。平台验收状态只按已归档记录更新，不根据本地预览或候选包生成结果推断。

## 三十、发布加固与公众号验收分流

- Windows 本机发布加固与公众号真实平台验收是两条独立状态线。测试、类型检查、生产构建、打包程序生产态冒烟、安装/启动/卸载和产物哈希全部通过，只能将候选标记为“本机发布加固完成”。
- 2026-08-29 `0.2.0-rc.2` 已完成基础启动、PDF、DOCX、离线 HTML、公式、Mermaid、完整 UI 共 7 组打包程序冒烟，以及 NSIS 隔离目录静默安装、已安装程序启动和静默卸载。该结果不勾选任何公众号平台步骤。
- 用户决定暂缓极简墨白与深蓝科技的进一步真实公众号验证。验收矩阵必须继续保留未完成状态，不能用本地主题预览、移动宽度审计或发布加固报告替代。
- 第二台 Windows 不再作为当前候选的发布阻断项；该决策与公众号粘贴、替换、保存、重开、手机预览和发布状态无关。
- 当前产物未做 Authenticode 签名。`NotSigned` 是发行元数据与用户提示边界，不改变方案 B 的 replacementItems、approvedOmittedReferenceKeys、五种任务终态或人工验收声明。
- 2026-08-29 已增加正式签名构建门禁：证书预检、RFC 3161 时间戳和最终 Authenticode `Valid` 为对外签名包的必要条件。签名状态不替代公众号真实草稿验收，也不扩大 API 权限。
- 由于 fantastic-editor 当前为公益免费项目，暂不采购商业代码签名证书；`0.3.0-rc.2` 可按未签名候选发布。发行清单和用户提示必须如实标记 `NotSigned`、提供 SHA-256，并说明 Windows 可能出现未知发布者或 SmartScreen 警告。
- `npm run dist:signed` 流程保留为未来可选能力；是否签名不改变公众号的单篇发布确认、图片、公式和 Mermaid 的替换与验收规则。
- 当前产品只支持单公众号账号；一键发布仅针对当前单篇文章且必须二次确认，不支持多账号、群发或定时群发。签名策略不改变这一边界。
- 资源管理器右键重命名、文件名下目录和 IMA 外部粘贴修复只影响 canonical Markdown 进入输出任务之前的编辑体验；公众号适配器仍只读取已提交的 ParsedDocument/ResolutionSnapshot。IMA 剪贴板源缺失的正文不得由公众号任务猜测补齐。

## 三十一、可访问性改进对公众号流程的隔离

- 状态 live region、文档标签语义、键盘分栏和解析重试属于编辑器通用 UI，不改变公众号 HTML、主题编译、替换项顺序或人工验收状态机。
- 解析失败后重新解析成功，只恢复当前文档的预览与输出就绪条件；既有公众号 jobId、已复制/已确认替换项和验收勾选必须按现有文档身份变化规则失效，不能跨重试猜测复用。
- 诊断区的“清除提示”不等于批准省略或解除 blocking。公众号导出仍必须重新执行预检，并只接受一次任务范围的 approvedOmittedReferenceKeys。
- 本轮不恢复已暂缓的极简墨白与深蓝科技真实平台验证，也不生成或覆盖 `0.2.0-rc.2` 发行产物。

## 三十二、主题预览模态可访问性隔离

- 公众号主题手机预览是 Renderer 模态检查界面。打开后焦点进入弹窗，Tab/Shift+Tab 只在弹窗控件间循环，Escape 关闭并把焦点返回“导出”入口；背景文档的 Ctrl+Tab、Ctrl+Shift+Tab 和 Ctrl+W 在此期间暂停。
- 弹窗通过 `aria-labelledby`、`aria-describedby` 描述目的，320px、375px、414px 宽度按钮以 `aria-pressed` 表达当前档位。以上属性不进入导出 HTML、主题定义、替换图片清单或人工验收记录。
- 关闭主题预览不会创建 OutputResult、不会勾选 `mobilePreviewed`，也不会保留或批准省略项。焦点回归成功只是编辑器可访问性验收，不是公众号平台移动端验收。
- 真实 Electron UI 冒烟必须验证初始焦点、Escape 关闭、焦点回归和背景快捷键隔离；三主题的真实平台验收继续按当前产品决策保持暂缓状态。

## 三十三、工作区体验改进与公众号任务隔离

- 文档解析/资源耗时指标、标签排序和最近文件属于编辑器工作区 UI，不改变公众号 HTML、主题编译、replacementItems、approvedOmittedReferenceKeys 或五种输出终态。
- 标签重排不改变 documentId 或 sourceHash，但切换当前文档仍按既有规则使不匹配的公众号任务状态失效；不得因为标签只是换位就复用另一文档的 jobId。
- 最近文件打开必须重新经历解析、资源授权和公众号预检，历史 recentId 不携带任何输出批准或人工验收状态。Renderer 不获得最近文件路径，因此该入口不能绕过单文件授权边界。
- 性能分级为 slow 只提示本机交互耗时，不等于公众号兼容性失败；真实平台验收和当前暂缓状态不受影响。

## 三十四、0.2.0-rc.3 发行与平台验收边界

- `0.2.0-rc.3` 已通过公众号 HTML 生产态冒烟、完整 UI 冒烟和整个本机发行门禁；这证明本地适配器和剪贴板生成链路可运行，不证明任一新主题已在真实公众号保存、重开或手机预览。
- 结构化编辑旧投影保护适用于公式和 Mermaid 源码面板，避免连续编辑时把相邻 fenced code 内容误当目标资源；它不改变公式/流程图的 replacementItems 编号或已归档验收记录。
- 当前没有新增 `rc.3` 真实公众号人工记录。此前 `rc.2` 微信原生增强记录仍是历史证据，不自动迁移为 `rc.3` 三主题通过。
